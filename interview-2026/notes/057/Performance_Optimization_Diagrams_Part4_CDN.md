# Performance Optimization - Complete Diagrams Guide (Part 4: CDN)

## 🌐 CDN: Content Delivery, Edge Caching, Geographic Distribution

---

## 1. CDN Architecture

### CDN Overview
```
┌─────────────────────────────────────────────────────────────┐
│              CDN Architecture                              │
└─────────────────────────────────────────────────────────────┘

User (Tokyo)
    │
    │ Request: example.com/image.jpg
    ▼
┌─────────────────────────────────────────────────────────┐
│  DNS Resolution                                          │
│  • Returns CDN edge server IP (closest to user)        │
│  • Based on geographic location                          │
└──────────────┬──────────────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────────────┐
│  CDN Edge Server (Tokyo)                                 │
│  ┌──────────────────────────────────────────────────┐  │
│  │  Check Cache                                     │  │
│  │  ├───► Cache Hit? ──► Return cached content     │  │
│  │  │     (Fast, <50ms)                             │  │
│  │  │                                                │  │
│  │  └───► Cache Miss? ──► Fetch from origin         │  │
│  │        (Slower, ~200ms)                          │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
               │
               │ Cache Miss
               ▼
┌─────────────────────────────────────────────────────────┐
│  Origin Server (US East)                                  │
│  • Original content source                               │
│  • Serves content to edge servers                        │
└─────────────────────────────────────────────────────────┘
```

### CDN Network Topology
```
┌─────────────────────────────────────────────────────────────┐
│              Global CDN Network                            │
└─────────────────────────────────────────────────────────────┘

                    Origin Server
                    (US East)
                         │
                         │
        ┌────────────────┼────────────────┐
        │                │                │
        │                │                │
    ┌───▼───┐       ┌───▼───┐       ┌───▼───┐
    │ Edge  │       │ Edge  │       │ Edge  │
    │ (EU)  │       │ (US)  │       │ (APAC)│
    └───┬───┘       └───┬───┘       └───┬───┘
        │               │               │
        │               │               │
    ┌───┴───┐       ┌───┴───┐       ┌───┴───┐
    │Users  │       │Users  │       │Users  │
    │(EU)   │       │(US)   │       │(APAC) │
    └───────┘       └───────┘       └───────┘

Edge Locations:
• Distributed globally
• Close to end users
• Reduced latency
• Lower bandwidth costs
```

---

## 2. CDN Caching Strategy

### Cache Hierarchy
```
┌─────────────────────────────────────────────────────────────┐
│              CDN Cache Layers                               │
└─────────────────────────────────────────────────────────────┘

User Request
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│  Layer 1: Edge Cache (L1)                               │
│  • Closest to user                                       │
│  • Fastest response                                      │
│  • Limited capacity                                       │
└──────────────┬──────────────────────────────────────────┘
               │ Cache Miss
               ▼
┌─────────────────────────────────────────────────────────┐
│  Layer 2: Regional Cache (L2)                           │
│  • Regional distribution                                 │
│  • Larger capacity                                       │
│  • Slightly higher latency                               │
└──────────────┬──────────────────────────────────────────┘
               │ Cache Miss
               ▼
┌─────────────────────────────────────────────────────────┐
│  Layer 3: Origin Shield                                  │
│  • Protects origin server                                │
│  • Reduces origin load                                   │
│  • Last cache layer before origin                        │
└──────────────┬──────────────────────────────────────────┘
               │ Cache Miss
               ▼
┌─────────────────────────────────────────────────────────┐
│  Origin Server                                           │
│  • Source of truth                                       │
│  • Slowest response                                      │
└─────────────────────────────────────────────────────────┘
```

### Cache-Control Headers
```
┌─────────────────────────────────────────────────────────────┐
│              HTTP Cache Headers                             │
└─────────────────────────────────────────────────────────────┘

Cache-Control Directives
─────────────────────────────────────────────────────────────
public
    • Can be cached by any cache (CDN, browser)
    • Example: public, max-age=3600

private
    • Only browser can cache, not CDN
    • Example: private, max-age=3600

max-age=<seconds>
    • How long content is fresh
    • Example: max-age=3600 (1 hour)

s-maxage=<seconds>
    • Max age for shared caches (CDN)
    • Overrides max-age for CDN
    • Example: s-maxage=86400, max-age=3600

no-cache
    • Must revalidate before serving
    • Example: no-cache

no-store
    • Don't cache at all
    • Example: no-store

must-revalidate
    • Must check with origin if stale
    • Example: must-revalidate

Example Headers:
─────────────────────────────────────────────────────────────
Static Content (images, CSS, JS):
    Cache-Control: public, max-age=31536000, immutable

Dynamic Content (API responses):
    Cache-Control: public, s-maxage=300, max-age=60

Private Content (user-specific):
    Cache-Control: private, max-age=3600

No Cache (sensitive data):
    Cache-Control: no-store, no-cache, must-revalidate
```

### Cache Key Components
```
┌─────────────────────────────────────────────────────────────┐
│              CDN Cache Key                                  │
└─────────────────────────────────────────────────────────────┘

Cache Key = f(URL, Headers, Query Parameters)

Components:
─────────────────────────────────────────────────────────────
1. URL Path
   example.com/products/item123
   → Always included

2. Query Parameters (configurable)
   example.com/search?q=shoes&page=1
   → Include if affects content
   → Exclude if tracking only (?utm_source)

3. Headers (configurable)
   Accept-Language: en-US
   Accept-Encoding: gzip
   → Include for content negotiation

4. Cookies (usually excluded)
   session_id=abc123
   → Exclude for public content
   → Include for personalized content

Example Configurations:
─────────────────────────────────────────────────────────────
Static Assets:
    Key = URL only
    example.com/images/logo.png

API with Query Params:
    Key = URL + Query String
    example.com/api/products?category=electronics

Multi-language:
    Key = URL + Accept-Language header
    example.com/page + Accept-Language: en-US

Personalized:
    Key = URL + Cookie
    example.com/dashboard + session_id
```

---

## 3. CDN Features

### Content Optimization
```
┌─────────────────────────────────────────────────────────────┐
│              CDN Content Optimization                      │
└─────────────────────────────────────────────────────────────┘

1. Image Optimization
─────────────────────────────────────────────────────────────
    Original: image.jpg (2MB)
        │
        │ CDN Processing
        ▼
    ┌─────────────────────────────────────┐
    │  Format Conversion                   │
    │  • WebP for modern browsers          │
    │  • JPEG/PNG fallback                 │
    │                                      │
    │  Resizing                            │
    │  • On-the-fly resizing               │
    │  • ?width=800&height=600             │
    │                                      │
    │  Compression                         │
    │  • Quality optimization               │
    │  • Lossless/lossy                    │
    └─────────────────────────────────────┘
        │
        ▼
    Optimized: image.webp (200KB)
    → 90% size reduction

2. Compression
─────────────────────────────────────────────────────────────
    Original: styles.css (500KB)
        │
        │ CDN Compression
        ▼
    Compressed: styles.css.gz (100KB)
    → 80% size reduction
    → Content-Encoding: gzip

3. Minification
─────────────────────────────────────────────────────────────
    Original: app.js (1MB, formatted)
        │
        │ CDN Minification
        ▼
    Minified: app.min.js (400KB, minified)
    → 60% size reduction
```

### Security Features
```
┌─────────────────────────────────────────────────────────────┐
│              CDN Security                                  │
└─────────────────────────────────────────────────────────────┘

1. DDoS Protection
─────────────────────────────────────────────────────────────
    Attack Traffic
        │
        ▼
    ┌──────────────┐
    │  CDN Edge    │  ← Absorbs attack
    │  (Distributed)│
    └──────────────┘
        │
        │ Filtered traffic
        ▼
    ┌──────────────┐
    │  Origin      │  ← Protected
    └──────────────┘

2. SSL/TLS Termination
─────────────────────────────────────────────────────────────
    User ──HTTPS──► CDN ──HTTPS──► Origin
           (TLS)          (TLS)

    • CDN handles SSL certificates
    • Reduces origin server load
    • Supports HTTP/2, HTTP/3

3. WAF (Web Application Firewall)
─────────────────────────────────────────────────────────────
    Request
        │
        ▼
    ┌──────────────┐
    │  WAF Rules   │  ← Block malicious requests
    │  • SQLi      │
    │  • XSS       │
    │  • Rate limit│
    └──────┬───────┘
           │
           │ Valid requests
           ▼
    ┌──────────────┐
    │  Origin      │
    └──────────────┘
```

---

## 4. CDN Caching Strategies

### Cache Warming
```
┌─────────────────────────────────────────────────────────────┐
│              Cache Warming Strategy                         │
└─────────────────────────────────────────────────────────────┘

Pre-populate CDN cache before traffic spike
─────────────────────────────────────────────────────────────

1. Identify Popular Content
   • Top 1000 products
   • Homepage assets
   • Common API responses

2. Warm Cache
   ┌──────────┐
   │  Origin  │
   └────┬─────┘
        │
        │ Prefetch requests
        ▼
   ┌──────────┐
   │   CDN    │  ← Cache populated
   │  (Edge)  │
   └──────────┘

3. Benefits
   • Reduced origin load during traffic spike
   • Better user experience
   • Predictable performance

Implementation:
─────────────────────────────────────────────────────────────
# Script to warm cache
for url in popular_urls:
    curl -H "X-Cache-Warm: true" $url
```

### Cache Invalidation
```
┌─────────────────────────────────────────────────────────────┐
│              CDN Cache Invalidation                        │
└─────────────────────────────────────────────────────────────┘

1. TTL-Based (Automatic)
─────────────────────────────────────────────────────────────
    Cache-Control: max-age=3600
        │
        │ After 1 hour
        ▼
    Cache expires automatically
        │
        ▼
    Next request fetches fresh from origin

2. Manual Invalidation
─────────────────────────────────────────────────────────────
    Content Updated
        │
        │ API Call
        ▼
    ┌──────────────┐
    │  CDN API     │
    │  /purge      │
    └──────┬───────┘
           │
           │ Invalidate
           ▼
    ┌──────────────┐
    │  Edge Cache  │  ← Cleared
    └──────────────┘

    Methods:
    • Purge by URL: /purge?url=/path/to/file
    • Purge by pattern: /purge?pattern=/images/*
    • Purge all: /purge?all=true

3. Version-Based
─────────────────────────────────────────────────────────────
    Original: /images/logo.png
        │
        │ Update
        ▼
    Versioned: /images/logo.v2.png
        │
        │ Different URL = cache miss
        ▼
    CDN fetches new version

    Benefits:
    • No invalidation needed
    • Instant updates
    • Can serve old version during transition
```

---

## 5. CDN Performance Metrics

### Key Performance Indicators
```
┌─────────────────────────────────────────────────────────────┐
│              CDN Performance Metrics                        │
└─────────────────────────────────────────────────────────────┘

1. Cache Hit Ratio
─────────────────────────────────────────────────────────────
    Hit Ratio = (Cache Hits / Total Requests) × 100%

    Target: >90% for static content
            >70% for dynamic content

    Monitoring:
    • Track by content type
    • Alert if <80%
    • Optimize TTL if low

2. Response Time
─────────────────────────────────────────────────────────────
    Cache Hit:  <50ms (from edge)
    Cache Miss: 200-500ms (from origin via edge)

    P50 (Median): <100ms
    P95: <200ms
    P99: <500ms

3. Bandwidth Savings
─────────────────────────────────────────────────────────────
    Savings = (Origin Bandwidth - CDN Bandwidth) / Origin Bandwidth

    Target: >80% bandwidth savings

4. Origin Offload
─────────────────────────────────────────────────────────────
    Offload = (Requests served by CDN / Total Requests) × 100%

    Target: >90% requests served by CDN

5. Geographic Distribution
─────────────────────────────────────────────────────────────
    • Latency by region
    • Cache hit ratio by region
    • Identify regions needing more edge servers
```

---

## 6. CDN Best Practices

### CDN Configuration Checklist
```
┌─────────────────────────────────────────────────────────────┐
│              CDN Best Practices                            │
└─────────────────────────────────────────────────────────────┘

Content Strategy:
☐ Cache static assets aggressively (long TTL)
☐ Cache dynamic content with shorter TTL
☐ Use versioned URLs for cache busting
☐ Separate cache policies by content type

Cache Headers:
☐ Set appropriate Cache-Control headers
☐ Use s-maxage for CDN, max-age for browser
☐ Implement ETag for validation
☐ Use Vary header for content negotiation

Security:
☐ Enable HTTPS (TLS termination at CDN)
☐ Configure WAF rules
☐ Use signed URLs for private content
☐ Implement rate limiting

Performance:
☐ Enable compression (gzip, brotli)
☐ Optimize images (WebP, resizing)
☐ Minify CSS/JS
☐ Use HTTP/2 or HTTP/3

Monitoring:
☐ Track cache hit ratio
☐ Monitor response times
☐ Alert on origin load spikes
☐ Analyze geographic performance

Cost Optimization:
☐ Use appropriate cache TTLs
☐ Purge cache strategically (not too often)
☐ Monitor bandwidth usage
☐ Choose right CDN plan for traffic patterns
```

---

## Key Takeaways

### Summary
```
┌─────────────────────────────────────────────────────────────┐
│              CDN Strategy Summary                          │
└─────────────────────────────────────────────────────────────┘

1. Architecture
   • Edge servers close to users
   • Multi-layer caching
   • Origin protection

2. Caching Strategy
   • Long TTL for static content
   • Shorter TTL for dynamic content
   • Version-based URLs for updates

3. Performance
   • Cache hit ratio >90%
   • Response time <100ms (P95)
   • 80%+ bandwidth savings

4. Security
   • TLS termination
   • DDoS protection
   • WAF integration

5. Optimization
   • Image optimization
   • Compression
   • Minification

Remember:
• CDN reduces latency and origin load
• Proper cache headers are critical
• Monitor cache hit ratio
• Balance freshness vs performance
• Use versioned URLs for updates
```

---

**Next: Part 5 will cover Load Balancing.**

