# Java Enterprise Technologies - In-Depth Diagrams (Part 6: Vert.x)

## ⚡ Vert.x - Reactive Toolkit & Event-Driven Architecture

---

## 1. Vert.x Architecture

### Vert.x Core Concepts
```
┌─────────────────────────────────────────────────────────────┐
│              Vert.x Architecture                            │
└─────────────────────────────────────────────────────────────┘

Application Layer
┌─────────────────────────────────────┐
│  ┌──────────┐  ┌──────────┐        │
│  │ Verticles│  │ Workers │        │
│  │ (Event   │  │ (Blocking│       │
│  │  Loop)   │  │  Tasks) │        │
│  └──────────┘  └──────────┘        │
└─────────────────────────────────────┘
    │
    │ Uses
    ▼
┌─────────────────────────────────────┐
│  Vert.x Core                        │
│  ┌──────────────────────────────┐  │
│  │ Event Loop                    │  │
│  │  ├─ Single-threaded per core  │  │
│  │  ├─ Non-blocking I/O         │  │
│  │  └─ Event-driven              │  │
│  └──────────────────────────────┘  │
│  ┌──────────────────────────────┐  │
│  │ Event Bus                     │  │
│  │  ├─ Message passing           │  │
│  │  ├─ Pub/Sub                   │  │
│  │  └─ Point-to-point            │  │
│  └──────────────────────────────┘  │
│  ┌──────────────────────────────┐  │
│  │ Async APIs                    │  │
│  │  ├─ Future<T>                 │  │
│  │  └─ Promise<T>                 │  │
│  └──────────────────────────────┘  │
└─────────────────────────────────────┘
    │
    │ Built on
    ▼
┌─────────────────────────────────────┐
│  Netty (NIO Framework)              │
│  ┌──────────────────────────────┐  │
│  │  ├─ Non-blocking I/O         │  │
│  │  ├─ Event loop                │  │
│  │  └─ High performance          │  │
│  └──────────────────────────────┘  │
└─────────────────────────────────────┘
```

### Event Loop Model
```
┌─────────────────────────────────────────────────────────────┐
│              Event Loop Architecture                        │
└─────────────────────────────────────────────────────────────┘

Vert.x Instance
┌─────────────────────────────────────┐
│  ┌──────────────────────────────┐  │
│  │ Event Loop 1                  │  │
│  │  ├─ Thread: event-loop-1      │  │
│  │  ├─ Handles: HTTP, TCP, etc.   │  │
│  │  └─ Non-blocking              │  │
│  └──────────────────────────────┘  │
│  ┌──────────────────────────────┐  │
│  │ Event Loop 2                  │  │
│  │  ├─ Thread: event-loop-2      │  │
│  │  ├─ Handles: Different verticles│ │
│  │  └─ Non-blocking              │  │
│  └──────────────────────────────┘  │
│  ┌──────────────────────────────┐  │
│  │ Worker Pool                   │  │
│  │  ├─ Threads: worker-1,2,3... │  │
│  │  ├─ Handles: Blocking tasks   │  │
│  │  └─ Blocking allowed          │  │
│  └──────────────────────────────┘  │
└─────────────────────────────────────┘

Event Loop Execution:
┌─────────────────────────────────────┐
│  while (running) {                  │
│      // Poll for events             │
│      events = pollEvents();         │
│                                     │
│      // Process events              │
│      for (Event event : events) {   │
│          handler.handle(event);     │
│      }                              │
│                                     │
│      // Run timers                  │
│      runTimers();                   │
│  }                                  │
└─────────────────────────────────────┘
```

---

## 2. Verticles

### Verticle Types
```
┌─────────────────────────────────────────────────────────────┐
│              Verticle Types                                  │
└─────────────────────────────────────────────────────────────┘

Standard Verticle
┌─────────────────────────────────────┐
│  public class HttpVerticle           │
│      extends AbstractVerticle {     │
│                                      │
│      @Override                       │
│      public void start() {           │
│          vertx.createHttpServer()    │
│              .requestHandler(req -> {│
│                  // Handle request  │
│              })                      │
│              .listen(8080);         │
│      }                               │
│  }                                  │
│                                     │
│  Runs on: Event Loop                │
│  Thread: Single event loop thread   │
│  Blocking: Never block!             │
└─────────────────────────────────────┘

Worker Verticle
┌─────────────────────────────────────┐
│  public class DatabaseVerticle       │
│      extends AbstractVerticle {     │
│                                      │
│      @Override                       │
│      public void start() {           │
│          // Can block                │
│          String result =             │
│              blockingDatabaseCall(); │
│      }                               │
│  }                                  │
│                                     │
│  Deployment:                        │
│  vertx.deployVerticle(               │
│      new DatabaseVerticle(),        │
│      new DeploymentOptions()         │
│          .setWorker(true));          │
│                                     │
│  Runs on: Worker Pool                │
│  Thread: Worker thread              │
│  Blocking: Allowed                  │
└─────────────────────────────────────┘

Multi-Threaded Worker
┌─────────────────────────────────────┐
│  Deployment:                        │
│  vertx.deployVerticle(               │
│      new Verticle(),                 │
│      new DeploymentOptions()         │
│          .setWorker(true)            │
│          .setMultiThreaded(true));   │
│                                     │
│  Runs on: Worker Pool                │
│  Thread: Multiple worker threads    │
│  Blocking: Allowed                  │
│  Concurrency: Thread-safe required  │
└─────────────────────────────────────┘
```

### Verticle Lifecycle
```
┌─────────────────────────────────────────────────────────────┐
│              Verticle Lifecycle                            │
└─────────────────────────────────────────────────────────────┘

1. Deployment
   ┌──────────────────────┐
   │ vertx.deployVerticle(  │
   │   new MyVerticle());   │
   └──────────────────────┘
   │
   ▼
2. start()
   ┌──────────────────────┐
   │ @Override            │
   │ public void start() {│
   │     // Initialize    │
   │     // Start server  │
   │ }                    │
   └──────────────────────┘
   │
   ▼
3. Running
   ┌──────────────────────┐
   │ Verticle is active   │
   │ Processing events    │
   └──────────────────────┘
   │
   │ (Until undeploy)
   │
   ▼
4. stop()
   ┌──────────────────────┐
   │ @Override            │
   │ public void stop() { │
   │     // Cleanup       │
   │     // Close resources│
   │ }                    │
   └──────────────────────┘
```

---

## 3. Event Bus

### Event Bus Architecture
```
┌─────────────────────────────────────────────────────────────┐
│              Vert.x Event Bus                              │
└─────────────────────────────────────────────────────────────┘

Verticle A                    Event Bus                  Verticle B
┌──────────────┐            ┌──────────────┐          ┌──────────────┐
│              │            │              │          │              │
│  Sender      │───────────►│  Message     │─────────►│  Consumer    │
│              │  Publish   │  Router      │  Deliver │              │
│              │            │              │          │              │
└──────────────┘            └──────────────┘          └──────────────┘
    │                            │                          │
    │                            │                          │
    │                            │                          │
    └────────────────────────────┴──────────────────────────┘
                                 │
                                 │
                    ┌────────────┴────────────┐
                    │                         │
              Point-to-Point            Publish/Subscribe
```

### Message Patterns

#### Point-to-Point
```
┌─────────────────────────────────────────────────────────────┐
│              Point-to-Point Messaging                      │
└─────────────────────────────────────────────────────────────┘

Sender
┌─────────────────────────────────────┐
│  eventBus.send(                     │
│      "user.create",                  │
│      new User("John"));              │
└─────────────────────────────────────┘
    │
    │ Message
    ▼
Event Bus
┌─────────────────────────────────────┐
│  Routes to one consumer             │
│  (Round-robin if multiple)          │
└─────────────────────────────────────┘
    │
    │ Delivers to
    ▼
Consumer
┌─────────────────────────────────────┐
│  eventBus.consumer(                 │
│      "user.create",                  │
│      message -> {                    │
│          User user =                 │
│              (User) message.body(); │
│          // Process                  │
│          message.reply("OK");        │
│      });                             │
└─────────────────────────────────────┘
```

#### Publish/Subscribe
```
┌─────────────────────────────────────────────────────────────┐
│              Publish/Subscribe Messaging                    │
└─────────────────────────────────────────────────────────────┘

Publisher
┌─────────────────────────────────────┐
│  eventBus.publish(                  │
│      "news.update",                  │
│      new News("Breaking news"));     │
└─────────────────────────────────────┘
    │
    │ Broadcast
    ▼
Event Bus
┌─────────────────────────────────────┐
│  Delivers to ALL subscribers         │
└─────────────────────────────────────┘
    │
    │ Multiple consumers
    ▼
Subscriber 1          Subscriber 2          Subscriber 3
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│ eventBus.    │    │ eventBus.    │    │ eventBus.    │
│ consumer(    │    │ consumer(    │    │ consumer(    │
│   "news.     │    │   "news.     │    │   "news.     │
│   update",   │    │   update",   │    │   update",   │
│   handler);  │    │   handler);  │    │   handler);  │
└──────────────┘    └──────────────┘    └──────────────┘
```

#### Request-Reply
```
┌─────────────────────────────────────────────────────────────┐
│              Request-Reply Pattern                         │
└─────────────────────────────────────────────────────────────┘

Requestor
┌─────────────────────────────────────┐
│  eventBus.request(                  │
│      "user.get",                    │
│      userId,                        │
│      reply -> {                     │
│          if (reply.succeeded()) {    │
│              User user =             │
│                  (User) reply        │
│                      .result()       │
│                      .body();        │
│          }                           │
│      });                             │
└─────────────────────────────────────┘
    │
    │ Request
    ▼
Event Bus
┌─────────────────────────────────────┐
│  Routes to handler                  │
└─────────────────────────────────────┘
    │
    │ Delivers
    ▼
Handler
┌─────────────────────────────────────┐
│  eventBus.consumer(                 │
│      "user.get",                    │
│      message -> {                   │
│          Long id =                   │
│              (Long) message.body(); │
│          User user = findById(id);  │
│          message.reply(user);       │  ← Reply
│      });                             │
└─────────────────────────────────────┘
    │
    │ Reply
    ▼
Requestor receives reply
```

---

## 4. Async Programming

### Future-Based Async
```
┌─────────────────────────────────────────────────────────────┐
│              Future-Based Async Operations                  │
└─────────────────────────────────────────────────────────────┘

Composing Futures
┌─────────────────────────────────────┐
│  Future<String> future1 =            │
│      vertx.createHttpClient()        │
│          .get(8080, "localhost", "/") │
│          .compose(response ->        │
│              response.body());       │
│                                     │
│  Future<String> future2 =            │
│      future1.compose(body -> {      │
│          // Transform                │
│          return process(body);       │
│      });                             │
│                                     │
│  future2.onSuccess(result -> {      │
│      System.out.println(result);    │
│  }).onFailure(error -> {            │
│      error.printStackTrace();       │
│  });                                 │
└─────────────────────────────────────┘

Parallel Execution
┌─────────────────────────────────────┐
│  Future<String> f1 = httpCall1();    │
│  Future<String> f2 = httpCall2();    │
│  Future<String> f3 = httpCall3();    │
│                                     │
│  CompositeFuture.all(f1, f2, f3)    │
│      .onSuccess(result -> {         │
│          // All succeeded           │
│      })                              │
│      .onFailure(error -> {          │
│          // One failed              │
│      });                             │
└─────────────────────────────────────┘
```

### Promise Pattern
```
┌─────────────────────────────────────────────────────────────┐
│              Promise Pattern                                │
└─────────────────────────────────────────────────────────────┘

Creating Promise
┌─────────────────────────────────────┐
│  Promise<String> promise =           │
│      Promise.promise();              │
│                                     │
│  // Async operation                 │
│  asyncOperation(result -> {         │
│      if (success) {                 │
│          promise.complete(result);  │
│      } else {                        │
│          promise.fail(error);       │
│      }                               │
│  });                                 │
│                                     │
│  return promise.future();            │
└─────────────────────────────────────┘

Using Promise
┌─────────────────────────────────────┐
│  Future<String> future =             │
│      createPromise();                │
│                                     │
│  future                             │
│      .onSuccess(result -> {         │
│          // Handle success           │
│      })                              │
│      .onFailure(error -> {          │
│          // Handle failure          │
│      });                             │
└─────────────────────────────────────┘
```

---

## 5. HTTP Server

### HTTP Server Setup
```
┌─────────────────────────────────────────────────────────────┐
│              Vert.x HTTP Server                             │
└─────────────────────────────────────────────────────────────┘

Server Creation
┌─────────────────────────────────────┐
│  HttpServer server =                │
│      vertx.createHttpServer();      │
│                                     │
│  server.requestHandler(req -> {    │
│      // Handle request              │
│      req.response()                 │
│          .putHeader("Content-Type", │
│              "text/plain")          │
│          .end("Hello Vert.x!");    │
│  });                                 │
│                                     │
│  server.listen(8080, result -> {   │
│      if (result.succeeded()) {      │
│          System.out.println(        │
│              "Server started");     │
│      } else {                        │
│          result.cause()             │
│              .printStackTrace();    │
│      }                               │
│  });                                 │
└─────────────────────────────────────┘
```

### Router Pattern
```
┌─────────────────────────────────────────────────────────────┐
│              Router-Based Routing                          │
└─────────────────────────────────────────────────────────────┘

Router Setup
┌─────────────────────────────────────┐
│  Router router = Router.router(vertx);│
│                                     │
│  router.get("/users/:id")           │
│      .handler(ctx -> {              │
│          Long id = Long.parseLong(  │
│              ctx.pathParam("id"));  │
│          // Get user                │
│          ctx.response()            │
│              .putHeader(            │
│                  "Content-Type",    │
│                  "application/json")│
│              .end(json.encode(user));│
│      });                             │
│                                     │
│  router.post("/users")              │
│      .handler(BodyHandler.create()) │
│      .handler(ctx -> {              │
│          User user = json.decodeValue(│
│              ctx.getBodyAsString(), │
│              User.class);           │
│          // Create user             │
│          ctx.response()            │
│              .setStatusCode(201)   │
│              .end();                │
│      });                             │
│                                     │
│  HttpServer server =                │
│      vertx.createHttpServer()       │
│          .requestHandler(router)    │
│          .listen(8080);             │
└─────────────────────────────────────┘
```

---

## Key Concepts Summary

### Vert.x Core
```
Event Loop: Single-threaded, non-blocking
Verticles: Deployment units
Event Bus: Message passing
Async APIs: Future, Promise
```

### Event Bus Patterns
```
Point-to-Point: One consumer
Publish/Subscribe: Multiple consumers
Request-Reply: Async RPC
```

### Async Programming
```
Future: Composable async operations
Promise: Manual completion
Non-blocking: Never block event loop
```

### HTTP
```
HttpServer: Non-blocking HTTP server
Router: RESTful routing
BodyHandler: Request body parsing
```

---

**This completes all 6 parts of Java Enterprise Technologies!**

**Summary:**
- Part 1: Jakarta EE (Servlets, JPA, CDI, JAX-RS, JAX-WS)
- Part 2: Hibernate (ORM, Relationships, Caching, Optimization)
- Part 3: JPA (Entity Mapping, Relationships, Inheritance, Criteria API)
- Part 4: Quarkus (Cloud-native, GraalVM Native Images)
- Part 5: Micronaut (Dependency Injection, Compile-time Optimization)
- Part 6: Vert.x (Reactive Toolkit, Event-Driven Architecture)

All diagrams are in ASCII/text format for easy understanding! 🚀

