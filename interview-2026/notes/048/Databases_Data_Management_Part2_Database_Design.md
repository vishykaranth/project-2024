# Databases & Data Management - In Depth Diagrams (Part 2: Database Design)

## 🏗️ Database Design: Normalization, Denormalization, Indexing Strategies

---

## 1. Database Normalization

### First Normal Form (1NF)
```
┌─────────────────────────────────────────────────────────────┐
│              First Normal Form (1NF)                        │
└─────────────────────────────────────────────────────────────┘

Before (Not 1NF):
┌─────┬───────┬──────────────────────┐
│ id  │ name  │      courses         │
├─────┼───────┼──────────────────────┤
│  1  │ John  │ Math, Physics, Chem  │  ← Multiple values
│  2  │ Jane  │ Math, Biology       │
└─────┴───────┴──────────────────────┘

After (1NF):
┌─────┬───────┬─────────┐
│ id  │ name  │ course  │
├─────┼───────┼─────────┤
│  1  │ John  │ Math    │
│  1  │ John  │ Physics │
│  1  │ John  │ Chem    │
│  2  │ Jane  │ Math    │
│  2  │ Jane  │ Biology │
└─────┴───────┴─────────┘

Rules:
✓ Atomic values (no multi-value attributes)
✓ Each cell contains single value
✓ No repeating groups
```

### Second Normal Form (2NF)
```
┌─────────────────────────────────────────────────────────────┐
│              Second Normal Form (2NF)                       │
└─────────────────────────────────────────────────────────────┘

Before (1NF but not 2NF):
┌─────────┬───────┬─────────┬──────────┐
│ order_id│ cust  │ product │ quantity │
├─────────┼───────┼─────────┼──────────┤
│  101    │ John  │ Laptop  │    2     │
│  101    │ John  │ Mouse   │    5     │
│  102    │ Jane  │ Laptop  │    1     │
└─────────┴───────┴─────────┴──────────┘
    PK: (order_id, product)
    
Problem: cust depends only on order_id (partial dependency)

After (2NF):
Orders Table:              OrderItems Table:
┌─────────┬───────┐       ┌─────────┬─────────┬──────────┐
│order_id │ cust  │       │order_id │ product │ quantity │
├─────────┼───────┤       ├─────────┼─────────┼──────────┤
│  101    │ John  │       │  101    │ Laptop  │    2     │
│  102    │ Jane  │       │  101    │ Mouse   │    5     │
└─────────┴───────┘       │  102    │ Laptop  │    1     │
                           └─────────┴─────────┴──────────┘
    PK: order_id              PK: (order_id, product)
    
Rules:
✓ Must be in 1NF
✓ No partial dependencies
✓ All non-key attributes fully depend on primary key
```

### Third Normal Form (3NF)
```
┌─────────────────────────────────────────────────────────────┐
│              Third Normal Form (3NF)                        │
└─────────────────────────────────────────────────────────────┘

Before (2NF but not 3NF):
┌─────┬───────┬─────────┬──────────┐
│ id  │ name  │ dept_id │ dept_loc │
├─────┼───────┼─────────┼──────────┤
│  1  │ John  │   10    │   NYC    │
│  2  │ Jane  │   10    │   NYC    │
│  3  │ Bob   │   20    │   LA     │
└─────┴───────┴─────────┴──────────┘
    PK: id
    
Problem: dept_loc depends on dept_id (transitive dependency)

After (3NF):
Employees Table:          Departments Table:
┌─────┬───────┬─────────┐  ┌─────────┬──────────┐
│ id  │ name  │ dept_id │  │ dept_id │ dept_loc │
├─────┼───────┼─────────┤  ├─────────┼──────────┤
│  1  │ John  │   10    │  │   10    │   NYC    │
│  2  │ Jane  │   10    │  │   20    │   LA     │
│  3  │ Bob   │   20    │  └─────────┴──────────┘
└─────┴───────┴─────────┘
    PK: id              PK: dept_id
    
Rules:
✓ Must be in 2NF
✓ No transitive dependencies
✓ Non-key attributes depend only on primary key
```

### Boyce-Codd Normal Form (BCNF)
```
┌─────────────────────────────────────────────────────────────┐
│              Boyce-Codd Normal Form (BCNF)                  │
└─────────────────────────────────────────────────────────────┘

Before (3NF but not BCNF):
┌─────────┬─────────┬──────────┐
│student_id│ course  │ teacher │
├─────────┼─────────┼──────────┤
│   101   │  Math   │  Smith   │
│   101   │ Physics │  Jones   │
│   102   │  Math   │  Smith   │
└─────────┴─────────┴──────────┘
    PK: (student_id, course)
    
Problem: teacher → course (teacher determines course)
         But teacher is not a superkey

After (BCNF):
Enrollments:              Teaching:
┌─────────┬─────────┐   ┌─────────┬──────────┐
│student_id│ course  │   │ teacher │  course  │
├─────────┼─────────┤   ├─────────┼──────────┤
│   101   │  Math   │   │  Smith   │   Math   │
│   101   │ Physics │   │  Jones   │  Physics │
│   102   │  Math   │   └─────────┴──────────┘
└─────────┴─────────┘
    PK: (student_id, course)    PK: teacher
    
Rules:
✓ Must be in 3NF
✓ Every determinant is a candidate key
✓ Stricter than 3NF
```

---

## 2. Denormalization

### When to Denormalize
```
┌─────────────────────────────────────────────────────────────┐
│              Denormalization Strategy                       │
└─────────────────────────────────────────────────────────────┘

Normalized (3NF):
┌─────────┬───────┐  ┌─────────┬──────────┐
│order_id │ cust  │  │order_id │ product  │
├─────────┼───────┤  ├─────────┼──────────┤
│  101    │ John  │  │  101    │ Laptop   │
│  102    │ Jane  │  │  101    │ Mouse    │
└─────────┴───────┘  │  102    │ Laptop   │
                     └─────────┴──────────┘
                     
    Requires JOIN for order details
    
Denormalized:
┌─────────┬───────┬─────────┬──────────┐
│order_id │ cust  │ product │ quantity │
├─────────┼───────┼─────────┼──────────┤
│  101    │ John  │ Laptop  │    2     │
│  101    │ John  │ Mouse   │    5     │
│  102    │ Jane  │ Laptop  │    1     │
└─────────┴───────┴─────────┴──────────┘
    
    Single table, faster reads
    But: Data redundancy, update anomalies
    
Trade-offs:
✓ Faster reads (no JOINs)
✓ Simpler queries
✗ Data redundancy
✗ Update anomalies
✗ More storage
```

### Denormalization Patterns
```
┌─────────────────────────────────────────────────────────────┐
│              Common Denormalization Patterns                │
└─────────────────────────────────────────────────────────────┘

1. Flattening (Pre-joined):
   Normalized: Users + Profiles (JOIN)
   Denormalized: Single Users table with profile columns
   
2. Redundant Data:
   Normalized: Orders + Customers (FK)
   Denormalized: Orders with customer_name copied
   
3. Computed Columns:
   Normalized: Calculate total on-the-fly
   Denormalized: Store total_amount column
   
4. Materialized Views:
   Normalized: Complex aggregations
   Denormalized: Pre-computed summary tables
   
5. Historical Snapshots:
   Normalized: Current state only
   Denormalized: Store historical values
```

---

## 3. Indexing Strategies

### B-Tree Index Structure
```
┌─────────────────────────────────────────────────────────────┐
│              B-Tree Index                                   │
└─────────────────────────────────────────────────────────────┘

                    Root
                     │
        ┌────────────┼────────────┐
        │            │            │
        ▼            ▼            ▼
      [10, 20]    [30, 40]    [50, 60]
        │            │            │
    ┌───┴───┐    ┌───┴───┐    ┌───┴───┐
    │       │    │       │    │       │
   [5,7]  [15,18] [25,28] [35,38] [45,48] [55,58]
    │       │      │       │      │       │
    ▼       ▼      ▼       ▼      ▼       ▼
  Data   Data   Data   Data   Data   Data
  
Characteristics:
- Balanced tree (all leaves at same level)
- O(log n) search time
- Ordered traversal
- Good for range queries
```

### Index Types

#### Primary Index
```
┌─────────────────────────────────────────────────────────────┐
│              Primary Index                                  │
└─────────────────────────────────────────────────────────────┘

Table (Sorted by PK):         Index:
┌─────┬───────┬───────┐      ┌─────┬────────┐
│ id  │ name  │ dept  │      │ id  │ offset │
├─────┼───────┼───────┤      ├─────┼────────┤
│  1  │ John  │ Eng   │      │  1  │   0    │
│  2  │ Jane  │ Sales │      │  2  │  100   │
│  3  │ Bob   │ IT    │      │  3  │  200   │
└─────┴───────┴───────┘      └─────┴────────┘
    PK: id                       Points to row
    
- One per table
- On primary key
- Automatically created
- Clustered (data sorted by index)
```

#### Secondary Index
```
┌─────────────────────────────────────────────────────────────┐
│              Secondary Index                                │
└─────────────────────────────────────────────────────────────┘

Table:                         Index on 'name':
┌─────┬───────┬───────┐      ┌───────┬─────┐
│ id  │ name  │ dept  │      │ name  │ id  │
├─────┼───────┼───────┤      ├───────┼─────┤
│  1  │ John  │ Eng   │      │ Bob   │  3  │
│  2  │ Jane  │ Sales │      │ Jane  │  2  │
│  3  │ Bob   │ IT    │      │ John  │  1  │
└─────┴───────┴───────┘      └───────┴─────┘
    PK: id                       Sorted by name
    
- Multiple per table
- On non-primary key columns
- Non-clustered (separate structure)
- Points to primary key
```

#### Composite Index
```
┌─────────────────────────────────────────────────────────────┐
│              Composite Index                                │
└─────────────────────────────────────────────────────────────┘

Table:                         Composite Index (last_name, first_name):
┌─────┬──────────┬──────────┐  ┌──────────┬──────────┬─────┐
│ id  │first_name│last_name │  │last_name │first_name│ id  │
├─────┼──────────┼──────────┤  ├──────────┼──────────┼─────┤
│  1  │  John    │  Smith   │  │  Brown   │   Bob    │  3  │
│  2  │  Jane    │  Doe     │  │  Doe     │   Jane   │  2  │
│  3  │  Bob     │  Brown   │  │  Smith   │   John   │  1  │
└─────┴──────────┴──────────┘  └──────────┴──────────┴─────┘

CREATE INDEX idx_name ON users(last_name, first_name);

Useful for:
- WHERE last_name = 'Smith' AND first_name = 'John'
- WHERE last_name = 'Smith' (leftmost prefix)
- ORDER BY last_name, first_name

Not useful for:
- WHERE first_name = 'John' (not leftmost)
```

### Index Selection Strategy
```
┌─────────────────────────────────────────────────────────────┐
│              Index Selection Decision Tree                  │
└─────────────────────────────────────────────────────────────┘

                    Query Pattern?
                    │
        ┌───────────┼───────────┐
        │           │           │
    Equality    Range      Sorting
        │           │           │
        ▼           ▼           ▼
    Hash Index  B-Tree    B-Tree Index
    or B-Tree   Index     (on sort columns)
    
    High        Frequent   JOIN
    Cardinality?  WHERE?   Columns?
        │           │           │
        ▼           ▼           ▼
    Yes: Index  Yes: Index  Yes: Index
    No: Skip    No: Skip    No: Skip
    
When to Index:
✓ Primary keys (automatic)
✓ Foreign keys (for JOINs)
✓ Frequently filtered columns
✓ Columns in WHERE, JOIN, ORDER BY
✓ High selectivity (unique values)

When NOT to Index:
✗ Low cardinality (few unique values)
✗ Frequently updated columns
✗ Small tables
✗ Columns rarely used in queries
```

### Covering Index
```
┌─────────────────────────────────────────────────────────────┐
│              Covering Index                                 │
└─────────────────────────────────────────────────────────────┘

Query:
SELECT name, dept FROM employees WHERE id = 5;

Without Covering Index:
    Index Lookup → Table Lookup
    ┌─────┐      ┌──────────────┐
    │ id  │───►  │ id │ name │ dept │
    └─────┘      └──────────────┘
    (index)      (table - additional read)
    
With Covering Index:
    Index Lookup (all data in index)
    ┌─────┬───────┬───────┐
    │ id  │ name  │ dept  │
    └─────┴───────┴───────┘
    (index - single read)
    
CREATE INDEX idx_covering ON employees(id, name, dept);

Benefits:
- Index contains all needed columns
- No table lookup needed
- Faster query execution
- Reduced I/O
```

### Partial Index
```
┌─────────────────────────────────────────────────────────────┐
│              Partial Index                                   │
└─────────────────────────────────────────────────────────────┘

Table:                         Partial Index (only active):
┌─────┬───────┬────────┐      ┌─────┬───────┐
│ id  │ name  │ status │      │ id  │ name  │
├─────┼───────┼────────┤      ├─────┼───────┤
│  1  │ John  │ active │      │  1  │ John  │
│  2  │ Jane  │active  │      │  2  │ Jane  │
│  3  │ Bob   │inactive│      └─────┴───────┘
│  4  │ Alice │active  │      (only active rows)
└─────┴───────┴────────┘

CREATE INDEX idx_active ON employees(id, name) 
WHERE status = 'active';

Benefits:
- Smaller index size
- Faster queries on subset
- Less maintenance overhead
- Useful for filtered queries
```

---

## 4. Index Maintenance

### Index Fragmentation
```
┌─────────────────────────────────────────────────────────────┐
│              Index Fragmentation                            │
└─────────────────────────────────────────────────────────────┘

Healthy Index:              Fragmented Index:
    Root                        Root
     │                           │
     ▼                           ▼
  [10, 20]                   [10, 20]
     │                           │
     ▼                           ▼
  [5, 7]                     [5, 7]  [15, 18]
  [15, 18]                   (scattered)
  
Causes:
- Frequent INSERTs/DELETEs
- Updates to indexed columns
- Page splits

Solutions:
- REBUILD INDEX (recreates)
- REORGANIZE INDEX (defragments)
- Regular maintenance
```

### Index Statistics
```
┌─────────────────────────────────────────────────────────────┐
│              Index Statistics                                │
└─────────────────────────────────────────────────────────────┘

Query Optimizer uses statistics:
┌─────────────────────────────┐
│ Statistics:                 │
│ - Cardinality               │
│ - Distribution              │
│ - Selectivity               │
│ - Histogram                 │
└─────────────────────────────┘
         │
         ▼
    Query Plan Selection
    
UPDATE STATISTICS employees;

Why important:
- Optimizer chooses best plan
- Outdated stats → bad plans
- Regular updates needed
```

---

## Key Design Principles Summary

### Normalization
```
1NF: Atomic values, no repeating groups
2NF: No partial dependencies
3NF: No transitive dependencies
BCNF: Every determinant is candidate key
```

### Denormalization
```
When: Read-heavy, complex JOINs, performance critical
Trade-off: Speed vs. data integrity
Patterns: Flattening, redundancy, computed columns
```

### Indexing
```
Types: Primary, Secondary, Composite, Covering, Partial
Strategy: Index frequently queried columns
Maintenance: Rebuild, reorganize, update statistics
```

---

**Next: Part 3 will cover Transaction Management (ACID, Isolation Levels, Deadlocks).**

