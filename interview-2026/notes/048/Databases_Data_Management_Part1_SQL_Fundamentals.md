# Databases & Data Management - In Depth Diagrams (Part 1: SQL Fundamentals)

## 📊 SQL Fundamentals: Joins, Subqueries, Window Functions, CTEs

---

## 1. SQL Joins

### INNER JOIN
```
┌─────────────────────────────────────────────────────────────┐
│              INNER JOIN                                      │
└─────────────────────────────────────────────────────────────┘

Table A:          Table B:          Result:
┌─────┬─────┐    ┌─────┬─────┐    ┌─────┬─────┬─────┬─────┐
│ id  │ name│    │ id  │ dept│    │ id  │name │ id  │dept │
├─────┼─────┤    ├─────┼─────┤    ├─────┼─────┼─────┼─────┤
│  1  │ A   │    │  1  │ Eng │    │  1  │ A   │  1  │ Eng │
│  2  │ B   │    │  3  │ Sales│   │  3  │ C   │  3  │Sales│
│  3  │ C   │    │  4  │ IT  │    └─────┴─────┴─────┴─────┘
└─────┴─────┘    └─────┴─────┘
    │                │
    └────────┬───────┘
             │
        (matching keys)
        
SQL:
SELECT * FROM A
INNER JOIN B ON A.id = B.id;

Returns only matching rows from both tables
```

### LEFT JOIN (LEFT OUTER JOIN)
```
┌─────────────────────────────────────────────────────────────┐
│              LEFT JOIN                                       │
└─────────────────────────────────────────────────────────────┘

Table A:          Table B:          Result:
┌─────┬─────┐    ┌─────┬─────┐    ┌─────┬─────┬─────┬─────┐
│ id  │ name│    │ id  │ dept│    │ id  │name │ id  │dept │
├─────┼─────┤    ├─────┼─────┤    ├─────┼─────┼─────┼─────┤
│  1  │ A   │    │  1  │ Eng │    │  1  │ A   │  1  │ Eng │
│  2  │ B   │    │  3  │ Sales│   │  2  │ B   │NULL │NULL │
│  3  │ C   │    │  4  │ IT  │    │  3  │ C   │  3  │Sales│
└─────┴─────┘    └─────┴─────┘    └─────┴─────┴─────┴─────┘
    │                │
    └────────┬───────┘
             │
        (all from A)
        
SQL:
SELECT * FROM A
LEFT JOIN B ON A.id = B.id;

Returns all rows from left table + matching from right
NULL for non-matching right table values
```

### RIGHT JOIN (RIGHT OUTER JOIN)
```
┌─────────────────────────────────────────────────────────────┐
│              RIGHT JOIN                                      │
└─────────────────────────────────────────────────────────────┘

Table A:          Table B:          Result:
┌─────┬─────┐    ┌─────┬─────┐    ┌─────┬─────┬─────┬─────┐
│ id  │ name│    │ id  │ dept│    │ id  │name │ id  │dept │
├─────┼─────┤    ├─────┼─────┤    ├─────┼─────┼─────┼─────┤
│  1  │ A   │    │  1  │ Eng │    │  1  │ A   │  1  │ Eng │
│  2  │ B   │    │  3  │ Sales│   │NULL │NULL │  3  │Sales│
│  3  │ C   │    │  4  │ IT  │    │NULL │NULL │  4  │ IT  │
└─────┴─────┘    └─────┴─────┘    └─────┴─────┴─────┴─────┘
    │                │
    └────────┬───────┘
             │
        (all from B)
        
SQL:
SELECT * FROM A
RIGHT JOIN B ON A.id = B.id;

Returns all rows from right table + matching from left
NULL for non-matching left table values
```

### FULL OUTER JOIN
```
┌─────────────────────────────────────────────────────────────┐
│              FULL OUTER JOIN                                 │
└─────────────────────────────────────────────────────────────┘

Table A:          Table B:          Result:
┌─────┬─────┐    ┌─────┬─────┐    ┌─────┬─────┬─────┬─────┐
│ id  │ name│    │ id  │ dept│    │ id  │name │ id  │dept │
├─────┼─────┤    ├─────┼─────┤    ├─────┼─────┼─────┼─────┤
│  1  │ A   │    │  1  │ Eng │    │  1  │ A   │  1  │ Eng │
│  2  │ B   │    │  3  │ Sales│   │  2  │ B   │NULL │NULL │
│  3  │ C   │    │  4  │ IT  │    │  3  │ C   │  3  │Sales│
└─────┴─────┘    └─────┴─────┘    │NULL │NULL │  4  │ IT  │
    │                │            └─────┴─────┴─────┴─────┘
    └────────┬───────┘
             │
        (all from both)
        
SQL:
SELECT * FROM A
FULL OUTER JOIN B ON A.id = B.id;

Returns all rows from both tables
NULL for non-matching values
```

### CROSS JOIN (Cartesian Product)
```
┌─────────────────────────────────────────────────────────────┐
│              CROSS JOIN                                      │
└─────────────────────────────────────────────────────────────┘

Table A:          Table B:          Result:
┌─────┬─────┐    ┌─────┬─────┐    ┌─────┬─────┬─────┬─────┐
│ id  │ name│    │ id  │ dept│    │ id  │name │ id  │dept │
├─────┼─────┤    ├─────┼─────┤    ├─────┼─────┼─────┼─────┤
│  1  │ A   │    │  1  │ Eng │    │  1  │ A   │  1  │ Eng │
│  2  │ B   │    │  2  │ Sales│   │  1  │ A   │  2  │Sales│
└─────┴─────┘    └─────┴─────┘    │  2  │ B   │  1  │ Eng │
                                  │  2  │ B   │  2  │Sales│
                                  └─────┴─────┴─────┴─────┘
                                  
SQL:
SELECT * FROM A
CROSS JOIN B;

Returns all possible combinations
Result rows = rows(A) × rows(B)
```

### SELF JOIN
```
┌─────────────────────────────────────────────────────────────┐
│              SELF JOIN                                       │
└─────────────────────────────────────────────────────────────┘

Employee Table:                  Result (Manager Hierarchy):
┌─────┬───────┬─────────┐      ┌──────┬─────────┬──────────┐
│ id  │ name  │ mgr_id  │      │emp_id│ emp_name│ mgr_name  │
├─────┼───────┼─────────┤      ├──────┼─────────┼──────────┤
│  1  │ Alice │  NULL   │      │  2  │ Bob     │ Alice     │
│  2  │ Bob   │   1     │      │  3  │ Charlie │ Bob       │
│  3  │ Charlie│  2     │      └──────┴─────────┴──────────┘
└─────┴───────┴─────────┘
    │              │
    └──────┬───────┘
           │
      (same table)
      
SQL:
SELECT e.id, e.name, m.name AS manager
FROM Employee e
LEFT JOIN Employee m ON e.mgr_id = m.id;

Join table to itself
Useful for hierarchical data
```

---

## 2. Subqueries

### Scalar Subquery
```
┌─────────────────────────────────────────────────────────────┐
│              Scalar Subquery                                 │
└─────────────────────────────────────────────────────────────┘

Outer Query:                    Subquery:
SELECT name,                   SELECT AVG(salary)
  (SELECT AVG(salary)           FROM employees
   FROM employees) AS avg_sal
FROM employees
WHERE salary > (SELECT AVG(salary)
                FROM employees);
                
    ┌──────────────┐
    │ Outer Query  │
    │              │
    │  ┌────────┐  │
    │  │Subquery│  │  ← Returns single value
    │  └────────┘  │
    │              │
    └──────────────┘
    
Returns single value
Used in SELECT, WHERE, HAVING
Must return exactly one row, one column
```

### Correlated Subquery
```
┌─────────────────────────────────────────────────────────────┐
│              Correlated Subquery                            │
└─────────────────────────────────────────────────────────────┘

SELECT e1.name, e1.salary
FROM employees e1
WHERE salary > (
    SELECT AVG(salary)
    FROM employees e2
    WHERE e2.dept_id = e1.dept_id  ← References outer query
);

    ┌──────────────┐
    │ Outer Query  │
    │  (e1)        │
    │              │
    │  ┌────────┐  │
    │  │Subquery│  │  ← References e1.dept_id
    │  │ (e2)   │  │
    │  └────────┘  │
    │              │
    └──────────────┘
    
Executes once per outer row
References columns from outer query
Can be slow for large datasets
```

### EXISTS Subquery
```
┌─────────────────────────────────────────────────────────────┐
│              EXISTS Subquery                                 │
└─────────────────────────────────────────────────────────────┘

SELECT name
FROM customers c
WHERE EXISTS (
    SELECT 1
    FROM orders o
    WHERE o.customer_id = c.id
);

    ┌──────────────┐
    │ Outer Query  │
    │              │
    │  ┌────────┐  │
    │  │EXISTS  │  │  ← Returns TRUE/FALSE
    │  │Subquery│  │
    │  └────────┘  │
    │              │
    └──────────────┘
    
Returns boolean (TRUE/FALSE)
Stops at first match (efficient)
Useful for existence checks
```

### IN Subquery
```
┌─────────────────────────────────────────────────────────────┐
│              IN Subquery                                     │
└─────────────────────────────────────────────────────────────┘

SELECT name
FROM employees
WHERE dept_id IN (
    SELECT id
    FROM departments
    WHERE location = 'NYC'
);

    ┌──────────────┐
    │ Outer Query  │
    │              │
    │  ┌────────┐  │
    │  │  IN    │  │  ← Returns list of values
    │  │Subquery│  │
    │  └────────┘  │
    │              │
    └──────────────┘
    
Returns list of values
Checks if value is in list
Can use NOT IN for exclusion
```

---

## 3. Window Functions

### ROW_NUMBER()
```
┌─────────────────────────────────────────────────────────────┐
│              ROW_NUMBER() Window Function                  │
└─────────────────────────────────────────────────────────────┘

SELECT 
    name,
    salary,
    dept_id,
    ROW_NUMBER() OVER (
        PARTITION BY dept_id 
        ORDER BY salary DESC
    ) AS rank
FROM employees;

Result:
┌──────┬────────┬─────────┬──────┐
│ name │ salary │ dept_id │ rank │
├──────┼────────┼─────────┼──────┤
│ A    │ 5000   │   1     │  1   │
│ B    │ 4000   │   1     │  2   │
│ C    │ 3000   │   1     │  3   │
│ D    │ 6000   │   2     │  1   │
│ E    │ 5000   │   2     │  2   │
└──────┴────────┴─────────┴──────┘

    ┌──────────────┐
    │ Window Frame  │
    │              │
    │ PARTITION BY │  ← Groups rows
    │ ORDER BY     │  ← Orders within group
    │              │
    └──────────────┘
    
Assigns unique sequential number
No ties (always different numbers)
```

### RANK() and DENSE_RANK()
```
┌─────────────────────────────────────────────────────────────┐
│              RANK() vs DENSE_RANK()                          │
└─────────────────────────────────────────────────────────────┘

RANK():
┌──────┬────────┬──────┐      DENSE_RANK():
│ name │ salary │ rank │      ┌──────┬────────┬──────┐
├──────┼────────┼──────┤      │ name │ salary │ rank │
│ A    │ 5000   │  1   │      ├──────┼────────┼──────┤
│ B    │ 4000   │  2   │      │ A    │ 5000   │  1   │
│ C    │ 4000   │  2   │      │ B    │ 4000   │  2   │
│ D    │ 3000   │  4   │      │ C    │ 4000   │  2   │
└──────┴────────┴──────┘      │ D    │ 3000   │  3   │
                              └──────┴────────┴──────┘
RANK(): Skips numbers after ties
DENSE_RANK(): No gaps after ties
```

### LAG() and LEAD()
```
┌─────────────────────────────────────────────────────────────┐
│              LAG() and LEAD()                               │
└─────────────────────────────────────────────────────────────┘

SELECT 
    date,
    sales,
    LAG(sales, 1) OVER (ORDER BY date) AS prev_sales,
    LEAD(sales, 1) OVER (ORDER BY date) AS next_sales
FROM daily_sales;

Result:
┌──────────┬───────┬────────────┬─────────────┐
│   date   │ sales │ prev_sales │ next_sales  │
├──────────┼───────┼────────────┼─────────────┤
│ 2024-01-01│ 100  │   NULL     │    150      │
│ 2024-01-02│ 150  │   100      │    120      │
│ 2024-01-03│ 120  │   150      │   NULL      │
└──────────┴───────┴────────────┴─────────────┘

    ┌──────────────┐
    │ Current Row  │
    │              │
    │  ┌────────┐  │
    │  │ LAG    │  │  ← Previous row
    │  └────────┘  │
    │              │
    │  ┌────────┐  │
    │  │ LEAD   │  │  ← Next row
    │  └────────┘  │
    └──────────────┘
    
LAG(): Access previous row
LEAD(): Access next row
Useful for time series analysis
```

### SUM() OVER() - Running Total
```
┌─────────────────────────────────────────────────────────────┐
│              Running Total with Window Function              │
└─────────────────────────────────────────────────────────────┘

SELECT 
    date,
    sales,
    SUM(sales) OVER (
        ORDER BY date 
        ROWS BETWEEN UNBOUNDED PRECEDING 
        AND CURRENT ROW
    ) AS running_total
FROM daily_sales;

Result:
┌──────────┬───────┬──────────────┐
│   date   │ sales │running_total│
├──────────┼───────┼──────────────┤
│ 2024-01-01│ 100  │     100      │
│ 2024-01-02│ 150  │     250      │
│ 2024-01-03│ 120  │     370      │
└──────────┴───────┴──────────────┘

    ┌──────────────┐
    │ Window Frame │
    │              │
    │ UNBOUNDED    │  ← From start
    │ PRECEDING    │
    │              │
    │ CURRENT ROW  │  ← To current
    │              │
    └──────────────┘
    
Cumulative sum
Window frame defines range
```

---

## 4. Common Table Expressions (CTEs)

### Simple CTE
```
┌─────────────────────────────────────────────────────────────┐
│              Common Table Expression (CTE)                 │
└─────────────────────────────────────────────────────────────┘

WITH high_salary_employees AS (
    SELECT id, name, salary
    FROM employees
    WHERE salary > 5000
)
SELECT * FROM high_salary_employees;

    ┌──────────────┐
    │ Main Query   │
    │              │
    │  ┌────────┐  │
    │  │  CTE   │  │  ← Temporary named result set
    │  └────────┘  │
    │              │
    └──────────────┘
    
CTE Benefits:
- Improves readability
- Reusable within query
- Can reference itself (recursive)
- Scope limited to query
```

### Multiple CTEs
```
┌─────────────────────────────────────────────────────────────┐
│              Multiple CTEs                                   │
└─────────────────────────────────────────────────────────────┘

WITH 
    dept_stats AS (
        SELECT dept_id, AVG(salary) AS avg_sal
        FROM employees
        GROUP BY dept_id
    ),
    high_performers AS (
        SELECT e.id, e.name, e.salary
        FROM employees e
        JOIN dept_stats d ON e.dept_id = d.dept_id
        WHERE e.salary > d.avg_sal * 1.2
    )
SELECT * FROM high_performers;

    ┌──────────────┐
    │ Main Query   │
    │              │
    │  ┌────────┐  │
    │  │ CTE 1  │  │
    │  └────────┘  │
    │              │
    │  ┌────────┐  │
    │  │ CTE 2  │  │  ← Can use CTE 1
    │  └────────┘  │
    │              │
    └──────────────┘
    
Multiple CTEs in sequence
Later CTEs can reference earlier ones
```

### Recursive CTE
```
┌─────────────────────────────────────────────────────────────┐
│              Recursive CTE                                  │
└─────────────────────────────────────────────────────────────┘

WITH RECURSIVE employee_hierarchy AS (
    -- Anchor: Top-level managers
    SELECT id, name, mgr_id, 0 AS level
    FROM employees
    WHERE mgr_id IS NULL
    
    UNION ALL
    
    -- Recursive: Subordinates
    SELECT e.id, e.name, e.mgr_id, eh.level + 1
    FROM employees e
    JOIN employee_hierarchy eh ON e.mgr_id = eh.id
)
SELECT * FROM employee_hierarchy;

    ┌──────────────┐
    │ Anchor Query │  ← Base case
    │              │
    │    UNION     │
    │              │
    │ Recursive    │  ← References itself
    │   Query      │
    └──────────────┘
    
Two parts:
1. Anchor (base case)
2. Recursive (references CTE)
Useful for hierarchical data
```

---

## 5. Join Performance Comparison

### Join Algorithm Visualization
```
┌─────────────────────────────────────────────────────────────┐
│              Join Algorithms                                │
└─────────────────────────────────────────────────────────────┘

Nested Loop Join:
    For each row in A:
        For each row in B:
            If match: output
            
    ┌─────┐
    │  A  │──┐
    └─────┘  │
             │
             ▼
         ┌─────┐
         │  B  │
         └─────┘
         
Time: O(n × m)
Good for: Small tables, indexed joins

Hash Join:
    Build hash table from smaller table
    Probe larger table
    
    ┌─────┐
    │  A  │──► Hash Table
    └─────┘      │
                 │
    ┌─────┐      │
    │  B  │──►──┘
    └─────┘
    
Time: O(n + m)
Good for: Large tables, no indexes

Sort-Merge Join:
    Sort both tables
    Merge sorted results
    
    ┌─────┐      ┌─────┐
    │  A  │──► Sort ──┐
    └─────┘           │
                      ▼
    ┌─────┐      ┌─────┐    Merge
    │  B  │──► Sort ──┘
    └─────┘
    
Time: O(n log n + m log m)
Good for: Pre-sorted data
```

---

## Key SQL Concepts Summary

### Join Types
```
INNER JOIN: Matching rows only
LEFT JOIN: All left + matching right
RIGHT JOIN: All right + matching left
FULL OUTER: All rows from both
CROSS JOIN: Cartesian product
SELF JOIN: Join table to itself
```

### Subquery Types
```
Scalar: Returns single value
Correlated: References outer query
EXISTS: Boolean check
IN: List membership check
```

### Window Functions
```
ROW_NUMBER(): Sequential numbering
RANK(): Rank with gaps
DENSE_RANK(): Rank without gaps
LAG/LEAD(): Previous/next row
SUM/AVG OVER(): Running aggregates
```

### CTEs
```
Simple: Named temporary result set
Multiple: Chain of CTEs
Recursive: Self-referencing CTE
```

---

**Next: Part 2 will cover Database Design (Normalization, Denormalization, Indexing Strategies).**

