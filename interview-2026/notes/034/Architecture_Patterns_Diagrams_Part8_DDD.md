# Architecture Patterns - Complete Diagrams Guide (Part 8: Domain-Driven Design)

## 🎯 Domain-Driven Design (DDD)

---

## 1. DDD Overview

### Core Concepts
```
┌─────────────────────────────────────────────────────────────┐
│              Domain-Driven Design                            │
└─────────────────────────────────────────────────────────────┘

    ┌──────────────────────────────┐
    │      Ubiquitous Language     │
    │  (Shared vocabulary)         │
    └──────────────┬───────────────┘
                   │
    ┌──────────────▼───────────────┐
    │      Domain Model            │
    │  ┌────────────────────────┐ │
    │  │  Entities               │ │
    │  │  Value Objects          │ │
    │  │  Aggregates            │ │
    │  │  Domain Services       │ │
    │  └────────────────────────┘ │
    └──────────────┬───────────────┘
                   │
    ┌──────────────▼───────────────┐
    │      Bounded Contexts        │
    │  ┌────────┐  ┌────────┐    │
    │  │Ordering│  │Shipping│    │
    │  │Context │  │Context │    │
    │  └────────┘  └────────┘    │
    └──────────────────────────────┘

Focus on domain
Rich domain model
Business logic in domain
```

### Strategic Design
```
┌─────────────────────────────────────────────────────────────┐
│              Strategic Design                                │
└─────────────────────────────────────────────────────────────┘

    ┌──────────────────────────────┐
    │      Bounded Context         │
    │  (Order Management)          │
    │                              │
    │  ┌────────────────────────┐ │
    │  │  Domain Model          │ │
    │  │  - Order (Aggregate)    │ │
    │  │  - OrderItem (Entity)  │ │
    │  │  - Money (Value Object)│ │
    │  └────────────────────────┘ │
    │                              │
    │  Ubiquitous Language:       │
    │  - Order                    │
    │  - OrderItem                │
    │  - Total                    │
    └──────────────────────────────┘

Each bounded context:
- Own domain model
- Own ubiquitous language
- Clear boundaries
- Independent
```

---

## 2. Bounded Contexts

### Context Boundaries
```
┌─────────────────────────────────────────────────────────────┐
│              Bounded Contexts                                │
└─────────────────────────────────────────────────────────────┘

    ┌─────────────────────┐
    │  Ordering Context   │
    │                     │
    │  Order              │
    │  OrderItem          │
    │  Customer           │
    └─────────────────────┘
            │
            │ Context Map
            │
    ┌───────┴───────┐
    │               │
┌───▼────┐    ┌─────▼───┐
│Shipping│    │Payment  │
│Context │    │Context  │
│        │    │         │
│Shipment│    │Payment  │
│Package │    │Transaction│
└────────┘    └─────────┘

Each context:
- Own model
- Own language
- Clear boundaries
- Independent team
```

### Context Mapping
```
┌─────────────────────────────────────────────────────────────┐
│              Context Mapping                                 │
└─────────────────────────────────────────────────────────────┘

    Ordering Context
            │
            │ Customer Shared Kernel
            │
    ┌───────┴───────┐
    │               │
Shipping Context  Payment Context
    │               │
    │               │
    └───────┬───────┘
            │
    Customer Context
    (Shared Kernel)

Relationships:
- Shared Kernel (shared code)
- Customer-Supplier (upstream-downstream)
- Conformist (follow upstream)
- Anticorruption Layer (translate)
- Separate Ways (independent)
```

---

## 3. Aggregates

### Aggregate Pattern
```
┌─────────────────────────────────────────────────────────────┐
│              Aggregate                                       │
└─────────────────────────────────────────────────────────────┘

    ┌──────────────────────────────┐
    │      Order (Aggregate Root)   │
    │                              │
    │  ┌────────────────────────┐ │
    │  │  OrderId (Identity)   │ │
    │  │  CustomerId           │ │
    │  │  Status               │ │
    │  │  Total                │ │
    │  └────────────────────────┘ │
    │                              │
    │  ┌────────────────────────┐ │
    │  │  OrderItems           │ │
    │  │  (Entities)           │ │
    │  │  - OrderItem 1        │ │
    │  │  - OrderItem 2        │ │
    │  └────────────────────────┘ │
    │                              │
    │  ┌────────────────────────┐ │
    │  │  Domain Methods        │ │
    │  │  - addItem()           │ │
    │  │  - removeItem()        │ │
    │  │  - calculateTotal()     │ │
    │  └────────────────────────┘ │
    └──────────────────────────────┘

Aggregate Root:
- Single entry point
- Enforces invariants
- Transaction boundary
- Consistency boundary
```

### Aggregate Rules
```
┌─────────────────────────────────────────────────────────────┐
│              Aggregate Rules                                │
└─────────────────────────────────────────────────────────────┘

✅ AGGREGATE ROOT
   - Single root entity
   - Only root has global identity
   - External references to root only

✅ INVARIANTS
   - Enforced within aggregate
   - Consistency guaranteed
   - Transaction boundary

✅ SIZE
   - Small aggregates
   - Few entities
   - Performance consideration

✅ REFERENCES
   - Reference by identity
   - Not by object reference
   - Lazy loading

✅ TRANSACTIONS
   - One aggregate per transaction
   - Eventual consistency between aggregates
```

---

## 4. Entities and Value Objects

### Entities
```
┌─────────────────────────────────────────────────────────────┐
│              Entities                                        │
└─────────────────────────────────────────────────────────────┘

    ┌──────────────────────────────┐
    │      Order (Entity)          │
    │                              │
    │  Identity: OrderId          │
    │  - orderId: String          │
    │  - customerId: String       │
    │  - status: OrderStatus      │
    │  - items: List<OrderItem>   │
    │                              │
    │  Methods:                    │
    │  - addItem()                 │
    │  - removeItem()              │
    │  - calculateTotal()          │
    └──────────────────────────────┘

Characteristics:
- Has identity
- Mutable
- Lifecycle
- Equality by identity
```

### Value Objects
```
┌─────────────────────────────────────────────────────────────┐
│              Value Objects                                   │
└─────────────────────────────────────────────────────────────┘

    ┌──────────────────────────────┐
    │      Money (Value Object)    │
    │                              │
    │  - amount: BigDecimal        │
    │  - currency: Currency        │
    │                              │
    │  Methods:                    │
    │  - add(Money other)          │
    │  - multiply(BigDecimal)     │
    │  - equals()                  │
    └──────────────────────────────┘

Characteristics:
- No identity
- Immutable
- Equality by value
- Self-validating

Example:
Money tenDollars = new Money(10, USD);
Money twentyDollars = tenDollars.add(tenDollars);
```

---

## 5. Domain Services

### Domain Services
```
┌─────────────────────────────────────────────────────────────┐
│              Domain Services                                │
└─────────────────────────────────────────────────────────────┘

    ┌──────────────────────────────┐
    │  OrderPricingService        │
    │  (Domain Service)            │
    │                              │
    │  calculatePrice(Order): Money│
    │  - Apply discounts          │
    │  - Calculate tax            │
    │  - Apply promotions         │
    └──────────────────────────────┘

When to use:
- Operation doesn't belong to entity
- Involves multiple entities
- Stateless operation
- Domain concept

Example:
class OrderPricingService {
    Money calculateTotalPrice(Order order, 
                              Customer customer) {
        // Complex pricing logic
        // involving order and customer
    }
}
```

---

## 6. Ubiquitous Language

### Language Mapping
```
┌─────────────────────────────────────────────────────────────┐
│              Ubiquitous Language                            │
└─────────────────────────────────────────────────────────────┘

Business Language          Code Language
─────────────────────────────────────────
Order                     Order (class)
Order Item                OrderItem (class)
Total Price               Money (value object)
Customer                  Customer (entity)
Place Order               placeOrder() (method)
Cancel Order              cancelOrder() (method)

Same language everywhere:
- Code
- Documentation
- Conversations
- Tests
```

---

## 7. Repository Pattern

### Repository
```
┌─────────────────────────────────────────────────────────────┐
│              Repository Pattern                             │
└─────────────────────────────────────────────────────────────┘

    ┌──────────────────────────────┐
    │  OrderRepository            │
    │  (Interface)                 │
    │                              │
    │  + save(Order)              │
    │  + findById(OrderId)        │
    │  + findByCustomer(CustomerId)│
    │  + delete(OrderId)          │
    └──────────────┬───────────────┘
                   │
                   │ implements
                   │
    ┌──────────────▼───────────────┐
    │  JpaOrderRepository         │
    │  (Implementation)            │
    │                              │
    │  - EntityManager            │
    │  - Database operations      │
    └──────────────────────────────┘

Abstraction over persistence
Domain-focused interface
Technology-agnostic
```

---

## 8. Benefits and Challenges

### Advantages
```
┌─────────────────────────────────────────────────────────────┐
│              Advantages                                     │
└─────────────────────────────────────────────────────────────┘

✅ DOMAIN FOCUS
   - Business logic central
   - Rich domain model
   - Domain experts involved

✅ COMMUNICATION
   - Ubiquitous language
   - Better understanding
   - Clear concepts

✅ MAINTAINABILITY
   - Clear boundaries
   - Organized code
   - Easy to understand

✅ TESTABILITY
   - Domain logic testable
   - Isolated testing
   - Business rules clear

✅ EVOLUTION
   - Adapt to changes
   - Refactor easily
   - Long-term maintainable
```

### Challenges
```
┌─────────────────────────────────────────────────────────────┐
│              Challenges                                     │
└─────────────────────────────────────────────────────────────┘

❌ COMPLEXITY
   - Learning curve
   - Many concepts
   - Requires expertise

❌ TIME
   - Initial investment
   - Domain modeling
   - Refactoring

❌ TEAM
   - Domain experts needed
   - Collaboration required
   - Communication critical

❌ OVER-ENGINEERING
   - Simple domains don't need it
   - Unnecessary complexity
   - YAGNI violation
```

---

## 9. When to Use DDD

### Ideal Scenarios
```
┌─────────────────────────────────────────────────────────────┐
│              When to Use DDD                                │
└─────────────────────────────────────────────────────────────┘

✅ COMPLEX DOMAINS
   - Rich business logic
   - Complex rules
   - Domain expertise needed

✅ LARGE APPLICATIONS
   - Multiple teams
   - Multiple contexts
   - Long-term project

✅ BUSINESS CRITICAL
   - Core business logic
   - Competitive advantage
   - Domain knowledge important

✅ EVOLVING REQUIREMENTS
   - Changing business
   - Frequent changes
   - Need flexibility

✅ TEAM COLLABORATION
   - Domain experts available
   - Close collaboration
   - Shared understanding
```

---

## Key Takeaways

### Summary
```
┌─────────────────────────────────────────────────────────────┐
│              DDD Summary                                    │
└─────────────────────────────────────────────────────────────┘

DEFINITION:
Approach to software development focusing on domain model

CHARACTERISTICS:
- Bounded contexts
- Aggregates
- Entities and value objects
- Ubiquitous language

BEST FOR:
- Complex domains
- Large applications
- Business-critical
- Evolving requirements

NOT FOR:
- Simple domains
- CRUD applications
- Small projects
- Data-centric apps

KEY CONCEPTS:
- Bounded Context
- Aggregate
- Entity
- Value Object
- Domain Service
- Ubiquitous Language
```

---

**Next: Part 9 will cover CQRS (Command Query Responsibility Segregation).**

