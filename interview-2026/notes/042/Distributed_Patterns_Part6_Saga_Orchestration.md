# Distributed Patterns - Complete Diagrams Guide (Part 6: Saga Orchestration)

## 🎯 Saga Orchestration Pattern

---

## 1. Orchestration Pattern Overview

### Centralized Coordination
```
┌─────────────────────────────────────────────────────────────┐
│              Orchestration Pattern                          │
└─────────────────────────────────────────────────────────────┘

    Order Service
    │
    │ Initiate Order
    │
    ▼
┌──────────────────┐
│  Saga            │
│  Orchestrator    │  ← Central coordinator
│  (State Machine) │
└────────┬─────────┘
         │
    ┌────┴────┐
    │         │
    ▼         ▼
┌──────────┐ ┌──────────┐
│ Payment  │ │Inventory │
│ Service  │ │ Service  │
│(passive) │ │(passive) │
└────┬─────┘ └────┬─────┘
     │            │
     │ Response   │ Response
     │            │
     └─────┬──────┘
           │
           ▼
    ┌──────────────────┐
    │  Saga            │
    │  Orchestrator    │
    │  (decides next)  │
    └──────────────────┘

Key Characteristics:
- Central coordinator
- Orchestrator manages flow
- Services are passive
- Easier to understand
- Better error handling
```

---

## 2. Orchestrator State Machine

### State Transitions
```
┌─────────────────────────────────────────────────────────────┐
│              Orchestrator State Machine                     │
└─────────────────────────────────────────────────────────────┘

    START
    │
    ▼
┌──────────────┐
│  PENDING     │  ← Initial state
└──────┬───────┘
       │
       ▼
┌──────────────┐
│  STEP_1      │  ← Reserve Inventory
│  IN_PROGRESS │
└──────┬───────┘
       │
   ┌───┴───┐
   │       │
   ▼       ▼
SUCCESS  FAILED
   │       │
   │       ▼
   │   ┌──────────────┐
   │   │ COMPENSATING │
   │   │ (rollback)   │
   │   └──────────────┘
   │
   ▼
┌──────────────┐
│  STEP_2      │  ← Process Payment
│  IN_PROGRESS │
└──────┬───────┘
       │
   ┌───┴───┐
   │       │
   ▼       ▼
SUCCESS  FAILED
   │       │
   │       ▼
   │   ┌──────────────┐
   │   │ COMPENSATING │
   │   │ Step 2       │
   │   │ Step 1       │
   │   └──────────────┘
   │
   ▼
┌──────────────┐
│  STEP_3      │  ← Create Order
│  IN_PROGRESS │
└──────┬───────┘
       │
   ┌───┴───┐
   │       │
   ▼       ▼
SUCCESS  FAILED
   │       │
   │       ▼
   │   ┌──────────────┐
   │   │ COMPENSATING │
   │   └──────────────┘
   │
   ▼
┌──────────────┐
│  COMPLETED   │  ← Final success
└──────────────┘
```

---

## 3. Orchestrator Flow

### Step-by-Step Execution
```
┌─────────────────────────────────────────────────────────────┐
│              Orchestrator Execution Flow                    │
└─────────────────────────────────────────────────────────────┘

Orchestrator
    │
    │ Step 1: Reserve Inventory
    │
    ▼
┌──────────┐
│ Payment  │
│ Service  │
│(waiting) │
└──────────┘
    │
    ▼
┌──────────┐
│Inventory │
│ Service  │
│          │
│ Reserve Items(orderId)
│──────────┐
│          │
│◄─────────┘
│ Response: ✅ Success
│
└──────────┘
    │
    ▼
Orchestrator
    │
    │ Step 2: Process Payment
    │
    ▼
┌──────────┐
│ Payment  │
│ Service  │
│          │
│ Charge Payment(orderId, amount)
│──────────┐
│          │
│◄─────────┘
│ Response: ✅ Success
│
└──────────┘
    │
    ▼
Orchestrator
    │
    │ Step 3: Create Order
    │
    ▼
┌──────────┐
│  Order   │
│ Service  │
│          │
│ Create Order(orderId)
│──────────┐
│          │
│◄─────────┘
│ Response: ✅ Success
│
└──────────┘
    │
    ▼
Orchestrator
    │
    │ All steps completed
    │
    ▼
    COMPLETED
```

---

## 4. Compensation in Orchestration

### Compensation Flow
```
┌─────────────────────────────────────────────────────────────┐
│              Compensation Execution                         │
└─────────────────────────────────────────────────────────────┘

Normal Flow:
    Step 1: Reserve Inventory ✅
        │
        ▼
    Step 2: Process Payment ✅
        │
        ▼
    Step 3: Create Order ❌ (FAILED)
        │
        ▼
    Compensation Required

Compensation Flow (Reverse Order):
    ┌─────────────────────────────────────┐
    │  Step 2 Compensation:                │
    │  Refund Payment                       │
    │    │                                  │
    │    ▼                                  │
    │  Payment Service                      │
    │    Refund(orderId)                    │
    │    ────────────┐                     │
    │                │                      │
    │    ◄───────────┘                     │
    │    Response: ✅ Success               │
    └─────────────────────────────────────┘
        │
        ▼
    ┌─────────────────────────────────────┐
    │  Step 1 Compensation:                │
    │  Release Inventory                   │
    │    │                                  │
    │    ▼                                  │
    │  Inventory Service                    │
    │    Release(orderId)                   │
    │    ────────────┐                     │
    │                │                      │
    │    ◄───────────┘                     │
    │    Response: ✅ Success               │
    └─────────────────────────────────────┘
        │
        ▼
    All Compensations Complete
    Saga: COMPENSATED
```

---

## 5. Orchestrator Implementation

### Saga Definition
```
┌─────────────────────────────────────────────────────────────┐
│              Saga Definition                                │
└─────────────────────────────────────────────────────────────┘

Saga Steps:
    ┌─────────────────────────────────────┐
    │  Step 1: Reserve Inventory           │
    │  ┌───────────────────────────────┐   │
    │  │ Service: InventoryService      │   │
    │  │ Action: reserveItems(orderId)  │   │
    │  │ Compensate: releaseItems(orderId)│ │
    │  └───────────────────────────────┘   │
    └─────────────────────────────────────┘
    │
    ▼
    ┌─────────────────────────────────────┐
    │  Step 2: Process Payment             │
    │  ┌───────────────────────────────┐   │
    │  │ Service: PaymentService        │   │
    │  │ Action: chargePayment(orderId) │   │
    │  │ Compensate: refundPayment(orderId)││
    │  └───────────────────────────────┘   │
    └─────────────────────────────────────┘
    │
    ▼
    ┌─────────────────────────────────────┐
    │  Step 3: Create Order                │
    │  ┌───────────────────────────────┐   │
    │  │ Service: OrderService          │   │
    │  │ Action: createOrder(orderId)   │   │
    │  │ Compensate: cancelOrder(orderId)│  │
    │  └───────────────────────────────┘   │
    └─────────────────────────────────────┘
```

### Orchestrator Code Structure
```
┌─────────────────────────────────────────────────────────────┐
│              Code Structure                                 │
└─────────────────────────────────────────────────────────────┘

Saga Orchestrator:
    ┌─────────────────────────────────────┐
    │  class OrderSagaOrchestrator {       │
    │    SagaState state;                  │
    │    List<SagaStep> steps;             │
    │                                      │
    │    execute() {                       │
    │      for (step : steps) {            │
    │        try {                         │
    │          step.execute();             │
    │          state = step.getNextState(); │
    │        } catch (Exception e) {      │
    │          compensate();               │
    │          break;                      │
    │        }                            │
    │      }                              │
    │    }                                │
    │                                      │
    │    compensate() {                    │
    │      for (step : reverse(steps)) {   │
    │        if (step.isExecuted()) {      │
    │          step.compensate();          │
    │        }                            │
    │      }                              │
    │    }                                │
    │  }                                  │
    └─────────────────────────────────────┘
```

---

## 6. Orchestrator State Persistence

### State Storage
```
┌─────────────────────────────────────────────────────────────┐
│              State Persistence                              │
└─────────────────────────────────────────────────────────────┘

Saga State Table:
    ┌─────────────────────────────────────┐
    │  sagaId | state | currentStep | data│
    │  ───────┼───────┼─────────────┼─────│
    │  saga-1 | STEP2 | 2           | {...}│
    │  saga-2 | COMPENSATING | 1   | {...}│
    │  saga-3 | COMPLETED | 3      | {...}│
    └─────────────────────────────────────┘

Step Execution Log:
    ┌─────────────────────────────────────┐
    │  sagaId | step | status | timestamp  │
    │  ───────┼──────┼────────┼───────────│
    │  saga-1 | 1    | SUCCESS| 10:00:00  │
    │  saga-1 | 2    | SUCCESS| 10:00:05  │
    │  saga-1 | 3    | FAILED | 10:00:10  │
    │  saga-1 | 2    | COMPENSATED|10:00:15│
    │  saga-1 | 1    | COMPENSATED|10:00:20│
    └─────────────────────────────────────┘

Benefits:
- Recover from crashes
- Resume interrupted sagas
- Audit trail
- Debugging
```

---

## 7. Timeout and Retry Handling

### Timeout Management
```
┌─────────────────────────────────────────────────────────────┐
│              Timeout Handling                              │
└─────────────────────────────────────────────────────────────┘

Orchestrator
    │
    │ Step 1: Reserve Inventory
    │
    ▼
┌──────────┐
│Inventory │
│ Service  │
│          │
│ Request (with timeout: 5s)
│──────────┐
│          │
│    ⏱️  Timeout after 5s
│          │
│◄─────────┘
│ Response: TIMEOUT
│
└──────────┘
    │
    ▼
Orchestrator
    │
    │ Retry Logic:
    │ - Max retries: 3
    │ - Backoff: exponential
    │
    ▼
┌──────────┐
│Inventory │
│ Service  │
│          │
│ Retry 1 (after 1s)
│──────────┐
│          │
│    ⏱️  Timeout again
│          │
│◄─────────┘
│
└──────────┘
    │
    ▼
Orchestrator
    │
    │ Retry 2 (after 2s)
    │
    ▼
    ... (continue or fail)
```

---

## 8. Real-World Example

### Order Processing Orchestration
```
┌─────────────────────────────────────────────────────────────┐
│              Order Processing Orchestration                 │
└─────────────────────────────────────────────────────────────┘

Order Service
    │
    │ Create Order Request
    │
    ▼
┌──────────────────┐
│  Order Saga      │
│  Orchestrator    │
└────────┬─────────┘
         │
    ┌────┴────┐
    │         │
    ▼         ▼
┌──────────┐ ┌──────────┐
│Inventory │ │ Payment  │
│ Service  │ │ Service  │
└──────────┘ └──────────┘

Execution:
    1. Orchestrator → Inventory: Reserve items
       Response: ✅ Success
       
    2. Orchestrator → Payment: Charge payment
       Response: ✅ Success
       
    3. Orchestrator → Order: Create order
       Response: ❌ Failure (DB error)
       
    4. Orchestrator → Payment: Refund (compensate)
       Response: ✅ Success
       
    5. Orchestrator → Inventory: Release (compensate)
       Response: ✅ Success
       
    Final State: COMPENSATED

Orchestrator State:
    ┌─────────────────────────────────────┐
    │  sagaId: order-123                  │
    │  state: COMPENSATED                 │
    │  stepsExecuted: [1, 2]              │
    │  stepsCompensated: [2, 1]           │
    │  error: "Database connection failed"│
    └─────────────────────────────────────┘
```

---

## Key Concepts Summary

### Orchestration Benefits
```
✅ Centralized control
✅ Easy to understand flow
✅ Better error handling
✅ Easier debugging
✅ State management
✅ Timeout handling
```

### Orchestration Challenges
```
❌ Single point of failure
❌ Additional service needed
❌ More coupling
❌ Orchestrator complexity
❌ State persistence required
```

### Best Practices
```
1. Persist saga state
2. Implement idempotency
3. Handle timeouts properly
4. Design clear step definitions
5. Implement proper compensation
6. Monitor orchestrator health
7. Use state machine pattern
```

---

**Next: Part 7 will cover Outbox Pattern.**

