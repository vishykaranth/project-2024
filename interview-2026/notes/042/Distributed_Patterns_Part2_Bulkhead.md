# Distributed Patterns - Complete Diagrams Guide (Part 2: Bulkhead Pattern)

## 🚢 Bulkhead Pattern

---

## 1. Bulkhead Pattern Overview

### Basic Concept
```
┌─────────────────────────────────────────────────────────────┐
│              Bulkhead Pattern                                │
└─────────────────────────────────────────────────────────────┘

    Application
    ┌─────────────────────────────────────┐
    │  ┌──────┐  ┌──────┐  ┌──────┐      │
    │  │ Pool │  │ Pool │  │ Pool │      │
    │  │  A   │  │  B   │  │  C   │      │
    │  └──┬───┘  └──┬───┘  └──┬───┘      │
    │     │         │         │           │
    │     ▼         ▼         ▼           │
    │  Service A  Service B  Service C   │
    └─────────────────────────────────────┘

Purpose:
- Resource isolation
- Failure containment
- Prevent cascading failures
- Independent scaling
```

### Ship Bulkhead Analogy
```
┌─────────────────────────────────────────────────────────────┐
│              Ship Bulkhead Analogy                          │
└─────────────────────────────────────────────────────────────┘

    Ship Hull
    ┌─────┬─────┬─────┬─────┐
    │     │     │     │     │  ← Bulkheads (walls)
    │  1  │  2  │  3  │  4  │     Isolate compartments
    │     │     │     │     │
    └─────┴─────┴─────┴─────┘

If compartment 1 floods:
    ❌ Without bulkheads: Entire ship sinks
    ✅ With bulkheads: Only compartment 1 affected

Application:
    If Service A fails:
    ❌ Without isolation: All services affected
    ✅ With bulkheads: Only Service A affected
```

---

## 2. Resource Isolation Types

### Thread Pool Isolation
```
┌─────────────────────────────────────────────────────────────┐
│              Thread Pool Isolation                          │
└─────────────────────────────────────────────────────────────┘

Application
    │
    ├─── Thread Pool A (10 threads)
    │    │
    │    ├─── Service A Call 1
    │    ├─── Service A Call 2
    │    └─── Service A Call 3
    │
    ├─── Thread Pool B (15 threads)
    │    │
    │    ├─── Service B Call 1
    │    ├─── Service B Call 2
    │    └─── Service B Call 3
    │
    └─── Thread Pool C (5 threads)
         │
         ├─── Service C Call 1
         └─── Service C Call 2

Benefits:
- Service A slowdown doesn't block Service B
- Independent thread allocation
- Controlled resource usage
```

### Connection Pool Isolation
```
┌─────────────────────────────────────────────────────────────┐
│              Connection Pool Isolation                       │
└─────────────────────────────────────────────────────────────┘

Application
    │
    ├─── Database Connection Pool A (20 connections)
    │    │
    │    └─── Database A
    │
    ├─── Database Connection Pool B (15 connections)
    │    │
    │    └─── Database B
    │
    └─── External API Connection Pool (10 connections)
         │
         └─── External Service

Benefits:
- Database A overload doesn't affect Database B
- Independent connection limits
- Better resource management
```

### Memory Isolation
```
┌─────────────────────────────────────────────────────────────┐
│              Memory Isolation                                │
└─────────────────────────────────────────────────────────────┘

Application Memory
    ┌─────────────────────────────────────┐
    │  ┌──────────────┐                  │
    │  │ Service A    │  Max: 512 MB     │
    │  │ Memory Pool  │                  │
    │  └──────────────┘                  │
    │  ┌──────────────┐                  │
    │  │ Service B    │  Max: 256 MB     │
    │  │ Memory Pool  │                  │
    │  └──────────────┘                  │
    │  ┌──────────────┐                  │
    │  │ Service C    │  Max: 128 MB     │
    │  │ Memory Pool  │                  │
    │  └──────────────┘                  │
    └─────────────────────────────────────┘

Benefits:
- Memory leak in Service A doesn't crash entire app
- Predictable memory usage
- Better garbage collection
```

---

## 3. Bulkhead Implementation

### Thread Pool Configuration
```
┌─────────────────────────────────────────────────────────────┐
│              Thread Pool Configuration                      │
└─────────────────────────────────────────────────────────────┘

Service A Thread Pool:
    ┌─────────────────────────────────────┐
    │  Core Pool Size: 5                   │
    │  Max Pool Size: 10                   │
    │  Queue Capacity: 100                 │
    │  Rejection Policy: CallerRunsPolicy │
    └─────────────────────────────────────┘

Service B Thread Pool:
    ┌─────────────────────────────────────┐
    │  Core Pool Size: 3                  │
    │  Max Pool Size: 8                   │
    │  Queue Capacity: 50                  │
    │  Rejection Policy: AbortPolicy       │
    └─────────────────────────────────────┘

Service C Thread Pool:
    ┌─────────────────────────────────────┐
    │  Core Pool Size: 2                  │
    │  Max Pool Size: 5                   │
    │  Queue Capacity: 25                 │
    │  Rejection Policy: DiscardPolicy     │
    └─────────────────────────────────────┘
```

### Request Flow with Bulkheads
```
┌─────────────────────────────────────────────────────────────┐
│              Request Flow                                   │
└─────────────────────────────────────────────────────────────┘

Incoming Request
    │
    ▼
┌─────────────────────────────────┐
│  Request Router                 │
│  (Determines service)           │
└───────────┬─────────────────────┘
            │
    ┌───────┴───────┐
    │               │
    ▼               ▼
Service A      Service B
    │               │
    ▼               ▼
┌─────────┐    ┌─────────┐
│Thread   │    │Thread   │
│Pool A   │    │Pool B   │
│(10 max) │    │(8 max)  │
└────┬────┘    └────┬────┘
     │               │
     ▼               ▼
┌─────────┐    ┌─────────┐
│Service A│    │Service B│
│Backend  │    │Backend  │
└─────────┘    └─────────┘
```

---

## 4. Failure Containment

### Failure Scenario
```
┌─────────────────────────────────────────────────────────────┐
│              Failure Containment Example                     │
└─────────────────────────────────────────────────────────────┘

Without Bulkheads:
    ┌─────────────────────────────────────┐
    │  Shared Thread Pool (30 threads)    │
    │  ┌───────────────────────────────┐ │
    │  │ Service A: 25 threads (slow) │ │
    │  │ Service B: 5 threads          │ │
    │  │ Service C: 0 threads (blocked)│ │
    │  └───────────────────────────────┘ │
    │  ❌ Service C cannot execute       │
    │  ❌ Service B severely impacted    │
    └─────────────────────────────────────┘

With Bulkheads:
    ┌─────────────────────────────────────┐
    │  Pool A (10 threads)               │
    │  ┌───────────────────────────────┐ │
    │  │ Service A: 10 threads (slow)  │ │
    │  │ ❌ Only Service A affected    │ │
    │  └───────────────────────────────┘ │
    │  ┌───────────────────────────────┐ │
    │  │ Pool B (8 threads)            │ │
    │  │ Service B: 8 threads ✅       │ │
    │  └───────────────────────────────┘ │
    │  ┌───────────────────────────────┐ │
    │  │ Pool C (5 threads)            │ │
    │  │ Service C: 5 threads ✅       │ │
    │  └───────────────────────────────┘ │
    │  ✅ Services B and C unaffected     │
    └─────────────────────────────────────┘
```

---

## 5. Database Connection Pool Isolation

### Multi-Database Scenario
```
┌─────────────────────────────────────────────────────────────┐
│              Database Connection Pool Isolation             │
└─────────────────────────────────────────────────────────────┘

Application
    │
    ├─── User Service
    │    │
    │    └─── Connection Pool 1 (20 connections)
    │         │
    │         └─── User Database
    │
    ├─── Order Service
    │    │
    │    └─── Connection Pool 2 (15 connections)
    │         │
    │         └─── Order Database
    │
    └─── Payment Service
         │
         └─── Connection Pool 3 (10 connections)
              │
              └─── Payment Database

Benefits:
- User DB overload doesn't block Order DB
- Independent connection limits
- Better resource utilization
```

### Connection Pool Configuration
```
┌─────────────────────────────────────────────────────────────┐
│              Connection Pool Settings                       │
└─────────────────────────────────────────────────────────────┘

User Database Pool:
    ┌─────────────────────────────────────┐
    │  Min Connections: 5                 │
    │  Max Connections: 20                 │
    │  Connection Timeout: 30s            │
    │  Idle Timeout: 10min                │
    └─────────────────────────────────────┘

Order Database Pool:
    ┌─────────────────────────────────────┐
    │  Min Connections: 3                 │
    │  Max Connections: 15                │
    │  Connection Timeout: 30s            │
    │  Idle Timeout: 10min                │
    └─────────────────────────────────────┘

Payment Database Pool:
    ┌─────────────────────────────────────┐
    │  Min Connections: 2                  │
    │  Max Connections: 10                 │
    │  Connection Timeout: 30s            │
    │  Idle Timeout: 10min                │
    └─────────────────────────────────────┘
```

---

## 6. Microservices Bulkhead

### Service-Level Isolation
```
┌─────────────────────────────────────────────────────────────┐
│              Microservices Bulkhead                        │
└─────────────────────────────────────────────────────────────┘

API Gateway
    │
    ├─── User Service (Instance 1, 2, 3)
    │    │
    │    └─── Dedicated Resources
    │         - CPU: 2 cores
    │         - Memory: 2 GB
    │         - Thread Pool: 20
    │
    ├─── Order Service (Instance 1, 2)
    │    │
    │    └─── Dedicated Resources
    │         - CPU: 1 core
    │         - Memory: 1 GB
    │         - Thread Pool: 15
    │
    └─── Payment Service (Instance 1)
         │
         └─── Dedicated Resources
              - CPU: 1 core
              - Memory: 512 MB
              - Thread Pool: 10

Benefits:
- Independent scaling
- Failure isolation
- Resource guarantees
```

---

## 7. Real-World Example

### E-Commerce Platform
```
┌─────────────────────────────────────────────────────────────┐
│              E-Commerce Platform Example                   │
└─────────────────────────────────────────────────────────────┘

Web Application
    │
    ├─── Product Service Bulkhead
    │    │
    │    ├─── Thread Pool: 20 threads
    │    ├─── DB Pool: 15 connections
    │    └─── Cache Pool: 10 connections
    │
    ├─── Order Service Bulkhead
    │    │
    │    ├─── Thread Pool: 15 threads
    │    ├─── DB Pool: 10 connections
    │    └─── Message Queue: 5 connections
    │
    ├─── Payment Service Bulkhead
    │    │
    │    ├─── Thread Pool: 10 threads
    │    ├─── DB Pool: 8 connections
    │    └─── External API: 5 connections
    │
    └─── Notification Service Bulkhead
         │
         ├─── Thread Pool: 5 threads
         ├─── DB Pool: 3 connections
         └─── Email Service: 2 connections

Scenario: Payment Service Slowdown
    ❌ Without bulkheads: All services affected
    ✅ With bulkheads: Only payment service affected
    ✅ Product, Order, Notification continue normally
```

---

## 8. Implementation Patterns

### Java Implementation (Hystrix-style)
```
┌─────────────────────────────────────────────────────────────┐
│              Implementation Example                         │
└─────────────────────────────────────────────────────────────┘

Thread Pool Isolation:
    ┌─────────────────────────────────────┐
    │  ExecutorService productPool =      │
    │    new ThreadPoolExecutor(          │
    │      5,  // core pool size          │
    │      10, // max pool size           │
    │      60L, TimeUnit.SECONDS,          │
    │      new LinkedBlockingQueue<>(100) │
    │    );                                │
    │                                      │
    │  Future<String> result =             │
    │    productPool.submit(() ->          │
    │      productService.getProduct(id)); │
    └─────────────────────────────────────┘

Connection Pool Isolation:
    ┌─────────────────────────────────────┐
    │  HikariConfig userDbConfig =        │
    │    new HikariConfig();              │
    │  userDbConfig.setMaximumPoolSize(20);│
    │  userDbConfig.setMinimumIdle(5);    │
    │                                      │
    │  HikariDataSource userDb =          │
    │    new HikariDataSource(userDbConfig);│
    │                                      │
    │  HikariConfig orderDbConfig =       │
    │    new HikariConfig();              │
    │  orderDbConfig.setMaximumPoolSize(15);│
    │  orderDbConfig.setMinimumIdle(3);   │
    │                                      │
    │  HikariDataSource orderDb =         │
    │    new HikariDataSource(orderDbConfig);│
    └─────────────────────────────────────┘
```

---

## Key Concepts Summary

### Bulkhead Benefits
```
✅ Failure isolation
✅ Resource protection
✅ Independent scaling
✅ Predictable performance
✅ Better resource utilization
```

### Isolation Types
```
- Thread pool isolation
- Connection pool isolation
- Memory isolation
- CPU isolation
- Network isolation
```

### Best Practices
```
1. Isolate critical services
2. Set appropriate pool sizes
3. Monitor resource usage
4. Use separate pools for different services
5. Consider service criticality
6. Implement proper rejection policies
```

---

**Next: Part 3 will cover Retry Patterns.**

