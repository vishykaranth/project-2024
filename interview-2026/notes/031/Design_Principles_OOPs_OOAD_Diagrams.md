# Design Principles, OOPs & OOAD Concepts - Visual Diagrams

## 📋 Table of Contents
1. [SOLID Principles](#solid-principles)
2. [Other Design Principles](#other-design-principles)
3. [OOPs Concepts](#oops-concepts)
4. [OOAD Concepts](#ooad-concepts)
5. [Design Patterns Overview](#design-patterns-overview)

---

## SOLID Principles

### 1. Single Responsibility Principle (SRP)

**Definition**: A class should have only one reason to change.

#### ❌ Violation Example
```
┌─────────────────────────────────────┐
│         User Class                  │
├─────────────────────────────────────┤
│ + name: String                      │
│ + email: String                     │
│ + password: String                  │
│                                     │
│ + saveToDatabase()                  │  ← Database responsibility
│ + sendEmail()                       │  ← Email responsibility
│ + validateEmail()                   │  ← Validation responsibility
│ + generateReport()                  │  ← Reporting responsibility
└─────────────────────────────────────┘
         ❌ Too many responsibilities!
```

#### ✅ Correct Implementation
```
┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐
│   User Class    │      │  UserRepository  │      │  EmailService   │
├─────────────────┤      ├─────────────────┤      ├─────────────────┤
│ + name: String  │      │ + save(user)    │      │ + send(user)    │
│ + email: String │      │ + find(id)       │      │ + validate()     │
│ + password: Str │      │ + delete(user)   │      └─────────────────┘
└─────────────────┘      └─────────────────┘
         │                       │
         └───────────────────────┘
              ✅ Each class has one responsibility
```

**Diagram Flow:**
```
User Object
    │
    ├──→ UserRepository.save()     (Database responsibility)
    ├──→ EmailService.send()       (Email responsibility)
    └──→ Validator.validate()      (Validation responsibility)
```

---

### 2. Open/Closed Principle (OCP)

**Definition**: Software entities should be open for extension but closed for modification.

#### ❌ Violation Example
```
┌─────────────────────────────────────┐
│      AreaCalculator                 │
├─────────────────────────────────────┤
│ + calculateArea(shape: Shape)       │
│   if (shape.type == "Circle")       │  ← Must modify for new shapes
│     return π * r²                   │
│   else if (shape.type == "Rect")    │
│     return length * width            │
│   else if (shape.type == "Triangle")│  ← Adding new shape requires modification
│     return 0.5 * base * height      │
└─────────────────────────────────────┘
         ❌ Closed for extension!
```

#### ✅ Correct Implementation
```
┌─────────────────────┐
│    Shape (Abstract) │
├─────────────────────┤
│ + calculateArea()   │  ← Abstract method
└─────────────────────┘
         ▲
         │
    ┌────┴────┬──────────────┬──────────────┐
    │         │              │              │
┌─────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
│ Circle  │ │ Rectangle│ │ Triangle │ │ Pentagon │  ← New shapes can be added
├─────────┤ ├──────────┤ ├──────────┤ ├──────────┤     without modifying
│ + radius│ │ + length │ │ + base   │ │ + sides  │     existing code
│         │ │ + width  │ │ + height │ │          │
│         │ │          │ │          │ │          │
│ + calc  │ │ + calc   │ │ + calc   │ │ + calc   │
│  Area() │ │  Area()  │ │  Area()  │ │  Area()  │
└─────────┘ └──────────┘ └──────────┘ └──────────┘
    ✅ Open for extension, Closed for modification
```

**Extension Flow:**
```
Shape (Interface)
    │
    ├──→ Circle.calculateArea()      (Existing - no change)
    ├──→ Rectangle.calculateArea()   (Existing - no change)
    ├──→ Triangle.calculateArea()    (Existing - no change)
    └──→ Pentagon.calculateArea()    (New - just extend, no modification)
```

---

### 3. Liskov Substitution Principle (LSP)

**Definition**: Objects of a superclass should be replaceable with objects of its subclasses without breaking the application.

#### ❌ Violation Example
```
┌─────────────────────┐
│     Rectangle       │
├─────────────────────┤
│ + width: int        │
│ + height: int       │
│ + setWidth(w)       │
│ + setHeight(h)      │
└─────────────────────┘
         ▲
         │
┌─────────────────────┐
│      Square         │
├─────────────────────┤
│ + setWidth(w)       │  ← Sets both width AND height
│ + setHeight(h)      │  ← Violates LSP! Square behaves
└─────────────────────┘     differently than Rectangle
         ❌ Cannot substitute Square for Rectangle!
```

#### ✅ Correct Implementation
```
┌─────────────────────┐
│    Shape (Abstract) │
├─────────────────────┤
│ + calculateArea()   │
└─────────────────────┘
         ▲
         │
    ┌────┴────┐
    │         │
┌──────────┐ ┌──────────┐
│Rectangle │ │  Square  │
├──────────┤ ├──────────┤
│ + width  │ │ + side   │
│ + height │ │          │
│          │ │          │
│ + calc   │ │ + calc   │
│  Area()  │ │  Area()  │
└──────────┘ └──────────┘
    ✅ Both can be substituted for Shape
```

**Substitution Flow:**
```
Shape shape1 = new Rectangle();  ✅ Works
Shape shape2 = new Square();      ✅ Works
shape1.calculateArea();            ✅ Works
shape2.calculateArea();            ✅ Works
```

---

### 4. Interface Segregation Principle (ISP)

**Definition**: Clients should not be forced to depend on interfaces they don't use.

#### ❌ Violation Example
```
┌─────────────────────────────────────┐
│      Worker Interface               │
├─────────────────────────────────────┤
│ + work()                            │
│ + eat()                             │  ← Human needs
│ + sleep()                           │  ← Human needs
└─────────────────────────────────────┘
         ▲
         │
    ┌────┴────┐
    │         │
┌──────────┐ ┌──────────┐
│  Human   │ │  Robot   │
├──────────┤ ├──────────┤
│ + work() │ │ + work() │
│ + eat()  │ │ + eat()  │  ← Robot doesn't need this!
│ + sleep()│ │ + sleep()│  ← Robot doesn't need this!
└──────────┘ └──────────┘
         ❌ Robot forced to implement unnecessary methods!
```

#### ✅ Correct Implementation
```
┌─────────────────────┐
│    Workable         │
├─────────────────────┤
│ + work()            │
└─────────────────────┘
         ▲
         │
    ┌────┴────┐
    │         │
┌──────────┐ ┌──────────┐
│  Human   │ │  Robot   │
│          │ │          │
│ + work() │ │ + work() │
└──────────┘ └──────────┘

┌─────────────────────┐
│    Eatable          │
├─────────────────────┤
│ + eat()             │
└─────────────────────┘
         ▲
         │
┌──────────┐
│  Human   │
│          │
│ + eat()  │
└──────────┘

┌─────────────────────┐
│    Sleepable         │
├─────────────────────┤
│ + sleep()           │
└─────────────────────┘
         ▲
         │
┌──────────┐
│  Human   │
│          │
│ + sleep()│
└──────────┘
    ✅ Segregated interfaces - clients only implement what they need
```

**Interface Segregation:**
```
Worker Interface (Too Large)
    │
    ├──→ Workable (work only)
    ├──→ Eatable (eat only)
    └──→ Sleepable (sleep only)
    
Human implements: Workable + Eatable + Sleepable
Robot implements: Workable only
```

---

### 5. Dependency Inversion Principle (DIP)

**Definition**: High-level modules should not depend on low-level modules. Both should depend on abstractions.

#### ❌ Violation Example
```
┌─────────────────────┐
│   UserService       │  (High-level)
├─────────────────────┤
│ + saveUser()        │
│   MySQLDB db =      │  ← Direct dependency on low-level
│     new MySQLDB()   │
│   db.save()         │
└─────────────────────┘
         │
         ▼
┌─────────────────────┐
│     MySQLDB         │  (Low-level)
├─────────────────────┤
│ + save()            │
└─────────────────────┘
         ❌ High-level depends on low-level!
```

#### ✅ Correct Implementation
```
┌─────────────────────┐
│   UserService       │  (High-level)
├─────────────────────┤
│ - db: Database      │  ← Depends on abstraction
│                     │
│ + saveUser()        │
│   db.save()         │
└─────────────────────┘
         │
         │ depends on
         ▼
┌─────────────────────┐
│   Database          │  (Abstraction/Interface)
│   (Interface)       │
├─────────────────────┤
│ + save()            │
│ + find()            │
│ + delete()          │
└─────────────────────┘
         ▲
         │ implements
    ┌────┴────┐
    │         │
┌──────────┐ ┌──────────┐
│ MySQLDB  │ │PostgreSQL│  (Low-level)
├──────────┤ ├──────────┤
│ + save() │ │ + save() │
│ + find() │ │ + find() │
│ + delete()│ │ + delete()│
└──────────┘ └──────────┘
    ✅ Both depend on abstraction!
```

**Dependency Flow:**
```
UserService (High-level)
    │
    │ depends on
    ▼
Database (Interface/Abstraction)
    ▲
    │ implements
    ├──→ MySQLDB (Low-level)
    └──→ PostgreSQL (Low-level)
```

---

## Other Design Principles

### DRY (Don't Repeat Yourself)

#### ❌ Violation
```
┌─────────────────────┐      ┌─────────────────────┐
│   OrderService      │      │  PaymentService    │
├─────────────────────┤      ├─────────────────────┤
│ + validateEmail()   │      │ + validateEmail()   │  ← Duplicate code
│ + formatCurrency()  │      │ + formatCurrency()  │  ← Duplicate code
│ + logError()        │      │ + logError()        │  ← Duplicate code
└─────────────────────┘      └─────────────────────┘
```

#### ✅ Correct
```
┌─────────────────────┐
│   OrderService      │
└─────────────────────┘
         │ uses
         ▼
┌─────────────────────┐
│  UtilityService    │  ← Single source of truth
├─────────────────────┤
│ + validateEmail()   │
│ + formatCurrency()  │
│ + logError()        │
└─────────────────────┘
         ▲
         │ uses
┌─────────────────────┐
│  PaymentService     │
└─────────────────────┘
```

---

### KISS (Keep It Simple, Stupid)

#### ❌ Complex Solution
```
┌─────────────────────────────────────────────┐
│  ComplexCalculator                          │
├─────────────────────────────────────────────┤
│ + calculateSum(a, b)                        │
│   StrategyFactory factory =                 │
│     new StrategyFactory()                   │
│   Strategy strategy =                       │
│     factory.create("addition")              │
│   Context context =                         │
│     new Context(strategy)                   │
│   return context.execute(a, b)              │
└─────────────────────────────────────────────┘
         ❌ Over-engineered!
```

#### ✅ Simple Solution
```
┌─────────────────────┐
│  Calculator         │
├─────────────────────┤
│ + sum(a, b)         │
│   return a + b      │  ← Simple and clear
└─────────────────────┘
         ✅ Simple!
```

---

### YAGNI (You Aren't Gonna Need It)

#### ❌ Premature Optimization
```
┌─────────────────────────────────────┐
│   UserService                        │
├─────────────────────────────────────┤
│ + getUser()                          │
│ + createUser()                       │
│ + updateUser()                       │
│ + deleteUser()                       │
│ + archiveUser()                      │  ← Not needed yet
│ + restoreUser()                      │  ← Not needed yet
│ + exportUsers()                      │  ← Not needed yet
│ + importUsers()                      │  ← Not needed yet
│ + generateReport()                   │  ← Not needed yet
└─────────────────────────────────────┘
         ❌ Building features that may never be used!
```

#### ✅ Only What's Needed
```
┌─────────────────────┐
│   UserService       │
├─────────────────────┤
│ + getUser()         │  ← Current requirement
│ + createUser()      │  ← Current requirement
│ + updateUser()      │  ← Current requirement
│ + deleteUser()      │  ← Current requirement
└─────────────────────┘
         ✅ Only implement what's needed now!
```

---

## OOPs Concepts

### 1. Encapsulation

**Definition**: Binding data and methods together, hiding internal details.

#### Diagram
```
┌─────────────────────────────────────┐
│         BankAccount                 │
│  ┌─────────────────────────────┐   │
│  │   Private Data (Hidden)     │   │
│  │   ────────────────────────   │   │
│  │   - balance: double          │   │  ← Private (hidden)
│  │   - accountNumber: String    │   │  ← Private (hidden)
│  │   - pin: int                 │   │  ← Private (hidden)
│  └─────────────────────────────┘   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │   Public Methods (Interface)│   │
│  │   ────────────────────────   │   │
│  │   + deposit(amount)         │   │  ← Public (accessible)
│  │   + withdraw(amount, pin)   │   │  ← Public (accessible)
│  │   + getBalance(pin)         │   │  ← Public (accessible)
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
         │
         │ Can only access through public methods
         ▼
┌─────────────────────┐
│   External Code     │
├─────────────────────┤
│ account.deposit(100)│  ✅ Allowed
│ account.balance     │  ❌ Not allowed (private)
└─────────────────────┘
```

**Access Modifiers:**
```
┌──────────────┬──────────────┬──────────────┬──────────────┐
│   Modifier   │   Class      │   Package   │   Outside   │
├──────────────┼──────────────┼──────────────┼──────────────┤
│   private    │      ✅      │      ❌      │      ❌     │
│   default    │      ✅      │      ✅      │      ❌     │
│   protected  │      ✅      │      ✅      │   (subclass)│
│   public     │      ✅      │      ✅      │      ✅     │
└──────────────┴──────────────┴──────────────┴──────────────┘
```

---

### 2. Inheritance

**Definition**: Mechanism where a class acquires properties and methods from another class.

#### Single Inheritance
```
┌─────────────────────┐
│      Vehicle        │  (Parent/Superclass)
├─────────────────────┤
│ + brand: String     │
│ + speed: int        │
│                     │
│ + start()           │
│ + stop()            │
└─────────────────────┘
         ▲
         │ extends
         │
┌─────────────────────┐
│        Car          │  (Child/Subclass)
├─────────────────────┤
│ + numberOfDoors: int│  ← Additional property
│                     │
│ + openTrunk()       │  ← Additional method
│                     │
│ Inherits:           │
│ - brand, speed       │
│ - start(), stop()    │
└─────────────────────┘
```

#### Multi-level Inheritance
```
┌─────────────────────┐
│      Animal         │  (Grandparent)
├─────────────────────┤
│ + name: String      │
│ + age: int          │
│ + eat()             │
└─────────────────────┘
         ▲
         │ extends
         │
┌─────────────────────┐
│      Mammal         │  (Parent)
├─────────────────────┤
│ + furColor: String  │
│ + giveBirth()       │
└─────────────────────┘
         ▲
         │ extends
         │
┌─────────────────────┐
│        Dog          │  (Child)
├─────────────────────┤
│ + breed: String     │
│ + bark()            │
└─────────────────────┘
```

#### Hierarchical Inheritance
```
┌─────────────────────┐
│      Shape          │  (Parent)
├─────────────────────┤
│ + color: String     │
│ + draw()            │
└─────────────────────┘
         ▲
    ┌────┴────┬──────────────┬──────────────┐
    │         │              │              │
┌─────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
│ Circle  │ │ Rectangle│ │ Triangle │ │ Pentagon │
├─────────┤ ├──────────┤ ├──────────┤ ├──────────┤
│ + radius│ │ + length │ │ + base   │ │ + sides  │
│         │ │ + width  │ │ + height │ │          │
└─────────┘ └──────────┘ └──────────┘ └──────────┘
```

---

### 3. Polymorphism

**Definition**: Ability of objects to take multiple forms.

#### Compile-time Polymorphism (Method Overloading)
```
┌─────────────────────┐
│   Calculator        │
├─────────────────────┤
│ + add(a: int,       │  ← Same method name
│       b: int)       │    Different parameters
│                     │
│ + add(a: int,       │  ← Same method name
│       b: int,       │    Different parameters
│       c: int)       │
│                     │
│ + add(a: double,    │  ← Same method name
│       b: double)    │    Different parameters
└─────────────────────┘

Usage:
calc.add(5, 10)           → Calls first method
calc.add(5, 10, 15)      → Calls second method
calc.add(5.5, 10.5)      → Calls third method
```

#### Runtime Polymorphism (Method Overriding)
```
┌─────────────────────┐
│      Animal         │
├─────────────────────┤
│ + makeSound()       │  ← Base method
│   print("Some sound")│
└─────────────────────┘
         ▲
    ┌────┴────┐
    │         │
┌─────────┐ ┌──────────┐
│   Dog   │ │   Cat    │
├─────────┤ ├──────────┤
│ + make  │ │ + make   │
│  Sound()│ │  Sound() │  ← Override
│   print │ │   print  │
│  ("Woof")│ │ ("Meow") │
└─────────┘ └──────────┘

Animal animal1 = new Dog();
animal1.makeSound();  → "Woof" (Runtime decision)

Animal animal2 = new Cat();
animal2.makeSound();  → "Meow" (Runtime decision)
```

#### Polymorphism Flow
```
Animal (Reference Type)
    │
    ├──→ Dog.makeSound()     → "Woof"
    ├──→ Cat.makeSound()     → "Meow"
    └──→ Bird.makeSound()    → "Tweet"
    
Same reference type, different behaviors!
```

---

### 4. Abstraction

**Definition**: Hiding implementation details and showing only essential features.

#### Abstract Class
```
┌─────────────────────────────────────┐
│    AbstractAnimal (Abstract)       │
├─────────────────────────────────────┤
│ + name: String                      │  ← Concrete property
│                                     │
│ + makeSound() (Abstract)            │  ← Abstract method
│                                     │    (No implementation)
│ + eat()                             │  ← Concrete method
│   print("Eating...")                │    (Has implementation)
└─────────────────────────────────────┘
         ▲
    ┌────┴────┐
    │         │
┌─────────┐ ┌──────────┐
│   Dog   │ │   Cat    │
├─────────┤ ├──────────┤
│ + make  │ │ + make   │  ← Must implement
│  Sound()│ │  Sound() │    abstract method
│   print │ │   print  │
│  ("Woof")│ │ ("Meow") │
└─────────┘ └──────────┘
```

#### Interface (Pure Abstraction)
```
┌─────────────────────┐
│   Drawable          │  (Interface)
│   (Interface)       │
├─────────────────────┤
│ + draw()            │  ← All methods abstract
│ + resize()          │  ← All methods abstract
│ + getArea()         │  ← All methods abstract
└─────────────────────┘
         ▲
    ┌────┴────┐
    │         │ implements
┌─────────┐ ┌──────────┐
│ Circle  │ │ Rectangle│
├─────────┤ ├──────────┤
│ + draw()│ │ + draw() │  ← Must implement
│ + resize│ │ + resize │    all methods
│ + getArea│ │ + getArea│
└─────────┘ └──────────┘
```

#### Abstraction Levels
```
High Abstraction (Interface)
    │
    │  Drawable.draw()
    │
    ▼
Medium Abstraction (Abstract Class)
    │
    │  Shape.draw() { ... }
    │
    ▼
Low Abstraction (Concrete Class)
    │
    │  Circle.draw() { specific implementation }
    │
    ▼
Implementation Details (Hidden)
```

---

## OOAD Concepts

### 1. Class Diagram

#### Basic Class Diagram
```
┌─────────────────────────────────────┐
│         BankAccount                 │
├─────────────────────────────────────┤
│  - accountNumber: String            │  ← Private attribute
│  - balance: double                  │  ← Private attribute
│  - pin: int                         │  ← Private attribute
├─────────────────────────────────────┤
│  + deposit(amount: double): void    │  ← Public method
│  + withdraw(amount: double): boolean│  ← Public method
│  + getBalance(): double             │  ← Public method
│  - validatePin(pin: int): boolean  │  ← Private method
└─────────────────────────────────────┘
```

#### Class Relationships

**Association (Has-A)**
```
┌─────────────┐         ┌─────────────┐
│   Student   │─────────│   Course    │
├─────────────┤         ├─────────────┤
│ + name      │         │ + name      │
│ + enroll()  │         │ + code      │
└─────────────┘         └─────────────┘
    Student enrolls in Course
```

**Aggregation (Has-A, Weak)**
```
┌─────────────┐         ┌─────────────┐
│  University │─────────│  Department │
├─────────────┤         ├─────────────┤
│ + name      │         │ + name      │
│             │         │             │
└─────────────┘         └─────────────┘
    University has Departments
    (Departments can exist without University)
```

**Composition (Has-A, Strong)**
```
┌─────────────┐         ┌─────────────┐
│    House    │─────────│     Room    │
├─────────────┤         ├─────────────┤
│ + address   │         │ + roomNumber│
│             │         │             │
└─────────────┘         └─────────────┘
    House contains Rooms
    (Rooms cannot exist without House)
```

**Inheritance (Is-A)**
```
┌─────────────┐
│   Vehicle   │
├─────────────┤
│ + speed     │
│ + start()   │
└─────────────┘
       ▲
       │ extends
       │
┌─────────────┐
│     Car     │
├─────────────┤
│ + doors     │
│ + drive()   │
└─────────────┘
```

**Dependency (Uses)**
```
┌─────────────┐         ┌─────────────┐
│  OrderService│────────→│  PaymentService│
├─────────────┤         ├─────────────┤
│ + process() │         │ + pay()     │
└─────────────┘         └─────────────┘
    OrderService uses PaymentService
```

---

### 2. Object Diagram

```
┌─────────────────────┐
│   student1:Student  │
├─────────────────────┤
│ name = "John"       │
│ id = 101            │
│ age = 20            │
└─────────────────────┘
         │
         │ enrolled in
         ▼
┌─────────────────────┐
│   course1:Course    │
├─────────────────────┤
│ code = "CS101"      │
│ name = "Java"       │
│ credits = 3         │
└─────────────────────┘
```

---

### 3. Sequence Diagram

```
User          OrderService    PaymentService    InventoryService
 │                  │                │                  │
 │  placeOrder()    │                │                  │
 ├─────────────────>│                │                  │
 │                  │                │                  │
 │                  │  processPayment│                  │
 │                  ├───────────────>│                  │
 │                  │                │                  │
 │                  │  paymentResult│                  │
 │                  │<───────────────┤                  │
 │                  │                │                  │
 │                  │  reserveItems  │                  │
 │                  ├──────────────────────────────────>│
 │                  │                │                  │
 │                  │  reservationResult                │
 │                  │<──────────────────────────────────┤
 │                  │                │                  │
 │  orderResult     │                │                  │
 │<─────────────────┤                │                  │
 │                  │                │                  │
```

---

### 4. State Diagram

```
┌─────────┐
│  New    │
└────┬────┘
     │ create
     ▼
┌─────────┐
│ Pending │
└────┬────┘
     │
     ├─── payment ───> ┌─────────┐
     │                  │ Paid    │
     │                  └────┬────┘
     │                       │
     │                       │ ship ───> ┌─────────┐
     │                       │           │ Shipped │
     │                       │           └────┬────┘
     │                       │                 │
     │                       │                 │ deliver
     │                       │                 ▼
     │                       │           ┌─────────┐
     │                       │           │Delivered│
     │                       │           └─────────┘
     │                       │
     └─── cancel ────────────┴───> ┌─────────┐
                                    │Cancelled│
                                    └─────────┘
```

---

### 5. Activity Diagram

```
    [Start]
      │
      ▼
┌─────────────┐
│ Login User  │
└──────┬──────┘
       │
       ▼
┌─────────────┐      No
│ Valid User? │───────────> [End]
└──────┬──────┘
       │ Yes
       ▼
┌─────────────┐
│ Browse Items│
└──────┬──────┘
       │
       ▼
┌─────────────┐      No
│ Add to Cart?│───────────> [Continue Shopping]
└──────┬──────┘
       │ Yes
       ▼
┌─────────────┐
│ Checkout    │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ Process Payment│
└──────┬──────┘
       │
       ▼
    [End]
```

---

### 6. Use Case Diagram

```
┌─────────────────────────────────────┐
│         Online Shopping System      │
│                                     │
│  ┌──────────┐                       │
│  │  Browse  │                       │
│  │ Products │                       │
│  └──────────┘                       │
│                                     │
│  ┌──────────┐                       │
│  │ Add to   │                       │
│  │  Cart    │                       │
│  └──────────┘                       │
│                                     │
│  ┌──────────┐                       │
│  │ Checkout │                       │
│  └──────────┘                       │
│                                     │
│  ┌──────────┐                       │
│  │  Payment │                       │
│  └──────────┘                       │
└─────────────────────────────────────┘
         ▲
         │
    ┌────┴────┐
    │         │
┌─────────┐ ┌──────────┐
│Customer │ │  Admin   │
└─────────┘ └──────────┘
```

---

## Design Patterns Overview

### Creational Patterns

```
┌─────────────────────────────────────┐
│      Creational Patterns            │
├─────────────────────────────────────┤
│                                     │
│  ┌──────────┐  ┌──────────┐        │
│  │ Singleton│  │  Factory │        │
│  └──────────┘  └──────────┘        │
│                                     │
│  ┌──────────┐  ┌──────────┐        │
│  │ Abstract │  │  Builder  │        │
│  │ Factory  │  │           │        │
│  └──────────┘  └──────────┘        │
│                                     │
│  ┌──────────┐                      │
│  │ Prototype│                      │
│  └──────────┘                      │
└─────────────────────────────────────┘
```

### Structural Patterns

```
┌─────────────────────────────────────┐
│      Structural Patterns            │
├─────────────────────────────────────┤
│                                     │
│  ┌──────────┐  ┌──────────┐        │
│  │ Adapter  │  │  Bridge  │        │
│  └──────────┘  └──────────┘        │
│                                     │
│  ┌──────────┐  ┌──────────┐        │
│  │Composite │  │ Decorator│        │
│  └──────────┘  └──────────┘        │
│                                     │
│  ┌──────────┐  ┌──────────┐        │
│  │  Facade  │  │ Flyweight│        │
│  └──────────┘  └──────────┘        │
│                                     │
│  ┌──────────┐                      │
│  │  Proxy   │                      │
│  └──────────┘                      │
└─────────────────────────────────────┘
```

### Behavioral Patterns

```
┌─────────────────────────────────────┐
│      Behavioral Patterns            │
├─────────────────────────────────────┤
│                                     │
│  ┌──────────┐  ┌──────────┐        │
│  │ Observer │  │  Strategy│        │
│  └──────────┘  └──────────┘        │
│                                     │
│  ┌──────────┐  ┌──────────┐        │
│  │ Command  │  │  Iterator│        │
│  └──────────┘  └──────────┘        │
│                                     │
│  ┌──────────┐  ┌──────────┐        │
│  │  State   │  │  Visitor │        │
│  └──────────┘  └──────────┘        │
│                                     │
│  ┌──────────┐  ┌──────────┐        │
│  │ Template │  │  Mediator│        │
│  │  Method  │  │          │        │
│  └──────────┘  └──────────┘        │
│                                     │
│  ┌──────────┐                      │
│  │  Chain of│                      │
│  │Responsibility│                   │
│  └──────────┘                      │
└─────────────────────────────────────┘
```

---

## Summary Diagrams

### SOLID Principles Overview
```
┌─────────────────────────────────────────────────────┐
│                    SOLID                            │
├─────────────────────────────────────────────────────┤
│                                                     │
│  S ──> Single Responsibility                       │
│         One class, one reason to change             │
│                                                     │
│  O ──> Open/Closed                                  │
│         Open for extension, closed for modification │
│                                                     │
│  L ──> Liskov Substitution                         │
│         Subtypes must be substitutable              │
│                                                     │
│  I ──> Interface Segregation                        │
│         Many specific interfaces, not one general   │
│                                                     │
│  D ──> Dependency Inversion                         │
│         Depend on abstractions, not concretions     │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### OOPs Pillars
```
┌─────────────────────────────────────────────────────┐
│                  OOPs Pillars                      │
├─────────────────────────────────────────────────────┤
│                                                     │
│  ┌──────────────┐                                  │
│  │Encapsulation │  Data hiding, access control     │
│  └──────────────┘                                  │
│                                                     │
│  ┌──────────────┐                                  │
│  │ Inheritance  │  Code reuse, IS-A relationship  │
│  └──────────────┘                                  │
│                                                     │
│  ┌──────────────┐                                  │
│  │Polymorphism  │  Multiple forms, flexibility     │
│  └──────────────┘                                  │
│                                                     │
│  ┌──────────────┐                                  │
│  │  Abstraction │  Hide complexity, show essentials│
│  └──────────────┘                                  │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### OOAD Process Flow
```
Requirements
    │
    ▼
Analysis
    │
    ├──> Use Case Diagram
    ├──> Activity Diagram
    └──> Domain Model
    │
    ▼
Design
    │
    ├──> Class Diagram
    ├──> Sequence Diagram
    ├──> State Diagram
    └──> Component Diagram
    │
    ▼
Implementation
    │
    ├──> Code
    ├──> Unit Tests
    └──> Integration Tests
```

---

**Use these diagrams for quick reference and understanding of design principles, OOPs, and OOAD concepts!** 🎯

