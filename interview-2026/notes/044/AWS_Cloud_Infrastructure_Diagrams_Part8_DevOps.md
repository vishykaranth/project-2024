# AWS Cloud & Infrastructure - Complete Diagrams Guide (Part 8: DevOps Services)

## 🔧 DevOps Services: CodePipeline, CodeBuild, CodeDeploy

---

## 1. AWS CodePipeline

### CodePipeline Architecture
```
┌─────────────────────────────────────────────────────────────┐
│              CodePipeline Architecture                       │
└─────────────────────────────────────────────────────────────┘

    Source Code Repository
        │
        ├──► GitHub
        ├──► CodeCommit
        ├──► S3
        └──► Bitbucket
            │
            │ Source Change
            │
            ▼
┌─────────────────────────────────────┐
│    CodePipeline                      │
│  ┌──────────────────────────────┐  │
│  │  Pipeline                     │  │
│  │                                │  │
│  │  Stage 1: Source              │  │
│  │  ┌────────────────────────┐  │  │
│  │  │  Source Action          │  │  │
│  │  │  - Get source code      │  │  │
│  │  └────────────────────────┘  │  │
│  │                                │  │
│  │  Stage 2: Build               │  │
│  │  ┌────────────────────────┐  │  │
│  │  │  Build Action           │  │  │
│  │  │  - CodeBuild            │  │  │
│  │  │  - Compile & test       │  │  │
│  │  └────────────────────────┘  │  │
│  │                                │  │
│  │  Stage 3: Deploy               │  │
│  │  ┌────────────────────────┐  │  │
│  │  │  Deploy Action          │  │  │
│  │  │  - CodeDeploy           │  │  │
│  │  │  - Deploy to EC2/ECS   │  │  │
│  │  └────────────────────────┘  │  │
│  └──────────────────────────────┘  │
└─────────────────────────────────────┘
        │
        │ Artifacts
        │
        ▼
    Target Environment
    (Dev, Staging, Prod)

Key Features:
- Continuous delivery
- Automated pipelines
- Multi-stage workflows
- Integration with AWS services
- Manual approvals
```

### CodePipeline Stages
```
┌─────────────────────────────────────────────────────────────┐
│              CodePipeline Stages                            │
└─────────────────────────────────────────────────────────────┘

STAGE 1: SOURCE
┌─────────────────────┐
│  Source Action       │
│  - GitHub            │
│  - CodeCommit        │
│  - S3                │
│  - Bitbucket         │
│                      │
│  Output:             │
│  Source artifacts    │
└─────────────────────┘
        │
        │
        ▼
STAGE 2: BUILD
┌─────────────────────┐
│  Build Action        │
│  - CodeBuild         │
│  - Jenkins           │
│  - Custom action     │
│                      │
│  Output:             │
│  Build artifacts    │
└─────────────────────┘
        │
        │
        ▼
STAGE 3: TEST
┌─────────────────────┐
│  Test Action         │
│  - Unit tests        │
│  - Integration tests │
│  - Code quality      │
│                      │
│  Output:             │
│  Test results       │
└─────────────────────┘
        │
        │
        ▼
STAGE 4: DEPLOY (DEV)
┌─────────────────────┐
│  Deploy Action       │
│  - CodeDeploy        │
│  - CloudFormation    │
│  - ECS Deploy        │
│                      │
│  Output:             │
│  Deployed to Dev    │
└─────────────────────┘
        │
        │ Manual Approval
        │
        ▼
STAGE 5: DEPLOY (PROD)
┌─────────────────────┐
│  Deploy Action       │
│  - CodeDeploy        │
│  - CloudFormation    │
│  - ECS Deploy        │
│                      │
│  Output:             │
│  Deployed to Prod   │
└─────────────────────┘
```

### CodePipeline Actions
```
┌─────────────────────────────────────────────────────────────┐
│              CodePipeline Action Types                      │
└─────────────────────────────────────────────────────────────┘

SOURCE ACTIONS:
┌─────────────────────┐
│  - CodeCommit        │
│  - GitHub            │
│  - S3                │
│  - Bitbucket         │
│  - ECR               │
└─────────────────────┘

BUILD ACTIONS:
┌─────────────────────┐
│  - CodeBuild         │
│  - Jenkins           │
│  - Custom build      │
└─────────────────────┘

TEST ACTIONS:
┌─────────────────────┐
│  - CodeBuild (test) │
│  - Custom test      │
└─────────────────────┘

DEPLOY ACTIONS:
┌─────────────────────┐
│  - CodeDeploy        │
│  - CloudFormation    │
│  - ECS               │
│  - Elastic Beanstalk│
│  - S3                │
└─────────────────────┘

APPROVAL ACTIONS:
┌─────────────────────┐
│  - Manual approval   │
│  - SNS notification  │
└─────────────────────┘
```

---

## 2. AWS CodeBuild

### CodeBuild Architecture
```
┌─────────────────────────────────────────────────────────────┐
│              CodeBuild Architecture                         │
└─────────────────────────────────────────────────────────────┘

    CodePipeline / Manual Trigger
        │
        │ Start Build
        │
        ▼
┌─────────────────────────────────────┐
│    CodeBuild                         │
│  ┌──────────────────────────────┐  │
│  │  Build Project                │  │
│  │  ┌────────────────────────┐  │  │
│  │  │  Source:                │  │  │
│  │  │  - CodeCommit           │  │  │
│  │  │  - GitHub               │  │  │
│  │  │  - S3                   │  │  │
│  │  └────────────────────────┘  │  │
│  │  ┌────────────────────────┐  │  │
│  │  │  Environment:          │  │  │
│  │  │  - Docker image         │  │  │
│  │  │  - Compute type        │  │  │
│  │  │  - Environment variables│  │  │
│  │  └────────────────────────┘  │  │
│  │  ┌────────────────────────┐  │  │
│  │  │  Buildspec.yml:        │  │  │
│  │  │  - Install dependencies│  │  │
│  │  │  - Build                │  │  │
│  │  │  - Test                 │  │  │
│  │  │  - Package              │  │  │
│  │  └────────────────────────┘  │  │
│  └──────────────────────────────┘  │
└─────────────────────────────────────┘
        │
        │ Build Execution
        │
        ▼
┌─────────────────────────────────────┐
│    Build Environment                  │
│  ┌──────────────────────────────┐  │
│  │  EC2 Instance / Docker       │  │
│  │  ┌────────────────────────┐  │  │
│  │  │  Install dependencies  │  │  │
│  │  │  npm install            │  │  │
│  │  │  mvn install             │  │  │
│  │  └────────────────────────┘  │  │
│  │  ┌────────────────────────┐  │  │
│  │  │  Build                 │  │  │
│  │  │  npm run build         │  │  │
│  │  │  mvn package           │  │  │
│  │  └────────────────────────┘  │  │
│  │  ┌────────────────────────┐  │  │
│  │  │  Test                  │  │  │
│  │  │  npm test              │  │  │
│  │  │  mvn test              │  │  │
│  │  └────────────────────────┘  │  │
│  └──────────────────────────────┘  │
└─────────────────────────────────────┘
        │
        │ Build Artifacts
        │
        ▼
┌─────────────────────────────────────┐
│    Artifact Storage                   │
│  ┌──────────┐  ┌──────────┐      │
│  │   S3     │  │  ECR     │      │
│  │  Bucket  │  │  Images  │      │
│  └──────────┘  └──────────┘      │
└─────────────────────────────────────┘

Key Features:
- Fully managed
- Scalable
- Pay per build
- Custom environments
- Build caching
```

### CodeBuild Buildspec
```
┌─────────────────────────────────────────────────────────────┐
│              CodeBuild Buildspec                            │
└─────────────────────────────────────────────────────────────┘

buildspec.yml:
┌─────────────────────────────────────┐
│  version: 0.2                       │
│                                      │
│  phases:                             │
│    pre_build:                        │
│      commands:                       │
│        - echo Logging in to Amazon ECR...│
│        - aws ecr get-login...       │
│        - docker pull $IMAGE_REPO_NAME│
│                                      │
│    build:                            │
│      commands:                       │
│        - echo Build started on `date`│
│        - echo Building the Docker image...│
│        - docker build -t $IMAGE_REPO_NAME .│
│        - docker tag $IMAGE_REPO_NAME $IMAGE_REPO_NAME:$IMAGE_TAG│
│                                      │
│    post_build:                       │
│      commands:                       │
│        - echo Build completed on `date`│
│        - echo Pushing the Docker image...│
│        - docker push $IMAGE_REPO_NAME:$IMAGE_TAG│
│                                      │
│  artifacts:                          │
│    files:                            │
│      - '**/*'                        │
│    name: build-artifacts             │
└─────────────────────────────────────┘

Build Phases:
- install: Install dependencies
- pre_build: Pre-build commands
- build: Build commands
- post_build: Post-build commands
```

---

## 3. AWS CodeDeploy

### CodeDeploy Architecture
```
┌─────────────────────────────────────────────────────────────┐
│              CodeDeploy Architecture                        │
└─────────────────────────────────────────────────────────────┘

    CodePipeline / Manual
        │
        │ Deployment Package
        │
        ▼
┌─────────────────────────────────────┐
│    CodeDeploy                        │
│  ┌──────────────────────────────┐  │
│  │  Application                 │  │
│  │  ┌────────────────────────┐  │  │
│  │  │  Deployment Group      │  │  │
│  │  │  - EC2 Instances        │  │  │
│  │  │  - Lambda Functions     │  │  │
│  │  │  - ECS Services         │  │  │
│  │  └────────────────────────┘  │
│  │  ┌────────────────────────┐  │  │
│  │  │  AppSpec File          │  │  │
│  │  │  - Deployment steps    │  │  │
│  │  │  - Lifecycle hooks     │  │  │
│  │  └────────────────────────┘  │
│  └──────────────────────────────┘  │
└─────────────────────────────────────┘
        │
        │ Deploy
        │
        ▼
┌─────────────────────────────────────┐
│    Target Instances                   │
│  ┌──────────┐  ┌──────────┐      │
│  │  EC2-1   │  │  EC2-2   │      │
│  │          │  │          │      │
│  │  CodeDeploy│  │  CodeDeploy│      │
│  │  Agent    │  │  Agent    │      │
│  └──────────┘  └──────────┘      │
└─────────────────────────────────────┘

Deployment Strategies:
- In-place: Update existing instances
- Blue/Green: New instances, then switch
- Rolling: Gradual rollout
- All-at-once: All instances simultaneously
```

### CodeDeploy Deployment Strategies
```
┌─────────────────────────────────────────────────────────────┐
│              CodeDeploy Deployment Strategies               │
└─────────────────────────────────────────────────────────────┘

IN-PLACE DEPLOYMENT:
┌─────────────────────┐
│  Before:            │
│  ┌────┐ ┌────┐     │
│  │ v1 │ │ v1 │     │
│  └────┘ └────┘     │
│                     │
│  After:             │
│  ┌────┐ ┌────┐     │
│  │ v2 │ │ v2 │     │
│  └────┘ └────┘     │
│                     │
│  - Update in place  │
│  - Downtime possible│
└─────────────────────┘

BLUE/GREEN DEPLOYMENT:
┌─────────────────────┐
│  Blue (Current):    │
│  ┌────┐ ┌────┐     │
│  │ v1 │ │ v1 │     │
│  └────┘ └────┘     │
│                     │
│  Green (New):       │
│  ┌────┐ ┌────┐     │
│  │ v2 │ │ v2 │     │
│  └────┘ └────┘     │
│                     │
│  Switch traffic     │
│  - Zero downtime    │
│  - Easy rollback    │
└─────────────────────┘

ROLLING DEPLOYMENT:
┌─────────────────────┐
│  Step 1:            │
│  ┌────┐ ┌────┐     │
│  │ v2 │ │ v1 │     │
│  └────┘ └────┘     │
│                     │
│  Step 2:            │
│  ┌────┐ ┌────┐     │
│  │ v2 │ │ v2 │     │
│  └────┘ └────┘     │
│                     │
│  - Gradual rollout  │
│  - Reduced risk     │
└─────────────────────┘
```

### CodeDeploy AppSpec File
```
┌─────────────────────────────────────────────────────────────┐
│              CodeDeploy AppSpec File                        │
└─────────────────────────────────────────────────────────────┘

appspec.yml (EC2/On-Premises):
┌─────────────────────────────────────┐
│  version: 0.0                       │
│  os: linux                          │
│                                      │
│  files:                              │
│    - source: /                       │
│      destination: /var/www/html     │
│                                      │
│  hooks:                             │
│    BeforeInstall:                    │
│      - location: scripts/install_dependencies.sh│
│        timeout: 300                  │
│        runas: root                   │
│                                      │
│    AfterInstall:                     │
│      - location: scripts/configure_app.sh│
│        timeout: 300                  │
│        runas: root                   │
│                                      │
│    ApplicationStart:                 │
│      - location: scripts/start_server.sh│
│        timeout: 300                  │
│        runas: root                   │
│                                      │
│    ApplicationStop:                  │
│      - location: scripts/stop_server.sh│
│        timeout: 300                  │
│        runas: root                   │
└─────────────────────────────────────┘

appspec.yml (ECS):
┌─────────────────────────────────────┐
│  version: 1.0                       │
│                                      │
│  Resources:                         │
│    - TargetService:                  │
│        Type: AWS::ECS::Service      │
│        Properties:                  │
│          TaskDefinition: <TASK_DEF> │
│          LoadBalancerInfo:           │
│            ContainerName: "web"     │
│            ContainerPort: 80        │
└─────────────────────────────────────┘
```

---

## DevOps Services Summary

### Use Case Matrix
```
┌─────────────────────────────────────────────────────────────┐
│              DevOps Services Use Cases                      │
└─────────────────────────────────────────────────────────────┘

CodePipeline:
✓ Continuous delivery
✓ Automated workflows
✓ Multi-stage pipelines
✓ Integration with AWS services
✓ Manual approvals

CodeBuild:
✓ Build & compile
✓ Run tests
✓ Package artifacts
✓ Create Docker images
✓ Fully managed builds

CodeDeploy:
✓ Application deployment
✓ EC2/ECS/Lambda deployment
✓ Blue/green deployments
✓ Rolling deployments
✓ Zero-downtime deployments
```

---

**Next: Part 9 will cover Serverless Services (Lambda, Step Functions, AppSync).**

