# Distributed Systems - In-Depth Diagrams (Part 5: Consensus Algorithms)

## 🤝 Consensus Algorithms: Raft, Paxos, Byzantine Fault Tolerance

---

## 1. Consensus Problem

### The Consensus Challenge
```
┌─────────────────────────────────────────────────────────────┐
│              Consensus Problem                             │
└─────────────────────────────────────────────────────────────┘

Multiple Nodes:
┌──────────┐    ┌──────────┐    ┌──────────┐
│ Node 1   │    │ Node 2   │    │ Node 3   │
│          │    │          │    │          │
│ Value: ? │    │ Value: ? │    │ Value: ? │
└──────────┘    └──────────┘    └──────────┘
     │              │              │
     └──────────────┴──────────────┘
                │
                ▼
    How to agree on a value?
    How to handle failures?
    How to maintain consistency?
```

### Consensus Requirements
```
┌─────────────────────────────────────────────────────────────┐
│              Consensus Requirements                        │
└─────────────────────────────────────────────────────────────┘

1. Agreement:
   All non-faulty nodes agree on same value

2. Validity:
   If all nodes propose same value, that value is chosen

3. Termination:
   Algorithm eventually terminates

4. Integrity:
   A value can be chosen at most once

Additional (for practical systems):
- Fault tolerance
- Performance
- Understandability
```

---

## 2. Raft Algorithm

### Raft Overview
```
┌─────────────────────────────────────────────────────────────┐
│              Raft Algorithm                                │
└─────────────────────────────────────────────────────────────┘

Raft States:
┌──────────┐
│ Follower │◄────┐
└────┬─────┘     │
     │           │
     │ Vote      │ Timeout
     │           │
     ▼           │
┌──────────┐     │
│Candidate │─────┘
└────┬─────┘
     │
     │ Majority votes
     │
     ▼
┌──────────┐
│ Leader   │
└──────────┘

Leader:
- Handles all client requests
- Replicates log to followers
- Manages heartbeats

Follower:
- Receives log entries from leader
- Votes in elections
- Passive role

Candidate:
- Temporary state during election
- Requests votes
- Becomes leader or follower
```

### Raft Election
```
┌─────────────────────────────────────────────────────────────┐
│              Raft Leader Election                          │
└─────────────────────────────────────────────────────────────┘

Initial State (Leader fails):
┌──────────┐    ┌──────────┐    ┌──────────┐
│ Node 1   │    │ Node 2   │    │ Node 3   │
│ Follower │    │ Follower │    │ Follower │
│          │    │          │    │          │
│ No leader│    │ No leader│    │ No leader│
└──────────┘    └──────────┘    └──────────┘
     │              │              │
     │              │              │
     │ (Timeout)    │ (Timeout)    │ (Timeout)
     │              │              │
     ▼              ▼              ▼
┌──────────┐    ┌──────────┐    ┌──────────┐
│Candidate │    │Candidate │    │Candidate │
│ Term: 2  │    │ Term: 2  │    │ Term: 2  │
└────┬─────┘    └────┬─────┘    └────┬─────┘
     │              │              │
     │ Request Vote │ Request Vote │ Request Vote
     │              │              │
     └──────────────┴──────────────┘
                │
                ▼
    Node 2 gets majority (2 votes)
                │
                ▼
┌──────────┐    ┌──────────┐    ┌──────────┐
│ Node 1   │    │ Node 2   │    │ Node 3   │
│ Follower │    │ Leader   │    │ Follower │
│ Term: 2  │    │ Term: 2  │    │ Term: 2  │
└──────────┘    └──────────┘    └──────────┘
```

### Raft Log Replication
```
┌─────────────────────────────────────────────────────────────┐
│              Raft Log Replication                          │
└─────────────────────────────────────────────────────────────┘

Client Request:
┌──────────┐
│ Client   │: Write X = 5
└────┬─────┘
     │
     ▼
┌──────────┐
│ Leader   │: Append to log
│          │
│  ┌─────┐ │
│  │Log  │ │  [1: X=1] [2: Y=2] [3: X=5]
│  └─────┘ │
│          │
└────┬─────┘
     │
     ├───► AppendEntries ────► Follower 1
     │
     ├───► AppendEntries ────► Follower 2
     │
     ▼
┌──────────┐
│Follower  │: Append to log
│          │: Send ACK
└────┬─────┘
     │
     ▼
┌──────────┐
│ Leader   │: Receive majority ACKs
│          │: Apply to state machine
│          │: Return success to client
└──────────┘
```

### Raft Safety Properties
```
┌─────────────────────────────────────────────────────────────┐
│              Raft Safety                                    │
└─────────────────────────────────────────────────────────────┘

Election Safety:
- At most one leader per term
- Leader has majority votes

Log Matching:
- If two logs have same entry at index, all preceding entries match
- Leader never overwrites committed entries

Leader Completeness:
- Committed entries from previous terms are present in new leader's log

State Machine Safety:
- If server applies log entry at index, no other server applies different entry at same index
```

---

## 3. Paxos Algorithm

### Paxos Overview
```
┌─────────────────────────────────────────────────────────────┐
│              Paxos Algorithm                               │
└─────────────────────────────────────────────────────────────┘

Paxos Roles:
┌──────────┐
│ Proposer │  ← Proposes values
└──────────┘
┌──────────┐
│ Acceptor │  ← Accepts proposals
└──────────┘
┌──────────┐
│ Learner  │  ← Learns chosen value
└──────────┘

Phases:
1. Prepare Phase (Promise)
2. Accept Phase (Accept)
```

### Paxos Basic Algorithm
```
┌─────────────────────────────────────────────────────────────┐
│              Paxos Two-Phase Flow                          │
└─────────────────────────────────────────────────────────────┘

Phase 1: Prepare
┌──────────┐
│ Proposer │: Generate proposal number N
│          │: Send Prepare(N) to acceptors
└────┬─────┘
     │
     ├───► Prepare(N) ────► Acceptor 1
     │
     ├───► Prepare(N) ────► Acceptor 2
     │
     └───► Prepare(N) ────► Acceptor 3
          │
          ▼
┌──────────┐
│ Acceptor │: If N > highest seen, promise
│          │: Send Promise(N, value) or Promise(N, null)
└──────────┘

Phase 2: Accept
┌──────────┐
│ Proposer │: If majority promised
│          │: Send Accept(N, value) to acceptors
└────┬─────┘
     │
     ├───► Accept(N, value) ────► Acceptor 1
     │
     ├───► Accept(N, value) ────► Acceptor 2
     │
     └───► Accept(N, value) ────► Acceptor 3
          │
          ▼
┌──────────┐
│ Acceptor │: If N >= highest promised, accept
│          │: Send Accepted(N, value)
└──────────┘
     │
     ▼
┌──────────┐
│ Learner  │: Learn chosen value
└──────────┘
```

### Paxos Safety
```
┌─────────────────────────────────────────────────────────────┐
│              Paxos Safety Properties                        │
└─────────────────────────────────────────────────────────────┘

Validity:
- Only proposed values can be chosen

Agreement:
- At most one value is chosen

Termination:
- If majority of acceptors are non-faulty,
  and proposer doesn't fail, value is eventually chosen

Liveness:
- Requires unique proposal numbers
- May need leader election for liveness
```

---

## 4. Multi-Paxos

### Multi-Paxos Overview
```
┌─────────────────────────────────────────────────────────────┐
│              Multi-Paxos                                   │
└─────────────────────────────────────────────────────────────┘

Problem with Basic Paxos:
- One consensus per value
- Multiple rounds for sequence

Solution: Multi-Paxos
- Elect stable leader
- Leader coordinates all proposals
- Skip prepare phase for most entries
```

### Multi-Paxos Flow
```
┌─────────────────────────────────────────────────────────────┐
│              Multi-Paxos Operation                         │
└─────────────────────────────────────────────────────────────┘

Leader Election (once):
┌──────────┐
│ Leader   │: Elected via Paxos
└──────────┘

Log Entries (many):
┌──────────┐
│ Leader   │: Propose entry 1
│          │: Propose entry 2
│          │: Propose entry 3
│          │: ...
└────┬─────┘
     │
     ├───► Accept(1, value) ────► Acceptors
     │
     ├───► Accept(2, value) ────► Acceptors
     │
     └───► Accept(3, value) ────► Acceptors

Benefits:
- Single prepare phase (leader election)
- Multiple accept phases (log entries)
- Better performance
```

---

## 5. Byzantine Fault Tolerance (BFT)

### Byzantine Problem
```
┌─────────────────────────────────────────────────────────────┐
│              Byzantine Generals Problem                     │
└─────────────────────────────────────────────────────────────┘

Scenario:
    General A          General B          General C
    ┌──────┐           ┌──────┐           ┌──────┐
    │      │           │      │           │      │
    │  A   │──────────►│  B   │──────────►│  C   │
    │      │           │      │           │      │
    └──────┘           └──────┘           └──────┘

Problem:
- Some generals may be traitors (faulty)
- Traitors can send conflicting messages
- Need to agree on attack/retreat

Byzantine Failure:
- Node can behave arbitrarily
- Can send different messages to different nodes
- Can lie, delay, or not respond
```

### BFT Requirements
```
┌─────────────────────────────────────────────────────────────┐
│              BFT Requirements                               │
└─────────────────────────────────────────────────────────────┘

Fault Model:
- f Byzantine nodes
- n total nodes
- Need: n ≥ 3f + 1

Why 3f + 1?
- f Byzantine nodes
- Need f + 1 honest nodes to outvote
- But Byzantine nodes can lie
- Need 2f + 1 honest nodes
- Total: 2f + 1 + f = 3f + 1
```

### Practical Byzantine Fault Tolerance (PBFT)
```
┌─────────────────────────────────────────────────────────────┐
│              PBFT Algorithm                                 │
└─────────────────────────────────────────────────────────────┘

Phases:
1. Pre-Prepare
2. Prepare
3. Commit

Pre-Prepare:
┌──────────┐
│ Primary  │: Assign sequence number
│          │: Broadcast Pre-Prepare
└────┬─────┘
     │
     ├───► Pre-Prepare ────► Replica 1
     │
     ├───► Pre-Prepare ────► Replica 2
     │
     └───► Pre-Prepare ────► Replica 3

Prepare:
┌──────────┐
│ Replica  │: Validate Pre-Prepare
│          │: Broadcast Prepare
└────┬─────┘
     │
     ├───► Prepare ────► All replicas
     │
     ▼
    Collect 2f + 1 Prepares

Commit:
┌──────────┐
│ Replica  │: Broadcast Commit
└────┬─────┘
     │
     ├───► Commit ────► All replicas
     │
     ▼
    Collect 2f + 1 Commits
         │
         ▼
    Execute request
```

---

## 6. Raft vs Paxos vs BFT

### Comparison Table
```
┌─────────────────────────────────────────────────────────────┐
│              Algorithm Comparison                           │
└─────────────────────────────────────────────────────────────┘

Feature          Raft          Paxos         BFT
─────────────────────────────────────────────────────────
Understandability High         Low           Medium
Leader Election   Yes          Optional      Yes
Fault Tolerance   f failures   f failures    f Byzantine
                  (n ≥ 2f+1)   (n ≥ 2f+1)    (n ≥ 3f+1)
Performance       High         High          Lower
Use Cases         etcd,        Chubby,       Blockchain,
                  Consul       Spanner       Cryptocurrency
Complexity        Medium       High          High
```

### When to Use
```
┌─────────────────────────────────────────────────────────────┐
│              When to Use Which                             │
└─────────────────────────────────────────────────────────────┘

Use Raft when:
- Need understandable consensus
- Crash failures only
- Strong consistency needed
- Examples: etcd, Consul

Use Paxos when:
- Need proven algorithm
- Crash failures only
- Complex but flexible
- Examples: Chubby, Spanner

Use BFT when:
- Byzantine failures possible
- Malicious nodes
- Blockchain, cryptocurrency
- Examples: Bitcoin, Ethereum
```

---

## 7. Consensus in Practice

### Raft Implementation (etcd)
```
┌─────────────────────────────────────────────────────────────┐
│              etcd with Raft                                │
└─────────────────────────────────────────────────────────────┘

etcd Cluster:
┌──────────┐    ┌──────────┐    ┌──────────┐
│ etcd-1   │    │ etcd-2   │    │ etcd-3   │
│          │    │          │    │          │
│ Leader   │    │ Follower │    │ Follower │
│          │    │          │    │          │
└──────────┘    └──────────┘    └──────────┘
     │              │              │
     └──────────────┴──────────────┘
                │
                ▼
         Client Request
                │
                ▼
         Leader handles
                │
                ▼
         Replicate to followers
                │
                ▼
         Commit when majority
```

### Paxos Implementation (Chubby)
```
┌─────────────────────────────────────────────────────────────┐
│              Chubby with Paxos                             │
└─────────────────────────────────────────────────────────────┘

Chubby Cell:
┌──────────┐
│ Master   │  ← Leader (elected via Paxos)
└────┬─────┘
     │
     ├───► Replicas (followers)
     │
     ▼
    Handles all requests
    Replicates via Multi-Paxos
```

---

## Key Takeaways

### Consensus Summary
```
┌─────────────────────────────────────────────────────────────┐
│              Key Concepts                                   │
└─────────────────────────────────────────────────────────────┘

Raft:
- Understandable
- Leader-based
- Good for most use cases
- Crash failures

Paxos:
- Proven correct
- More complex
- Flexible
- Crash failures

BFT:
- Handles Byzantine failures
- More expensive (3f+1)
- Lower performance
- Malicious nodes
```

---

**Next: Part 6 will cover Distributed Locking.**

