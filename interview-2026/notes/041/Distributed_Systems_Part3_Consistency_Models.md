# Distributed Systems - In-Depth Diagrams (Part 3: Consistency Models)

## 🔗 Consistency Models: Strong, Eventual, Causal, Read-Your-Writes

---

## 1. Consistency Models Overview

### Consistency Model Hierarchy
```
┌─────────────────────────────────────────────────────────────┐
│              Consistency Model Spectrum                     │
└─────────────────────────────────────────────────────────────┘

Strongest ────────────────────────────────────────► Weakest

Linearizability
    │
    ├──► Sequential Consistency
    │
    ├──► Causal Consistency
    │
    ├──► Read-Your-Writes
    │
    ├──► Session Consistency
    │
    ├──► Monotonic Reads
    │
    ├──► Monotonic Writes
    │
    └──► Eventual Consistency

Performance ────────────────────────────────────────► Scalability
```

---

## 2. Strong Consistency

### Strong Consistency Model
```
┌─────────────────────────────────────────────────────────────┐
│              Strong Consistency                            │
└─────────────────────────────────────────────────────────────┘

Timeline:
T1: Client 1 writes X = 5
    │
    │
    ▼
┌──────────┐    ┌──────────┐    ┌──────────┐
│ Node A   │───►│ Node B   │───►│ Node C   │
│ X = 5    │    │ X = 5    │    │ X = 5    │
└──────────┘    └──────────┘    └──────────┘
     │              │              │
     │              │              │
     └──────────────┴──────────────┘
                │
                ▼
T2: Client 2 reads X
    │
    │
    ▼
┌──────────┐
│ Node B   │───► Returns X = 5 (always latest)
│ X = 5    │
└──────────┘

Guarantee:
- All reads see the most recent write
- No stale data
- Synchronous replication
```

### Strong Consistency Implementation
```
┌─────────────────────────────────────────────────────────────┐
│              Strong Consistency Pattern                    │
└─────────────────────────────────────────────────────────────┘

Write Operation:
┌──────────┐
│ Client   │: Write X = 5
└────┬─────┘
     │
     ▼
┌──────────┐
│ Primary  │: Write X = 5
│ Node     │
└────┬─────┘
     │
     ├───► Replicate to Node B (wait for ack)
     │
     ├───► Replicate to Node C (wait for ack)
     │
     └───► All nodes acknowledge
          │
          ▼
     Commit (return success to client)

Read Operation:
┌──────────┐
│ Client   │: Read X
└────┬─────┘
     │
     ▼
┌──────────┐
│ Any Node │: Read X = 5 (always latest)
└──────────┘

All nodes have same value
```

---

## 3. Eventual Consistency

### Eventual Consistency Model
```
┌─────────────────────────────────────────────────────────────┐
│              Eventual Consistency                          │
└─────────────────────────────────────────────────────────────┘

Timeline:
T0: Client 1 writes X = 5
    │
    │
    ▼
┌──────────┐    ┌──────────┐    ┌──────────┐
│ Node A   │    │ Node B   │    │ Node C   │
│ X = 5    │    │ X = 3    │    │ X = 3    │
└──────────┘    └──────────┘    └──────────┘
     │              │              │
     │              │ (Async replication)
     │              │

T1: Client 2 reads X from Node B
    │
    │
    ▼
┌──────────┐
│ Node B   │───► Returns X = 3 (stale!)
│ X = 3    │
└──────────┘

T2: Replication completes
┌──────────┐    ┌──────────┐    ┌──────────┐
│ Node A   │    │ Node B   │    │ Node C   │
│ X = 5    │───►│ X = 5    │───►│ X = 5    │
└──────────┘    └──────────┘    └──────────┘
     │              │              │
     └──────────────┴──────────────┘
                │
                ▼
         Eventually consistent

Guarantee:
- System will become consistent
- Temporary inconsistency allowed
- Asynchronous replication
```

### Eventual Consistency Trade-offs
```
┌─────────────────────────────────────────────────────────────┐
│              Eventual Consistency Benefits                  │
└─────────────────────────────────────────────────────────────┘

✓ High availability
✓ Low latency
✓ High throughput
✓ Better scalability
✓ Works during network partitions

✗ May return stale data
✗ No guarantee of immediate consistency
✗ Requires conflict resolution
```

---

## 4. Causal Consistency

### Causal Consistency Model
```
┌─────────────────────────────────────────────────────────────┐
│              Causal Consistency                            │
└─────────────────────────────────────────────────────────────┘

Causal Relationship:
┌──────────┐
│ Client 1 │: Write X = 5
└────┬─────┘
     │
     │ (causes)
     │
     ▼
┌──────────┐
│ Client 2 │: Read X = 5, then Write Y = X + 1 = 6
└────┬─────┘
     │
     │ (causally related)
     │
     ▼
┌──────────┐
│ Client 3 │: Must see X = 5 before Y = 6
└──────────┘

Guarantee:
- Causally related operations appear in order
- Unrelated operations can be reordered
- Stronger than eventual, weaker than sequential
```

### Causal Consistency Example
```
┌─────────────────────────────────────────────────────────────┐
│              Causal Consistency Timeline                    │
└─────────────────────────────────────────────────────────────┘

Node A:                    Node B:                    Node C:
─────────────────────────────────────────────────────────────
T1: Write X = 5            T2: Read X = 5
                           T3: Write Y = 6
                                                      T4: Read Y = 6
                                                      T5: Read X = 5 ✓
                                                      (Must see X before Y)

But:
T6: Write Z = 10 (unrelated)
                                                      T7: Read Z = 10
                                                      (Can see Z before X/Y)
                                                      (No causal relation)
```

### Causal Consistency Implementation
```
┌─────────────────────────────────────────────────────────────┐
│              Vector Clocks for Causal Consistency          │
└─────────────────────────────────────────────────────────────┘

Vector Clock: [Node A, Node B, Node C]

Initial: [0, 0, 0]

Node A writes X = 5:
[1, 0, 0]  ← Increment own counter

Node B reads X = 5, writes Y = 6:
[1, 1, 0]  ← Merge with A's clock, increment own

Node C reads Y = 6:
[1, 1, 0]  ← Must have seen [1, 0, 0] first
            (X = 5 before Y = 6)

Causal Order:
Operation A happens before B if:
- Vector clock of A < Vector clock of B
- All components <= and at least one <
```

---

## 5. Read-Your-Writes Consistency

### Read-Your-Writes Model
```
┌─────────────────────────────────────────────────────────────┐
│              Read-Your-Writes Consistency                  │
└─────────────────────────────────────────────────────────────┘

Client Session:
┌──────────┐
│ Client 1 │: Write X = 5
└────┬─────┘
     │
     │
     ▼
┌──────────┐    ┌──────────┐    ┌──────────┐
│ Node A   │    │ Node B   │    │ Node C   │
│ X = 5    │    │ X = 3    │    │ X = 3    │
└──────────┘    └──────────┘    └──────────┘
     │              │              │
     │              │ (Replicating)
     │              │

Immediately after:
┌──────────┐
│ Client 1 │: Read X
└────┬─────┘
     │
     │ (Must read own write)
     │
     ▼
┌──────────┐
│ Node A   │───► Returns X = 5 (own write)
│ X = 5    │
└──────────┘

Guarantee:
- After writing, you always read your own writes
- Other clients may read stale data
- Session-based guarantee
```

### Read-Your-Writes Implementation
```
┌─────────────────────────────────────────────────────────────┐
│              Read-Your-Writes Pattern                      │
└─────────────────────────────────────────────────────────────┘

Write Path:
┌──────────┐
│ Client   │: Write X = 5
└────┬─────┘
     │
     ▼
┌──────────┐
│ Node A   │: Write X = 5
│          │: Store client session ID
│          │: Mark as "recent write by client"
└──────────┘

Read Path:
┌──────────┐
│ Client   │: Read X
└────┬─────┘
     │
     │ (Check session)
     │
     ▼
┌──────────┐
│ Node A   │: Check if client recently wrote
│          │: If yes, return own write
│          │: If no, return latest value
└──────────┘

Techniques:
- Session affinity (route to same node)
- Write-through cache
- Client-side version tracking
```

---

## 6. Sequential Consistency

### Sequential Consistency Model
```
┌─────────────────────────────────────────────────────────────┐
│              Sequential Consistency                        │
└─────────────────────────────────────────────────────────────┘

Operations from all clients:
┌──────────┐
│ Client 1 │: Write X = 1
│ Client 2 │: Write X = 2
│ Client 1 │: Write Y = 3
│ Client 2 │: Write Y = 4
└──────────┘

Valid Sequential Order:
Option 1: W(X=1), W(X=2), W(Y=3), W(Y=4)
Option 2: W(X=2), W(X=1), W(Y=4), W(Y=3)
Option 3: W(X=1), W(Y=3), W(X=2), W(Y=4)

All nodes must see operations in SOME valid sequential order
(Not necessarily real-time order)

Guarantee:
- All operations appear in some sequential order
- All nodes see same order
- Program order preserved per client
```

### Sequential Consistency Example
```
┌─────────────────────────────────────────────────────────────┐
│              Sequential Consistency Timeline                │
└─────────────────────────────────────────────────────────────┘

Client 1:                    Client 2:
─────────────────────────────────────────────────────────────
W(X=1)                      W(X=2)
W(Y=3)                      W(Y=4)

All nodes see one of these orders:
Node A: W(X=1), W(X=2), W(Y=3), W(Y=4)
Node B: W(X=1), W(X=2), W(Y=3), W(Y=4)
Node C: W(X=1), W(X=2), W(Y=3), W(Y=4)

OR

Node A: W(X=2), W(X=1), W(Y=4), W(Y=3)
Node B: W(X=2), W(X=1), W(Y=4), W(Y=3)
Node C: W(X=2), W(X=1), W(Y=4), W(Y=3)

All nodes agree on the order
```

---

## 7. Monotonic Reads

### Monotonic Reads Consistency
```
┌─────────────────────────────────────────────────────────────┐
│              Monotonic Reads                                │
└─────────────────────────────────────────────────────────────┘

Client Session:
┌──────────┐
│ Client 1 │: Read X = 3 (from Node A)
└────┬─────┘
     │
     │
     ▼
┌──────────┐
│ Node A   │: X = 3
└──────────┘

Later:
┌──────────┐
│ Client 1 │: Read X (from Node B)
└────┬─────┘
     │
     │ (Must be >= 3)
     │
     ▼
┌──────────┐
│ Node B   │: X = 5 ✓ (newer or equal)
└──────────┘

Invalid:
┌──────────┐
│ Node B   │: X = 2 ✗ (older - violates monotonic)
└──────────┘

Guarantee:
- Once you read a value, you'll never read an older value
- Time doesn't go backwards for a client
```

---

## 8. Monotonic Writes

### Monotonic Writes Consistency
```
┌─────────────────────────────────────────────────────────────┐
│              Monotonic Writes                                │
└─────────────────────────────────────────────────────────────┘

Client Session:
┌──────────┐
│ Client 1 │: Write X = 1
└────┬─────┘
     │
     │
     ▼
┌──────────┐
│ Node A   │: X = 1
└──────────┘

┌──────────┐
│ Client 1 │: Write X = 2
└────┬─────┘
     │
     │ (Must be applied after X = 1)
     │
     ▼
┌──────────┐
│ Node B   │: X = 2 (applied after X = 1) ✓
└──────────┘

Invalid:
┌──────────┐
│ Node B   │: X = 2 (applied before X = 1) ✗
└──────────┘

Guarantee:
- Writes from same client are applied in order
- No reordering of writes from same client
```

---

## 9. Session Consistency

### Session Consistency Model
```
┌─────────────────────────────────────────────────────────────┐
│              Session Consistency                            │
└─────────────────────────────────────────────────────────────┘

Session Properties:
┌──────────┐
│ Session  │
│          │
│  ┌─────┐ │
│  │ RYW │ │  ← Read-Your-Writes
│  └─────┘ │
│  ┌─────┐ │
│  │ MR  │ │  ← Monotonic Reads
│  └─────┘ │
│  ┌─────┐ │
│  │ MW  │ │  ← Monotonic Writes
│  └─────┘ │
│          │
└──────────┘

Within a session:
- Read-your-writes
- Monotonic reads
- Monotonic writes

Across sessions:
- No guarantees
- Eventual consistency
```

### Session Consistency Example
```
┌─────────────────────────────────────────────────────────────┐
│              Session Consistency Flow                       │
└─────────────────────────────────────────────────────────────┘

Session 1:
┌──────────┐
│ Client 1 │: Write X = 5
│          │: Read X = 5 ✓ (RYW)
│          │: Read X = 5 ✓ (MR - not older)
│          │: Write X = 7
│          │: Read X = 7 ✓ (RYW)
└──────────┘

Session 2 (different client):
┌──────────┐
│ Client 2 │: Read X = 3 (stale - OK, different session)
│          │: Read X = 5 (eventually consistent)
└──────────┘
```

---

## 10. Consistency Model Comparison

### Comparison Table
```
┌─────────────────────────────────────────────────────────────┐
│              Consistency Models Comparison                  │
└─────────────────────────────────────────────────────────────┘

Model              Guarantee                    Performance
─────────────────────────────────────────────────────────────
Linearizability   Strongest, real-time order    Lowest
Sequential        All see same order            Low
Causal            Causal order preserved        Medium
Read-Your-Writes  Own writes visible            Medium
Session           RYW + MR + MW in session     Medium
Monotonic Reads   No older reads                Medium-High
Monotonic Writes  Writes in order               Medium-High
Eventual          Eventually consistent         Highest
```

### Use Case Mapping
```
┌─────────────────────────────────────────────────────────────┐
│              When to Use Which Model                        │
└─────────────────────────────────────────────────────────────┘

Linearizability:
- Financial transactions
- Leader election
- Distributed locks

Sequential:
- Multi-player games
- Collaborative editing

Causal:
- Social media feeds
- Comment threads

Read-Your-Writes:
- User profiles
- Shopping carts

Session:
- Web applications
- User sessions

Eventual:
- CDN
- Caching
- Analytics
```

---

## Key Takeaways

### Consistency Model Selection
```
┌─────────────────────────────────────────────────────────────┐
│              Selection Criteria                             │
└─────────────────────────────────────────────────────────────┘

Choose Strong Consistency when:
- Data correctness is critical
- Can tolerate lower performance
- Financial, medical data

Choose Eventual Consistency when:
- High availability needed
- High performance needed
- Can tolerate temporary inconsistency
- Social media, content delivery

Choose Causal Consistency when:
- Need causal relationships
- Better than eventual, cheaper than strong
- Social feeds, comments
```

---

**Next: Part 4 will cover Distributed Transactions.**

