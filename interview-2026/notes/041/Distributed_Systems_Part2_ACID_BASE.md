# Distributed Systems - In-Depth Diagrams (Part 2: ACID vs BASE)

## 🔄 ACID vs BASE: Transaction Guarantees

---

## 1. ACID Properties

### ACID Overview
```
┌─────────────────────────────────────────────────────────────┐
│              ACID Properties                                │
└─────────────────────────────────────────────────────────────┘

A - Atomicity:     All or nothing
C - Consistency:    Valid state transitions
I - Isolation:      Concurrent transactions don't interfere
D - Durability:     Committed changes persist

┌─────────────────────────────────────────────────────────────┐
│              ACID Transaction Flow                           │
└─────────────────────────────────────────────────────────────┘

    Begin Transaction
    │
    │
    ├──► Read Account A: $100
    ├──► Read Account B: $50
    ├──► Write Account A: $100 - $30 = $70
    ├──► Write Account B: $50 + $30 = $80
    │
    │
    Commit Transaction
    │
    │
    └──► All changes persist
         OR
         Rollback Transaction
         │
         └──► All changes reverted
```

---

## 2. Atomicity (A)

### Atomic Transaction
```
┌─────────────────────────────────────────────────────────────┐
│              Atomicity: All or Nothing                     │
└─────────────────────────────────────────────────────────────┘

Transaction: Transfer $100 from A to B

Step 1: Debit Account A
┌──────────┐
│ Account A│: $1000 - $100 = $900
└──────────┘

Step 2: Credit Account B
┌──────────┐
│ Account B│: $500 + $100 = $600
└──────────┘

If Step 1 succeeds but Step 2 fails:
┌──────────┐
│ Account A│: $900 (partially updated)
└──────────┘
┌──────────┐
│ Account B│: $500 (not updated)
└──────────┘

❌ INCONSISTENT STATE

With Atomicity:
┌──────────┐
│ Account A│: $1000 (rolled back)
└──────────┘
┌──────────┐
│ Account B│: $500 (unchanged)
└──────────┘

✅ ALL OR NOTHING
```

### Atomicity Implementation
```
┌─────────────────────────────────────────────────────────────┐
│              Atomicity Mechanisms                          │
└─────────────────────────────────────────────────────────────┘

Write-Ahead Log (WAL):
┌──────────┐
│ Database │
│          │
│  ┌─────┐ │
│  │ WAL │ │  ← Log all changes before commit
│  └─────┘ │
│          │
└──────────┘

Process:
1. Write to log: "Debit A: $100"
2. Write to log: "Credit B: $100"
3. Commit log
4. Apply changes to database
5. If failure: Rollback using log

Undo/Redo Logs:
- Undo: Rollback incomplete transactions
- Redo: Replay committed transactions
```

---

## 3. Consistency (C)

### Consistency in ACID
```
┌─────────────────────────────────────────────────────────────┐
│              Consistency: Valid State Transitions          │
└─────────────────────────────────────────────────────────────┘

Before Transaction:
┌──────────┐    ┌──────────┐
│ Account A│    │ Account B│
│ $1000    │    │ $500     │
└──────────┘    └──────────┘
     │              │
     └──────────────┘
    Total: $1500

Transaction: Transfer $100
┌──────────┐    ┌──────────┐
│ Account A│    │ Account B│
│ $900     │    │ $600     │
└──────────┘    └──────────┘
     │              │
     └──────────────┘
    Total: $1500 ✓ (Conserved)

Invalid State (Violates Consistency):
┌──────────┐    ┌──────────┐
│ Account A│    │ Account B│
│ $900     │    │ $500     │
└──────────┘    └──────────┘
     │              │
     └──────────────┘
    Total: $1400 ✗ (Money lost!)

Constraints:
- Balance >= 0
- Total money conserved
- Foreign key constraints
- Check constraints
```

### Consistency Enforcement
```
┌─────────────────────────────────────────────────────────────┐
│              Consistency Mechanisms                         │
└─────────────────────────────────────────────────────────────┘

Database Constraints:
┌──────────┐
│ Database │
│          │
│  ┌─────┐ │
│  │ PK  │ │  ← Primary Key
│  │ FK  │ │  ← Foreign Key
│  │ CK  │ │  ← Check Constraint
│  │ UNQ │ │  ← Unique Constraint
│  └─────┘ │
│          │
└──────────┘

Application Logic:
- Business rules
- Validation
- Data integrity checks
```

---

## 4. Isolation (I)

### Isolation Levels
```
┌─────────────────────────────────────────────────────────────┐
│              Isolation Levels (Weakest to Strongest)      │
└─────────────────────────────────────────────────────────────┘

Read Uncommitted:
┌──────────┐
│ Txn 1    │: Write X = 5 (uncommitted)
└──────────┘
┌──────────┐
│ Txn 2    │: Read X = 5 (dirty read) ✗
└──────────┘

Read Committed:
┌──────────┐
│ Txn 1    │: Write X = 5 (uncommitted)
└──────────┘
┌──────────┐
│ Txn 2    │: Read X = 3 (waits for commit) ✓
└──────────┘

Repeatable Read:
┌──────────┐
│ Txn 1    │: Read X = 3
│          │: Read X = 3 (same value) ✓
└──────────┘
┌──────────┐
│ Txn 2    │: Write X = 5 (blocked)
└──────────┘

Serializable:
┌──────────┐
│ Txn 1    │: Read X, Write Y
└──────────┘
┌──────────┐
│ Txn 2    │: Read Y, Write X
└──────────┘
    │
    │ (Executed serially)
    │
    └──► No conflicts ✓
```

### Isolation Problems
```
┌─────────────────────────────────────────────────────────────┐
│              Isolation Problems                             │
└─────────────────────────────────────────────────────────────┘

Dirty Read:
┌──────────┐
│ Txn 1    │: Write X = 5 (rollback later)
└──────────┘
┌──────────┐
│ Txn 2    │: Read X = 5 (dirty data) ✗
└──────────┘

Non-Repeatable Read:
┌──────────┐
│ Txn 1    │: Read X = 3
└──────────┘
┌──────────┐
│ Txn 2    │: Write X = 5, Commit
└──────────┘
┌──────────┐
│ Txn 1    │: Read X = 5 (different!) ✗
└──────────┘

Phantom Read:
┌──────────┐
│ Txn 1    │: SELECT COUNT(*) = 10
└──────────┘
┌──────────┐
│ Txn 2    │: INSERT row, Commit
└──────────┘
┌──────────┐
│ Txn 1    │: SELECT COUNT(*) = 11 (phantom!) ✗
└──────────┘
```

---

## 5. Durability (D)

### Durability Guarantee
```
┌─────────────────────────────────────────────────────────────┐
│              Durability: Committed Changes Persist         │
└─────────────────────────────────────────────────────────────┘

Transaction Flow:
┌──────────┐
│ Client   │: Commit Transaction
└────┬─────┘
     │
     ▼
┌──────────┐
│ Database │: Write to WAL (persistent)
│          │
│  ┌─────┐ │
│  │ WAL │ │  ← Written to disk
│  └─────┘ │
│          │
└────┬─────┘
     │
     ▼
┌──────────┐
│ Database │: Apply to data pages
│          │
│  ┌─────┐ │
│  │Data │ │  ← In memory (can be lost)
│  └─────┘ │
│          │
└──────────┘
     │
     │ (System crash)
     │
     ▼
┌──────────┐
│ Recovery │: Replay from WAL
│          │
│  ┌─────┐ │
│  │ WAL │ │  ← Reconstruct state
│  └─────┘ │
│          │
└──────────┘

✅ Changes persist even after crash
```

### Durability Mechanisms
```
┌─────────────────────────────────────────────────────────────┐
│              Durability Implementation                      │
└─────────────────────────────────────────────────────────────┘

Write-Ahead Logging (WAL):
1. Write to log (disk) first
2. Then update data pages
3. Log is durable (fsync)
4. Data pages can be buffered

Checkpointing:
- Periodically flush data pages
- Mark log as checkpointed
- Faster recovery

Replication:
- Write to multiple nodes
- Durability through redundancy
```

---

## 6. BASE Properties

### BASE Overview
```
┌─────────────────────────────────────────────────────────────┐
│              BASE Properties                               │
└─────────────────────────────────────────────────────────────┘

B - Basically Available:    System is available most of the time
A - Soft State:            State may change without input
S - Eventual Consistency:   System will become consistent

┌─────────────────────────────────────────────────────────────┐
│              BASE vs ACID Philosophy                        │
└─────────────────────────────────────────────────────────────┘

ACID:                    BASE:
┌──────────┐            ┌──────────┐
│ Strong   │            │ Flexible │
│ Strict   │            │ Loose    │
│ Immediate│            │ Eventual │
│          │            │          │
│ CP       │            │ AP       │
└──────────┘            └──────────┘
```

---

## 7. Basically Available (B)

### Availability in BASE
```
┌─────────────────────────────────────────────────────────────┐
│              Basically Available                            │
└─────────────────────────────────────────────────────────────┘

System Architecture:
┌──────────┐    ┌──────────┐    ┌──────────┐
│ Node 1   │    │ Node 2   │    │ Node 3   │
│          │    │          │    │          │
└──────────┘    └──────────┘    └──────────┘
     │              │              │
     └──────────────┴──────────────┘
                │
                ▼
         Client Request
                │
                ▼
         Response (available)

Even if:
- One node is down
- Network is slow
- Data is stale

System still responds (with degraded service)
```

### Availability Trade-offs
```
┌─────────────────────────────────────────────────────────────┐
│              Availability Levels                           │
└─────────────────────────────────────────────────────────────┘

99.9% Availability (3 nines):
- 8.76 hours downtime/year
- ~43 minutes/month

99.99% Availability (4 nines):
- 52.56 minutes downtime/year
- ~4 minutes/month

99.999% Availability (5 nines):
- 5.26 minutes downtime/year
- ~26 seconds/month

BASE systems prioritize:
- High availability
- Accept degraded service
- Over strict consistency
```

---

## 8. Soft State (S)

### Soft State Concept
```
┌─────────────────────────────────────────────────────────────┐
│              Soft State                                     │
└─────────────────────────────────────────────────────────────┘

Hard State (ACID):
┌──────────┐
│ Database │
│          │
│  X = 5   │  ← Fixed, immutable until transaction
│          │
└──────────┘

Soft State (BASE):
┌──────────┐    ┌──────────┐    ┌──────────┐
│ Node 1   │    │ Node 2   │    │ Node 3   │
│          │    │          │    │          │
│  X = 5   │    │  X = 7   │    │  X = 5   │
│          │    │          │    │          │
└──────────┘    └──────────┘    └──────────┘
     │              │              │
     │              │ (Partition)
     │              │
    State can change
    without explicit input
    (replication, timeouts, etc.)
```

### Soft State Examples
```
┌─────────────────────────────────────────────────────────────┐
│              Soft State in Practice                          │
└─────────────────────────────────────────────────────────────┘

Cache Entries:
┌──────────┐
│ Cache    │
│          │
│  Key: X  │  ← Can expire
│  Val: 5  │  ← Can be stale
│  TTL: 60s│
│          │
└──────────┘

Session Data:
┌──────────┐
│ Session  │
│          │
│  User: A │  ← Can timeout
│  Data: X │  ← Can be cleared
│          │
└──────────┘

Replication Lag:
┌──────────┐    ┌──────────┐
│ Primary  │    │ Replica  │
│          │    │          │
│  X = 5   │───►│  X = 5   │  ← Eventually
│          │    │          │
└──────────┘    └──────────┘
```

---

## 9. Eventual Consistency (E)

### Eventual Consistency Flow
```
┌─────────────────────────────────────────────────────────────┐
│              Eventual Consistency                          │
└─────────────────────────────────────────────────────────────┘

Time T0: Write X = 5
┌──────────┐    ┌──────────┐    ┌──────────┐
│ Node 1   │    │ Node 2   │    │ Node 3   │
│          │    │          │    │          │
│  X = 5   │    │  X = 3   │    │  X = 3   │
│          │    │          │    │          │
└──────────┘    └──────────┘    └──────────┘
     │              │              │
     │              │ (Replicating)
     │              │

Time T1: Replication in progress
┌──────────┐    ┌──────────┐    ┌──────────┐
│ Node 1   │    │ Node 2   │    │ Node 3   │
│          │    │          │    │          │
│  X = 5   │───►│  X = 5   │    │  X = 3   │
│          │    │          │    │          │
└──────────┘    └──────────┘    └──────────┘
     │              │              │
     │              │              │ (Replicating)
     │              │              │

Time T2: Eventually consistent
┌──────────┐    ┌──────────┐    ┌──────────┐
│ Node 1   │    │ Node 2   │    │ Node 3   │
│          │    │          │    │          │
│  X = 5   │    │  X = 5   │    │  X = 5   │
│          │    │          │    │          │
└──────────┘    └──────────┘    └──────────┘
     │              │              │
     └──────────────┴──────────────┘
            All nodes consistent
```

### Eventual Consistency Variants
```
┌─────────────────────────────────────────────────────────────┐
│              Consistency Variants                          │
└─────────────────────────────────────────────────────────────┘

Causal Consistency:
If A happens before B, all nodes see A before B

Read-Your-Writes:
After writing, you always read your own writes

Session Consistency:
Within a session, reads are consistent

Monotonic Reads:
If you read X = 5, you'll never read X = 3 later

Monotonic Writes:
Writes are applied in order
```

---

## 10. ACID vs BASE Comparison

### Side-by-Side Comparison
```
┌─────────────────────────────────────────────────────────────┐
│              ACID vs BASE                                  │
└─────────────────────────────────────────────────────────────┘

Property        ACID                    BASE
─────────────────────────────────────────────────────────
Consistency     Strong                  Eventual
Availability    May block               Always available
Isolation       Strict                  Loose
Durability      Guaranteed              Best effort
Transactions    ACID transactions       No strict transactions
Use Cases       Banking, Finance        Social media, CDN
Examples        PostgreSQL, MySQL       DynamoDB, Cassandra
CAP             CP                      AP
Performance     Lower (strict)          Higher (flexible)
Complexity      Higher                  Lower
```

### When to Use ACID
```
┌─────────────────────────────────────────────────────────────┐
│              ACID Use Cases                                │
└─────────────────────────────────────────────────────────────┘

Financial Transactions:
- Money transfers
- Account balances
- Payment processing

Critical Data:
- Medical records
- Legal documents
- Inventory (exact counts)

Requirements:
- Strong consistency
- Data integrity
- Can tolerate lower availability
```

### When to Use BASE
```
┌─────────────────────────────────────────────────────────────┐
│              BASE Use Cases                                │
└─────────────────────────────────────────────────────────────┘

Social Media:
- Likes, comments
- Feeds, timelines
- User profiles

Content Delivery:
- Caching
- CDN
- Search indexes

Requirements:
- High availability
- High performance
- Can tolerate eventual consistency
```

---

## Key Takeaways

### ACID Summary
```
┌─────────────────────────────────────────────────────────────┐
│              ACID Characteristics                          │
└─────────────────────────────────────────────────────────────┘

✓ Strong consistency
✓ Data integrity
✓ Predictable behavior
✗ Lower availability (during partitions)
✗ Lower performance
✗ More complex
```

### BASE Summary
```
┌─────────────────────────────────────────────────────────────┐
│              BASE Characteristics                          │
└─────────────────────────────────────────────────────────────┘

✓ High availability
✓ High performance
✓ Scalability
✗ Eventual consistency
✗ May return stale data
✗ More complex conflict resolution
```

---

**Next: Part 3 will cover Consistency Models in detail.**

