# Distributed Systems - In-Depth Diagrams (Part 1: CAP Theorem)

## 🎯 CAP Theorem: Consistency, Availability, Partition Tolerance

---

## 1. CAP Theorem Overview

### The Three Pillars
```
┌─────────────────────────────────────────────────────────────┐
│              CAP Theorem Triangle                           │
└─────────────────────────────────────────────────────────────┘

                    Consistency (C)
                        •
                       ╱│╲
                      ╱ │ ╲
                     ╱  │  ╲
                    ╱   │   ╲
                   ╱    │    ╲
                  ╱     │     ╲
                 ╱      │      ╲
                ╱       │       ╲
               ╱        │        ╲
              ╱         │         ╲
             ╱          │          ╲
            ╱           │           ╲
           ╱            │            ╲
          ╱             │             ╲
         ╱              │              ╲
        ╱               │               ╲
       ╱                │                ╲
      ╱                 │                 ╲
     ╱                  │                  ╲
    ╱                   │                   ╲
   ╱                    │                    ╲
  ╱                     │                     ╲
 ╱                      │                      ╲
╱                       │                       ╲
•───────────────────────┴───────────────────────•
Availability (A)                    Partition Tolerance (P)

Theorem: In a distributed system, you can only guarantee 2 out of 3
```

### CAP Theorem States
```
┌─────────────────────────────────────────────────────────────┐
│              CAP Combinations                                │
└─────────────────────────────────────────────────────────────┘

CA (Consistency + Availability):
    ┌──────────┐
    │    CA    │  ← No Partition Tolerance
    │          │     (Single node or perfect network)
    └──────────┘
    Examples: Traditional RDBMS (single node)
    
CP (Consistency + Partition Tolerance):
    ┌──────────┐
    │    CP    │  ← Sacrifices Availability
    │          │     (Blocks during partitions)
    └──────────┘
    Examples: MongoDB, HBase, Redis
    
AP (Availability + Partition Tolerance):
    ┌──────────┐
    │    AP    │  ← Sacrifices Consistency
    │          │     (Eventual consistency)
    └──────────┘
    Examples: DynamoDB, Cassandra, CouchDB
```

---

## 2. Consistency (C)

### Strong Consistency
```
┌─────────────────────────────────────────────────────────────┐
│              Strong Consistency                              │
└─────────────────────────────────────────────────────────────┘

Client 1: Write X = 5
    │
    │
    ▼
┌──────────┐
│ Node A   │───► Replicate ───►┌──────────┐
│ X = 5    │                    │ Node B   │
└──────────┘                    │ X = 5    │
                                └──────────┘
                                    │
                                    │
                                    ▼
Client 2: Read X
    │
    │
    ▼
┌──────────┐
│ Node B   │───► Returns X = 5 (always latest)
│ X = 5    │
└──────────┘

All reads see the most recent write
No stale data
Synchronous replication required
```

### Eventual Consistency (Weak)
```
┌─────────────────────────────────────────────────────────────┐
│              Eventual Consistency                           │
└─────────────────────────────────────────────────────────────┘

Client 1: Write X = 5
    │
    │
    ▼
┌──────────┐
│ Node A   │───► Async Replicate ───►┌──────────┐
│ X = 5    │                         │ Node B   │
└──────────┘                         │ X = ?    │
                                     └──────────┘
                                         │
                                         │
                                         ▼
Client 2: Read X (immediately)
    │
    │
    ▼
┌──────────┐
│ Node B   │───► Returns X = 3 (stale data)
│ X = 3    │
└──────────┘

Later (eventually):
┌──────────┐
│ Node B   │───► Returns X = 5 (updated)
│ X = 5    │
└──────────┘

Temporary inconsistency allowed
Converges to consistency over time
```

---

## 3. Availability (A)

### High Availability System
```
┌─────────────────────────────────────────────────────────────┐
│              High Availability                               │
└─────────────────────────────────────────────────────────────┘

    Client Request
    │
    │
    ▼
┌──────────┐
│ Load     │
│ Balancer │
└────┬─────┘
     │
     ├──────────┬──────────┐
     │          │          │
     ▼          ▼          ▼
┌─────────┐ ┌─────────┐ ┌─────────┐
│ Node 1  │ │ Node 2  │ │ Node 3  │
│ (UP)    │ │ (UP)    │ │ (UP)    │
└─────────┘ └─────────┘ └─────────┘
     │          │          │
     └──────────┴──────────┘
                │
                ▼
         Response (always available)

System responds to every request
No downtime
Redundancy ensures availability
```

### Low Availability (Single Point of Failure)
```
┌─────────────────────────────────────────────────────────────┐
│              Low Availability                               │
└─────────────────────────────────────────────────────────────┘

    Client Request
    │
    │
    ▼
┌──────────┐
│ Node 1   │───► CRASHED
│ (DOWN)   │
└──────────┘
     │
     │
     ▼
    ERROR: Service Unavailable

Single node failure = System failure
No redundancy
Downtime during failures
```

---

## 4. Partition Tolerance (P)

### Network Partition Scenario
```
┌─────────────────────────────────────────────────────────────┐
│              Network Partition                               │
└─────────────────────────────────────────────────────────────┘

Before Partition:
┌──────────┐    ┌──────────┐    ┌──────────┐
│ Node A   │◄──►│ Node B   │◄──►│ Node C   │
│          │    │          │    │          │
└──────────┘    └──────────┘    └──────────┘
     │              │              │
     └──────────────┴──────────────┘
            (Connected)

After Partition:
┌──────────┐    ┌──────────┐    ┌──────────┐
│ Node A   │    │ Node B   │    │ Node C   │
│          │    │          │    │          │
└──────────┘    └──────────┘    └──────────┘
     │              │              │
     │              │              │
     └──────────────┘              │
    Cluster 1                      │
                                    │
                                Cluster 2

Network split creates two isolated clusters
Nodes cannot communicate across partition
System must handle this gracefully
```

---

## 5. CAP Trade-offs in Practice

### CP System (Consistency + Partition Tolerance)
```
┌─────────────────────────────────────────────────────────────┐
│              CP System Behavior                             │
└─────────────────────────────────────────────────────────────┘

During Normal Operation:
┌──────────┐    ┌──────────┐
│ Node A   │◄──►│ Node B   │
│ X = 5    │    │ X = 5    │
└──────────┘    └──────────┘
     │              │
     └──────────────┘
    Client: Read X = 5 ✓

During Partition:
┌──────────┐    ┌──────────┐
│ Node A   │    │ Node B   │
│ X = 5    │    │ X = 5    │
└──────────┘    └──────────┘
     │              │
     │              │ (Partition)
     │              │
    Client: Read X = 5 ✓
    Client: Write X = 7 ✗ (BLOCKED - unavailable)

Behavior:
- Blocks writes during partition
- Maintains consistency
- Sacrifices availability
```

### AP System (Availability + Partition Tolerance)
```
┌─────────────────────────────────────────────────────────────┐
│              AP System Behavior                             │
└─────────────────────────────────────────────────────────────┘

During Normal Operation:
┌──────────┐    ┌──────────┐
│ Node A   │◄──►│ Node B   │
│ X = 5    │    │ X = 5    │
└──────────┘    └──────────┘
     │              │
     └──────────────┘
    Client: Read X = 5 ✓

During Partition:
┌──────────┐    ┌──────────┐
│ Node A   │    │ Node B   │
│ X = 7    │    │ X = 5    │
└──────────┘    └──────────┘
     │              │
     │              │ (Partition)
     │              │
    Client: Read X = 7 (from Node A) ✓
    Client: Read X = 5 (from Node B) ✓
    Client: Write X = 7 ✓ (ACCEPTED - available)

Behavior:
- Accepts writes during partition
- May return inconsistent data
- Maintains availability
- Eventually consistent
```

### CA System (Consistency + Availability)
```
┌─────────────────────────────────────────────────────────────┐
│              CA System Behavior                             │
└─────────────────────────────────────────────────────────────┘

Single Node (No Partition):
┌──────────┐
│ Node A   │
│ X = 5    │
└──────────┘
     │
     │
    Client: Read X = 5 ✓
    Client: Write X = 7 ✓

OR

Perfect Network (No Partitions):
┌──────────┐    ┌──────────┐
│ Node A   │◄──►│ Node B   │
│ X = 5    │    │ X = 5    │
└──────────┘    └──────────┘
     │              │
     └──────────────┘
    (Always connected)

Behavior:
- Assumes no network partitions
- Not realistic for distributed systems
- Only works in single node or perfect network
```

---

## 6. Real-World Examples

### CP Systems
```
┌─────────────────────────────────────────────────────────────┐
│              CP System Examples                             │
└─────────────────────────────────────────────────────────────┘

MongoDB (with strong consistency):
┌──────────┐    ┌──────────┐
│ Primary  │◄──►│ Secondary│
│          │    │          │
└──────────┘    └──────────┘
     │
     │ (Partition)
     │
    Blocks writes
    Maintains consistency
    Unavailable during partition

HBase:
- Strong consistency
- Blocks during network issues
- CP system

Redis (with replication):
- Can be configured as CP
- Blocks during partition
```

### AP Systems
```
┌─────────────────────────────────────────────────────────────┐
│              AP System Examples                             │
└─────────────────────────────────────────────────────────────┘

DynamoDB:
┌──────────┐    ┌──────────┐    ┌──────────┐
│ Node A   │    │ Node B   │    │ Node C   │
│ X = 7    │    │ X = 5    │    │ X = 7    │
└──────────┘    └──────────┘    └──────────┘
     │              │              │
     │              │ (Partition)
     │              │
    Always available
    May return stale data
    Eventually consistent

Cassandra:
- High availability
- Eventual consistency
- AP system

CouchDB:
- Always available
- Eventual consistency
```

---

## 7. CAP Theorem Nuances

### Modern Understanding
```
┌─────────────────────────────────────────────────────────────┐
│              CAP Theorem Refinements                         │
└─────────────────────────────────────────────────────────────┘

1. CAP is about trade-offs, not absolutes:
   - Systems can be "mostly CP" or "mostly AP"
   - Consistency can be tunable
   - Availability can be percentage-based

2. Partitions are rare but must be handled:
   - Most systems are CA during normal operation
   - CP/AP choice matters during partitions

3. Modern systems use:
   - Tunable consistency (Cassandra)
   - CRDTs (Conflict-free Replicated Data Types)
   - Multi-consistency models
```

### Consistency Levels
```
┌─────────────────────────────────────────────────────────────┐
│              Consistency Spectrum                            │
└─────────────────────────────────────────────────────────────┘

Strong ────────────────► Eventual
Consistency              Consistency

Strong:     All reads see latest write
Linearizable: Strong + real-time ordering
Sequential:  Operations appear in same order
Causal:      Causally related operations ordered
Eventual:    Eventually all nodes converge
```

---

## Key Takeaways

### CAP Theorem Summary
```
┌─────────────────────────────────────────────────────────────┐
│              CAP Decision Matrix                            │
└─────────────────────────────────────────────────────────────┘

Choose CP when:
- Data consistency is critical
- Can tolerate temporary unavailability
- Examples: Financial systems, inventory

Choose AP when:
- High availability is critical
- Can tolerate temporary inconsistency
- Examples: Social media, content delivery

Remember:
- Partitions WILL happen in distributed systems
- You must choose CP or AP (not CA)
- Modern systems often provide tunable consistency
```

---

**Next: Part 2 will cover ACID vs BASE properties.**

