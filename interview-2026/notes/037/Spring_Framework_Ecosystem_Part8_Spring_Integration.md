# Spring Framework Ecosystem - Complete Guide (Part 8: Spring Integration)

## 🔗 Spring Integration: Enterprise Integration Patterns, Messaging

---

## 1. Spring Integration Overview

### Integration Architecture
```
┌─────────────────────────────────────────────────────────────┐
│              Spring Integration Architecture                │
└─────────────────────────────────────────────────────────────┘

Message
    │
    │ (payload + headers)
    ▼
┌──────────────────────┐
│  Message Channel     │
│  (Transport)         │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│  Message Endpoint    │
│  (Processing)        │
│                      │
│  - Transformer       │
│  - Filter            │
│  - Router            │
│  - Splitter          │
│  - Aggregator        │
│  - Service Activator │
└──────────┬───────────┘
           │
           ▼
    Message Channel
    │
    ▼
    External System
```

### Message Structure
```
┌─────────────────────────────────────────────────────────────┐
│              Message Structure                              │
└─────────────────────────────────────────────────────────────┘

Message<T>
    │
    ├──► Headers
    │    ┌──────────────────────┐
    │    │ id: UUID             │
    │    │ timestamp: Long      │
    │    │ contentType: String  │
    │    │ custom headers       │
    │    └──────────────────────┘
    │
    └──► Payload
         ┌──────────────────────┐
         │ T (any type)          │
         │ - String               │
         │ - Object               │
         │ - File                 │
         │ - etc.                 │
         └──────────────────────┘

Example:
Message<String> message = MessageBuilder
    .withPayload("Hello")
    .setHeader("priority", "high")
    .setHeader("source", "system")
    .build();
```

---

## 2. Message Channels

### Channel Types
```
┌─────────────────────────────────────────────────────────────┐
│              Message Channels                               │
└─────────────────────────────────────────────────────────────┘

DirectChannel (Synchronous):
    Producer ────► Channel ────► Consumer
    │                              │
    │ (blocking)                    │
    └──────────────────────────────┘
    - Point-to-point
    - Same thread
    - No buffering

QueueChannel (Asynchronous):
    Producer ────► Queue ────► Consumer
    │              │            │
    │              │ (buffer)   │
    │              │            │
    └──────────────┴────────────┘
    - Point-to-point
    - Different threads
    - Buffered

PublishSubscribeChannel:
    Producer ────► Channel
    │              │
    │              ├───► Consumer 1
    │              ├───► Consumer 2
    │              └───► Consumer 3
    - One-to-many
    - Broadcast
```

### Channel Configuration
```
┌─────────────────────────────────────────────────────────────┐
│              Channel Configuration                         │
└─────────────────────────────────────────────────────────────┘

@Configuration
@EnableIntegration
public class IntegrationConfig {
    
    // Direct Channel
    @Bean
    public MessageChannel inputChannel() {
        return new DirectChannel();
    }
    
    // Queue Channel
    @Bean
    public MessageChannel queueChannel() {
        return new QueueChannel(100); // capacity 100
    }
    
    // Publish-Subscribe Channel
    @Bean
    public MessageChannel pubSubChannel() {
        return new PublishSubscribeChannel();
    }
    
    // Executor Channel (async)
    @Bean
    public MessageChannel executorChannel() {
        return new ExecutorChannel(executor());
    }
    
    @Bean
    public Executor executor() {
        return Executors.newFixedThreadPool(10);
    }
}
```

---

## 3. Enterprise Integration Patterns

### Message Router
```
┌─────────────────────────────────────────────────────────────┐
│              Message Router Pattern                         │
└─────────────────────────────────────────────────────────────┘

    Input Channel
    │
    │ Message
    ▼
┌──────────────────────┐
│  Router              │
│  (routes based on    │
│   condition)         │
└──────────┬───────────┘
           │
    ┌──────┴──────┐
    │             │
    ▼             ▼
Channel A    Channel B
    │             │
    ▼             ▼
Service A    Service B

Configuration:
┌─────────────────────────────────────┐
│ @Bean                               │
│ public IntegrationFlow routerFlow() {│
│   return IntegrationFlows           │
│     .from("inputChannel")          │
│     .route(Message.class,          │
│       m -> m.getHeaders().get("type"),│
│       mapping -> mapping            │
│         .channelMapping("A", "channelA")│
│         .channelMapping("B", "channelB")│
│     )                               │
│     .get();                         │
│ }                                  │
└─────────────────────────────────────┘
```

### Message Splitter
```
┌─────────────────────────────────────────────────────────────┐
│              Message Splitter Pattern                      │
└─────────────────────────────────────────────────────────────┘

    Input Channel
    │
    │ Message: [item1, item2, item3]
    ▼
┌──────────────────────┐
│  Splitter            │
│  (splits collection) │
└──────────┬───────────┘
           │
    ┌──────┴──────┐
    │             │
    ▼             ▼
  item1         item2
    │             │
    ▼             ▼
  item3         ...
    │
    ▼
Output Channel

Configuration:
┌─────────────────────────────────────┐
│ @Bean                               │
│ public IntegrationFlow splitterFlow() {│
│   return IntegrationFlows           │
│     .from("inputChannel")          │
│     .split()                        │
│     .channel("outputChannel")      │
│     .get();                        │
│ }                                  │
└─────────────────────────────────────┘

Custom Splitter:
┌─────────────────────────────────────┐
│ @Component                          │
│ public class OrderSplitter {       │
│   public List<OrderItem> split(Order order) {│
│     return order.getItems();        │
│   }                                 │
│ }                                  │
└─────────────────────────────────────┘
```

### Message Aggregator
```
┌─────────────────────────────────────────────────────────────┐
│              Message Aggregator Pattern                     │
└─────────────────────────────────────────────────────────────┘

    Input Channel 1
    │
    │ item1
    ▼
┌──────────────────────┐
│                      │
│  Aggregator          │
│  (collects messages) │
│                      │
│  - Groups by         │
│    correlation ID    │
│  - Waits for all     │
│    messages          │
│  - Releases when     │
│    complete          │
└──────────┬───────────┘
           │
    Input Channel 2
    │
    │ item2
    ▼
    (same aggregator)
           │
           │ Complete message
           ▼
    Output Channel

Configuration:
┌─────────────────────────────────────┐
│ @Bean                               │
│ public IntegrationFlow aggregatorFlow() {│
│   return IntegrationFlows           │
│     .from("inputChannel")          │
│     .aggregate(aggregator ->       │
│       aggregator                   │
│         .correlationStrategy(m -> m.getHeaders().get("groupId"))│
│         .releaseStrategy(g -> g.size() == 3)│
│         .outputProcessor(g ->      │
│           new ArrayList<>(g.getMessages()))│
│     )                               │
│     .channel("outputChannel")      │
│     .get();                        │
│ }                                  │
└─────────────────────────────────────┘
```

### Message Transformer
```
┌─────────────────────────────────────────────────────────────┐
│              Message Transformer Pattern                    │
└─────────────────────────────────────────────────────────────┘

    Input Channel
    │
    │ Message<String>
    ▼
┌──────────────────────┐
│  Transformer         │
│  (converts format)   │
└──────────┬───────────┘
           │
           │ Message<User>
           ▼
    Output Channel

Configuration:
┌─────────────────────────────────────┐
│ @Bean                               │
│ public IntegrationFlow transformerFlow() {│
│   return IntegrationFlows           │
│     .from("inputChannel")          │
│     .transform(String.class,       │
│       s -> new User(s))            │
│     .channel("outputChannel")      │
│     .get();                        │
│ }                                  │
└─────────────────────────────────────┘

Custom Transformer:
┌─────────────────────────────────────┐
│ @Component                          │
│ public class UserTransformer {      │
│   @Transformer                      │
│   public User transform(String name) {│
│     return new User(name);         │
│   }                                 │
│ }                                  │
└─────────────────────────────────────┘
```

### Message Filter
```
┌─────────────────────────────────────────────────────────────┐
│              Message Filter Pattern                         │
└─────────────────────────────────────────────────────────────┘

    Input Channel
    │
    │ Messages
    ▼
┌──────────────────────┐
│  Filter              │
│  (filters messages)  │
└──────────┬───────────┘
           │
    ┌──────┴──────┐
    │             │
    ▼             ▼
  Pass          Discard
    │
    ▼
Output Channel

Configuration:
┌─────────────────────────────────────┐
│ @Bean                               │
│ public IntegrationFlow filterFlow() {│
│   return IntegrationFlows           │
│     .from("inputChannel")          │
│     .filter(Message.class,        │
│       m -> m.getHeaders().get("priority").equals("high"))│
│     .channel("outputChannel")      │
│     .get();                        │
│ }                                  │
└─────────────────────────────────────┘
```

---

## 4. File Integration

### File Adapters
```
┌─────────────────────────────────────────────────────────────┐
│              File Integration                               │
└─────────────────────────────────────────────────────────────┘

File Inbound Channel Adapter:
    File System
    │
    │ (new file created)
    ▼
┌──────────────────────┐
│  File Inbound        │
│  Adapter             │
│                      │
│  - Watches directory │
│  - Reads files        │
│  - Sends to channel   │
└──────────┬───────────┘
           │
           │ Message<File>
           ▼
    Channel

Configuration:
┌─────────────────────────────────────┐
│ @Bean                               │
│ public IntegrationFlow fileInboundFlow() {│
│   return IntegrationFlows           │
│     .from(Files.inboundAdapter(     │
│       new File("/input"))          │
│       .patternFilter("*.csv")      │
│       .preventDuplicates(true))    │
│     .channel("fileChannel")        │
│     .get();                        │
│ }                                  │
└─────────────────────────────────────┘

File Outbound Channel Adapter:
    Channel
    │
    │ Message<File>
    ▼
┌──────────────────────┐
│  File Outbound        │
│  Adapter              │
│                      │
│  - Writes to file     │
│  - Creates directory  │
│  - Handles errors     │
└──────────┬───────────┘
           │
           ▼
    File System
```

---

## 5. JMS Integration

### JMS Message Channels
```
┌─────────────────────────────────────────────────────────────┐
│              JMS Integration                               │
└─────────────────────────────────────────────────────────────┘

JMS Inbound Channel Adapter:
    JMS Queue/Topic
    │
    │ (message arrives)
    ▼
┌──────────────────────┐
│  JMS Inbound         │
│  Adapter             │
│                      │
│  - Listens to queue  │
│  - Receives message  │
│  - Converts to SI    │
│    message           │
└──────────┬───────────┘
           │
           │ Message
           ▼
    Channel

Configuration:
┌─────────────────────────────────────┐
│ @Bean                               │
│ public IntegrationFlow jmsInboundFlow() {│
│   return IntegrationFlows           │
│     .from(Jms.inboundAdapter(       │
│       connectionFactory)           │
│       .destination("queue.name"))   │
│     .channel("jmsChannel")          │
│     .get();                        │
│ }                                  │
└─────────────────────────────────────┘

JMS Outbound Channel Adapter:
    Channel
    │
    │ Message
    ▼
┌──────────────────────┐
│  JMS Outbound        │
│  Adapter             │
│                      │
│  - Converts SI       │
│    message to JMS    │
│  - Sends to queue    │
└──────────┬───────────┘
           │
           ▼
    JMS Queue/Topic
```

---

## 6. HTTP Integration

### HTTP Gateways
```
┌─────────────────────────────────────────────────────────────┐
│              HTTP Integration                               │
└─────────────────────────────────────────────────────────────┘

HTTP Inbound Gateway:
    HTTP Request
    │
    │ POST /api/process
    ▼
┌──────────────────────┐
│  HTTP Inbound        │
│  Gateway             │
│                      │
│  - Receives HTTP     │
│  - Converts to       │
│    message           │
│  - Sends to channel  │
└──────────┬───────────┘
           │
           │ Message
           ▼
    Channel
           │
           │ Response
           ▼
┌──────────────────────┐
│  HTTP Response       │
└──────────────────────┘

Configuration:
┌─────────────────────────────────────┐
│ @Bean                               │
│ public IntegrationFlow httpInboundFlow() {│
│   return IntegrationFlows           │
│     .from(Http.inboundGateway(      │
│       "/api/process")               │
│       .requestMapping(m -> m        │
│         .methods(HttpMethod.POST)))  │
│     .channel("processChannel")      │
│     .get();                        │
│ }                                  │
└─────────────────────────────────────┘

HTTP Outbound Gateway:
    Channel
    │
    │ Message
    ▼
┌──────────────────────┐
│  HTTP Outbound       │
│  Gateway             │
│                      │
│  - Converts message  │
│    to HTTP request   │
│  - Sends HTTP        │
│  - Receives response │
└──────────┬───────────┘
           │
           │ HTTP Response
           ▼
    Channel
```

---

## Key Concepts Summary

### Integration Best Practices
```
┌─────────────────────────────────────────────────────────────┐
│              Best Practices                                 │
└─────────────────────────────────────────────────────────────┘

✅ Use appropriate channels
   - DirectChannel for sync
   - QueueChannel for async
   - PublishSubscribeChannel for broadcast

✅ Error handling
   - Error channels
   - Retry mechanisms
   - Dead letter queues

✅ Message transformation
   - Use transformers for format conversion
   - Keep payloads simple

✅ Monitoring
   - Track message flow
   - Monitor channel metrics
   - Log important events

✅ Testing
   - Mock channels
   - Test individual components
   - Integration tests
```

---

**Next: Part 9 will cover Spring WebFlux - Reactive Programming, Non-blocking I/O.**

